# Database URL (Required for migrations and database operations)
# The default local Docker setup in this repo uses postgres/postgres.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tradinggoose"
