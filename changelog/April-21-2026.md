# April-21-2026

## feat/billing-PAYG @ d101833e vs origin/staging

### Summary
- Adds the personal PAYG lifecycle across `apps/tradinggoose/app/api/billing/*`, `apps/tradinggoose/lib/billing/*`, and `apps/tradinggoose/global-navbar/settings-modal/components/subscription/*`: Stripe-backed activation, shared personal customer provisioning, card-on-file awareness, onboarding-allowance reconciliation, and portal-backed manage flows.
- Tightens admin billing tier editing in `apps/tradinggoose/app/admin/billing/*` and `apps/tradinggoose/lib/admin/billing/tier-mutations.ts` so new and edited tiers keep a Stripe monthly price ID instead of allowing partially configured self-serve tiers.
- Propagates explicit Copilot streamed turn state through `apps/tradinggoose/app/api/copilot/chat/route.ts`, `apps/tradinggoose/app/api/copilot/tools/mark-complete/route.ts`, and `apps/tradinggoose/stores/copilot/*` so paused, completed, and error turns persist consistently.
- Makes `/workspace/[workspaceId]/dashboard` the canonical workspace route, hardens Monaco asset tracing for Bun installs, reduces hydration noise in `apps/tradinggoose/app/layout.tsx`, escapes landing JSON-LD, adds landing trigger examples, and switches shared Zustand stores to equality-aware APIs.

### Branch Scope
- Compared `8dd89eeb49988bc21ddd55ecf2f11fdf46c2bb96..feat/billing-PAYG`.
- The range contains 13 branch-local commits from `10244197` through `d101833e`, covering workspace routing, Copilot turn-state streaming, personal PAYG, admin billing validation, hydration cleanup, equality-aware Zustand selectors, and billing portal/customer refactors.
- The working tree was clean during this documentation pass, so no uncommitted local changes were included.
- Main areas touched: `apps/tradinggoose/app/api/billing/*`, `apps/tradinggoose/lib/billing/*`, `apps/tradinggoose/global-navbar/settings-modal/components/subscription/*`, `apps/tradinggoose/app/api/admin/billing/*`, `apps/tradinggoose/app/api/copilot/*`, `apps/tradinggoose/stores/copilot/*`, workspace routing files under `apps/tradinggoose/app/workspace/*`, `apps/tradinggoose/proxy.ts`, `apps/tradinggoose/app/monaco-editor/esm/vs/[...assetPath]/route.ts`, `apps/tradinggoose/next.config.ts`, `apps/tradinggoose/app/layout.tsx`, and the shared Zustand store/hook adapters under `apps/tradinggoose/stores/*`.

### Key Changes
- `apps/tradinggoose/app/api/billing/payg/activate/route.ts` is the new personal PAYG activation entrypoint. It reuses the default personal subscription row from `apps/tradinggoose/lib/billing/core/subscription.ts`, takes a per-user advisory lock, records `paygActivationAttemptId` in subscription metadata, reuses `apps/tradinggoose/lib/billing/stripe-customers.ts` to provision or recover the personal Stripe customer, reconciles already-created active Stripe subscriptions before creating a new one, and only persists Stripe ids after `handleSubscriptionCreated()` plus `syncSubscriptionUsageLimits()` succeed.
- Personal billing state now exposes payment-method and activation readiness explicitly. `apps/tradinggoose/app/api/billing/route.ts` adds `hasPaymentMethodOnFile` by resolving the stored user Stripe customer through `getStripeCustomerDefaultPaymentMethodId()`, `apps/tradinggoose/lib/subscription/types.ts` and `apps/tradinggoose/stores/subscription/store.ts` carry that field through the client contract, and `apps/tradinggoose/lib/billing/subscriptions/utils.ts` now blocks personal usage-limit edits unless the user has a live `stripeSubscriptionId`.
- The settings billing UI was consolidated around Stripe Billing Portal instead of custom local cancellation flows. `apps/tradinggoose/lib/billing/billing-portal.ts` is now the shared client helper, `apps/tradinggoose/app/api/billing/portal/route.ts` uses `ensureStripeUserCustomer()` under its own advisory lock for personal sessions, `apps/tradinggoose/global-navbar/settings-modal/components/subscription/payg-ui.ts` centralizes the personal PAYG badge/action state machine, and `apps/tradinggoose/global-navbar/{components/sidebar-nav.tsx,components/user-menu.tsx,settings-modal/components/team-management/components/team-usage/team-usage.tsx,settings-modal/components/subscription/subscription.tsx}` all route billing management through that shared helper.
- Personal onboarding allowance and blocked-billing handling were made first-class billing rules. `apps/tradinggoose/lib/billing/core/usage.ts` now seeds `customUsageLimit` together with `grantedOnboardingAllowanceUsd`, adds `decrementGrantedOnboardingAllowanceByCurrentPeriodUsage()` and `resetUserCustomUsageLimitToGrantedOnboardingAllowance()`, and `apps/tradinggoose/lib/billing/webhooks/subscription.ts` uses that helper when a user leaves the default/free personal path. `apps/tradinggoose/lib/billing/calculations/usage-monitor.ts` and `apps/tradinggoose/app/api/usage/route.ts` now reject execution or usage-limit changes when personal billing is blocked or the requested limit change is invalid.
- Admin billing tier creation and editing now treat the monthly Stripe price id as required configuration. `apps/tradinggoose/lib/admin/billing/tier-mutations.ts` accepts a `requireStripeMonthlyPriceId` option, `apps/tradinggoose/app/api/admin/billing/tiers/route.ts` applies that validation on create, `apps/tradinggoose/app/api/admin/billing/tiers/[id]/route.ts` keeps structural-field edits constrained for subscribed tiers, and `apps/tradinggoose/app/admin/billing/{billing-admin.tsx,tier-detail.tsx,tier-editor.tsx}` mark the monthly Stripe price field as required in the shared editor surface.
- Copilot stream lifecycle is now an explicit SSE contract instead of a store-side guess. `apps/tradinggoose/app/api/copilot/chat/route.ts` emits `turn_state` events for stream start, waiting-for-tools, completed, and error phases, `apps/tradinggoose/app/api/copilot/tools/mark-complete/route.ts` wraps continuation SSE streams with the same `turn_state` envelope, and `apps/tradinggoose/stores/copilot/{streaming.ts,store.ts,store-state.ts}` consume that state so persisted chat status, continuation handling, and error turns stay aligned.
- Workspace navigation now standardizes on `/workspace/[workspaceId]/dashboard`. `apps/tradinggoose/proxy.ts` drops the legacy `/w` matchers, `apps/tradinggoose/app/workspace/[workspaceId]/{page.tsx,layout.tsx,providers/workspace-permissions-provider.tsx,dashboard/dashboard-client.tsx,templates/[id]/template.tsx,templates/components/template-card.tsx,logs/components/dashboard/components/workflow-details.tsx}` and `apps/tradinggoose/app/api/workspaces/invitations/[invitationId]/route.ts` all redirect or navigate to `/dashboard`, and `apps/tradinggoose/widgets/widgets/{editor_workflow/components/workflow-editor/workflow-canvas.tsx,list_workflow/components/folder-tree/folder-tree.tsx,list_workflow/components/folder-tree/components/workflow-item.tsx,list_workflow/index.tsx}` now track the active workflow through route/provider state instead of string-matching `/w/...`.
- Build/runtime polish in this branch stays in place for non-billing surfaces too. `apps/tradinggoose/next.config.ts` plus `apps/tradinggoose/app/monaco-editor/esm/vs/[...assetPath]/route.ts` now trace and serve Monaco assets from both regular `node_modules` and Bun `.bun/monaco-editor@*` installs, `apps/tradinggoose/app/layout.tsx` switches the public-env bootstrap to `next/script` and suppresses browser-extension-only hydration noise, `apps/tradinggoose/app/(auth)/login/login-form.tsx` and `apps/tradinggoose/app/(landing)/components/integrations/integrations.tsx` add targeted hydration/JSON-LD escaping fixes, `apps/tradinggoose/app/(landing)/components/feature/components/market-preview/indicators/catalog.ts` adds trigger examples to the landing indicator catalog, and the store/hook adapters under `apps/tradinggoose/stores/*`, `apps/tradinggoose/hooks/use-workspace-permissions.ts`, and `apps/tradinggoose/stores/workflows/workflow/store-client.tsx` now prefer `zustand/traditional` equality helpers.

### Design Decisions
- Personal PAYG remains a transition out of the default personal subscription row, not a separate pre-provisioned paid row. The branch intentionally uses advisory locks plus the `paygActivationAttemptId` metadata handshake in `apps/tradinggoose/app/api/billing/payg/activate/route.ts` so retries reconcile with Stripe instead of creating duplicate subscriptions.
- Personal Stripe customer ownership is centralized in `apps/tradinggoose/lib/billing/stripe-customers.ts`. Both the billing portal route and PAYG activation reuse that helper so customer creation, deleted-customer recovery, and idempotency-key selection stay consistent.
- Billing management for self-serve users now prefers Stripe Billing Portal over custom app-local cancel/restore flows. The replacement path is the shared `openBillingPortal()` helper plus the state machine in `apps/tradinggoose/global-navbar/settings-modal/components/subscription/payg-ui.ts`, not a bespoke dialog component.
- Onboarding allowance is treated as spend-down credit on the default personal path. `apps/tradinggoose/lib/billing/webhooks/subscription.ts` and `apps/tradinggoose/lib/billing/core/usage.ts` intentionally decrement `grantedOnboardingAllowanceUsd` when a personal user leaves that path, and only sync `customUsageLimit` automatically when it still equals the seeded default.
- Copilot turn status is now a transport-level stream contract. `turn_state` is emitted by the API routes and consumed by the stores so error turns and waiting-for-tools pauses do not depend on reconstructing state from tool-call timing alone.
- `/workspace/[workspaceId]/dashboard` is the canonical workspace URL now. The branch intentionally removes pathname-derived active-workflow logic and legacy `/w` redirect assumptions so dashboard widgets, invitation acceptance, and auth redirects all converge on one route.
- Monaco asset lookup is intentionally multi-root. `apps/tradinggoose/app/monaco-editor/esm/vs/[...assetPath]/route.ts` searches traced package roots and Bun install roots because this repo uses Bun and standalone tracing.
- The Zustand changes are deliberate consistency work, not a one-off optimization. Shared stores and selector consumers now use equality-aware `zustand/traditional` entrypoints so rerenders and hydration churn do not regress back to the plain hook behavior.

### Shared Contracts and Helpers to Reuse
- Reuse `apps/tradinggoose/lib/billing/stripe-customers.ts` for personal Stripe customer lifecycle work. `ensureStripeUserCustomer()`, `getStripeUserCustomerCreateIdempotencyKey()`, `getStripeUserCustomerReplacementIdempotencyKey()`, `getStripeCustomerDefaultPaymentMethodId()`, `isDeletedStripeCustomer()`, and `isMissingStripeCustomerError()` are the canonical helpers.
- Reuse `apps/tradinggoose/app/api/billing/payg/activate/route.ts` as the only personal PAYG activation path. Future personal activation logic should extend that route and its `paygActivationAttemptId` metadata flow instead of adding parallel checkout or activation endpoints.
- Reuse `apps/tradinggoose/lib/billing/billing-portal.ts` on the client and `apps/tradinggoose/app/api/billing/portal/route.ts` on the server for Stripe Billing Portal access. All user-menu, sidebar, team-usage, and subscription-surface flows already point there.
- Reuse `apps/tradinggoose/global-navbar/settings-modal/components/subscription/payg-ui.ts` for personal billing badge/action decisions, and keep `apps/tradinggoose/lib/billing/subscriptions/utils.ts` as the gating helper for whether personal usage limits are editable.
- Reuse `apps/tradinggoose/lib/billing/core/usage.ts` for onboarding-allowance settlement and limit resets. `decrementGrantedOnboardingAllowanceByCurrentPeriodUsage()` and `resetUserCustomUsageLimitToGrantedOnboardingAllowance()` are the branch’s canonical helpers for that bookkeeping.
- Reuse the `turn_state` SSE contract across `apps/tradinggoose/app/api/copilot/chat/route.ts`, `apps/tradinggoose/app/api/copilot/tools/mark-complete/route.ts`, and `apps/tradinggoose/stores/copilot/{streaming.ts,store.ts}` whenever future Copilot streaming work needs paused/completed/error lifecycle state.
- Reuse `/workspace/[workspaceId]/dashboard` plus the route/provider-based workflow selection flow in `apps/tradinggoose/widgets/widgets/list_workflow/*` and `apps/tradinggoose/widgets/widgets/editor_workflow/components/workflow-editor/workflow-canvas.tsx`. The canonical active-workflow signal is now `workflowId`, not a pathname suffix.
- Reuse the Monaco tracing pair `apps/tradinggoose/next.config.ts` plus `apps/tradinggoose/app/monaco-editor/esm/vs/[...assetPath]/route.ts` for any future Monaco asset changes, and reuse `zustand/traditional` `createWithEqualityFn` / `useStoreWithEqualityFn` when touching the shared stores already migrated in this branch.

### Removed or Replaced Items
- `apps/tradinggoose/global-navbar/settings-modal/components/subscription/components/cancel-subscription/cancel-subscription.tsx` and its `index.ts` re-export were removed. The replacement path is the shared `openBillingPortal()` helper plus the inline manage row in `apps/tradinggoose/global-navbar/settings-modal/components/subscription/subscription.tsx`.
- `apps/tradinggoose/global-navbar/settings-modal/components/subscription/components/index.ts` no longer exports `CancelSubscription`. Future billing settings UI should import `PlanCard`, `UsageLimit`, and `WorkspaceBillingOwnerEditor` only, then call the shared portal helper when needed.
- The legacy `/w` workspace routing contract was replaced by `/workspace/[workspaceId]/dashboard`. The branch updates redirects, invitation acceptance, template launch flows, workflow list navigation, and the proxy matcher accordingly; do not restore `/w` assumptions.
- `resolveStreamPausedTurnStatus()` was removed from `apps/tradinggoose/stores/copilot/store-state.ts`. The replacement is the explicit `turn_state` SSE envelope plus `resolveFinalStreamTurnState()` in `apps/tradinggoose/stores/copilot/store.ts`.
- Route-local `fetch('/api/billing/portal')` snippets in the user menu, sidebar usage indicator, team usage card, and subscription settings were replaced by `apps/tradinggoose/lib/billing/billing-portal.ts`.
- The synthetic pathname plumbing for workflow list highlighting was removed from `apps/tradinggoose/widgets/widgets/list_workflow/index.tsx` and `apps/tradinggoose/widgets/widgets/list_workflow/components/folder-tree/folder-tree.tsx`. The replacement contract is the explicit active workflow id supplied through route/provider state.

### Future Branch Guardrails
- Do not recreate personal Stripe customer lookup or creation logic in route handlers. Reuse `apps/tradinggoose/lib/billing/stripe-customers.ts` from billing summary, signup, portal, and PAYG flows.
- Do not allow personal usage-limit editing or “Increase Limit” UI until a real personal `stripeSubscriptionId` exists. Keep using `canEditUsageLimit()` and `getPersonalPaygUiState()` rather than branching on ad hoc tier checks.
- Do not restore `CancelSubscription`, direct portal-fetch snippets, or any other custom local cancel UI. Stripe Billing Portal is the canonical manage/cancel/restore surface now.
- Do not reintroduce `/w` URLs, `/w` proxy matchers, pathname overrides, or active-route checks based on URL string matching. Use `/workspace/[workspaceId]/dashboard` and explicit workflow ids.
- Do not infer Copilot paused/completed/error lifecycle from `awaiting_tools` alone. Future stream work should emit and consume the `turn_state` SSE events already wired through the chat and mark-complete routes.
- Do not assume Monaco assets only live under one `node_modules/monaco-editor` path. Keep Bun `.bun/monaco-editor@*` tracing and lookup rules aligned if the install layout changes.
- Do not switch the already-migrated shared stores back to plain Zustand `create` / `useStore`. Follow the `zustand/traditional` pattern used in `apps/tradinggoose/stores/*`, `apps/tradinggoose/hooks/use-workspace-permissions.ts`, and `apps/tradinggoose/stores/workflows/workflow/store-client.tsx`.
- Do not render raw JSON into `<script>` tags without escaping `<`. The branch intentionally uses `next/script` in `apps/tradinggoose/app/layout.tsx` and `JSON.stringify(...).replace(/</g, '\\u003c')` for JSON-LD in `apps/tradinggoose/app/(landing)/components/integrations/integrations.tsx`.
- Do not bypass the onboarding-allowance helpers when future billing code resets or carries forward personal usage. Extend `apps/tradinggoose/lib/billing/core/usage.ts` and `apps/tradinggoose/lib/billing/webhooks/subscription.ts` instead of adding separate bookkeeping.

### Validation Notes
- Reviewed `git status --short`, `git fetch origin staging`, `git merge-base origin/staging feat/billing-PAYG`, `git log --oneline 8dd89eeb49988bc21ddd55ecf2f11fdf46c2bb96..feat/billing-PAYG`, `git diff --stat 8dd89eeb49988bc21ddd55ecf2f11fdf46c2bb96..feat/billing-PAYG`, and `git diff --name-status --find-renames 8dd89eeb49988bc21ddd55ecf2f11fdf46c2bb96..feat/billing-PAYG`.
- Inspected the current owning files for the changed contracts, including `apps/tradinggoose/app/api/billing/{payg/activate/route.ts,portal/route.ts,route.ts}`, `apps/tradinggoose/lib/billing/{billing-portal.ts,stripe-customers.ts,core/subscription.ts,core/usage.ts,subscriptions/utils.ts,webhooks/subscription.ts,calculations/usage-monitor.ts}`, `apps/tradinggoose/global-navbar/settings-modal/components/subscription/{subscription.tsx,payg-ui.ts,subscription-permissions.ts,components/index.ts}`, `apps/tradinggoose/app/api/admin/billing/tiers/{route.ts,[id]/route.ts}`, `apps/tradinggoose/app/admin/billing/{billing-admin.tsx,tier-detail.tsx,tier-editor.tsx}`, `apps/tradinggoose/app/api/copilot/{chat/route.ts,tools/mark-complete/route.ts}`, `apps/tradinggoose/stores/copilot/{streaming.ts,store.ts,store-state.ts}`, `apps/tradinggoose/app/workspace/[workspaceId]/*`, `apps/tradinggoose/proxy.ts`, `apps/tradinggoose/app/monaco-editor/esm/vs/[...assetPath]/route.ts`, `apps/tradinggoose/next.config.ts`, `apps/tradinggoose/app/layout.tsx`, and the migrated Zustand store/hook adapters.
- Inspected merge-base versions of the removed/replaced files and exports, including `apps/tradinggoose/global-navbar/settings-modal/components/subscription/components/cancel-subscription/cancel-subscription.tsx` and its `index.ts`, to confirm the replacement guidance.
- Reviewed related test coverage in `apps/tradinggoose/app/api/billing/{payg/activate/route.test.ts,portal/route.test.ts,route.test.ts}`, `apps/tradinggoose/app/api/usage/route.test.ts`, `apps/tradinggoose/lib/billing/{stripe-customers.test.ts,core/subscription.test.ts,core/usage.test.ts,calculations/usage-monitor.test.ts,webhooks/subscription.test.ts,subscriptions/utils.test.ts}`, `apps/tradinggoose/app/api/admin/billing/tiers/[id]/route.test.ts`, `apps/tradinggoose/lib/admin/billing/tier-mutations.test.ts`, `apps/tradinggoose/app/api/copilot/chat/review-session-post.test.ts`, `apps/tradinggoose/app/api/copilot/tools/mark-complete/route.test.ts`, and `apps/tradinggoose/stores/copilot/store.test.ts`.
- No tests were executed during this documentation pass; validation was source-review only.
