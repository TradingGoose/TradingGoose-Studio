# April-23-2026

## docker_files @ f2bc8336 vs origin/staging

### Summary
- Standardizes the Docker and compose flow around pinned Bun 1.3.11 images, a dedicated guardrails build stage, and a bundled realtime socket-server artifact so the app and realtime containers stay smaller and more predictable.
- Tightens deployment configuration by making Redis, PostgreSQL credentials, auth secrets, encryption keys, and image tags explicit in local, Ollama, and production compose files instead of relying on implicit defaults or generated values.
- Simplifies image publishing to one multi-platform workflow that pushes GHCR everywhere and Docker Hub on `main`, then updates the docs and env templates to match the new contract.

### Branch Scope
- Compared `1309ad1279fb0af551a7aad47764c43a5fd1b3d8..f2bc8336` against `origin/staging`.
- The working tree was clean during this review, so only committed branch changes are included here.
- Main areas touched: `.dockerignore`, `.github/CONTRIBUTING.md`, `.github/workflows/images.yml`, `README.md`, `apps/tradinggoose/.env.example`, `apps/tradinggoose/.env.example.docker`, `apps/tradinggoose/lib/guardrails/requirements.txt`, `docker-compose.local.yml`, `docker-compose.ollama.yml`, `docker-compose.prod.yml`, `docker/app.Dockerfile`, `docker/db.Dockerfile`, and `docker/realtime.Dockerfile`.

### Key Changes
- `docker/app.Dockerfile` now builds on `oven/bun:1.3.11-alpine`, pins `turbo@2.5.8` and `sharp@0.34.3`, splits guardrails installation into its own `guardrails` stage, and copies the runtime guardrails virtualenv into the final image instead of rebuilding it in place.
- `docker/realtime.Dockerfile` now bundles `apps/tradinggoose/socket-server/index.ts` into `/tmp/realtime-build/socket-server.js` and ships only that artifact in the runtime image, rather than copying the full app tree, package tree, and root `package.json` into the container.
- `docker/db.Dockerfile` narrows the migration image to the files `packages/db` actually needs at runtime: `package.json`, `drizzle.config.ts`, `schema.ts`, `consts.ts`, `schema/`, and `migrations/`.
- `.github/workflows/images.yml` collapses the previous AMD64/ARM64 split plus ECR workflow into one matrix job that builds `docker/app.Dockerfile`, `docker/db.Dockerfile`, and `docker/realtime.Dockerfile` for `linux/amd64,linux/arm64`, pushes SHA tags everywhere, and publishes `latest` only on `main` for GHCR and Docker Hub.
- `docker-compose.local.yml`, `docker-compose.ollama.yml`, and `docker-compose.prod.yml` now require explicit `POSTGRES_USER`, `POSTGRES_PASSWORD`, `NEXT_PUBLIC_APP_URL`, `BETTER_AUTH_SECRET`, `ENCRYPTION_KEY`, `INTERNAL_API_SECRET`, and image tags where applicable, add a Redis service dependency, and wire `REDIS_URL=redis://redis:6379` into the app and realtime containers.
- `docker-compose.ollama.yml` also switches Ollama to `OLLAMA_IMAGE_TAG` instead of `latest`, keeps the GPU and CPU services separate, and makes the setup helper reference the actual service name when explaining how to pull extra models.
- `.dockerignore` now excludes repo metadata, build output, cache directories, environment files, logs, and local data volumes more aggressively so Docker builds stay focused on source inputs.
- `apps/tradinggoose/.env.example` and `apps/tradinggoose/.env.example.docker` now document the explicit local boot variables, including the Redis URL, socket URL, and compose-specific database/bootstrap defaults.
- `.github/CONTRIBUTING.md` and `README.md` now explain the new compose bootstrap flow and the need to copy the appropriate env template before running Docker-based setups.

### Design Decisions
- The branch makes the runtime contract explicit instead of hiding it behind generated secrets or image defaults. That keeps local Docker, production compose, and CI image publishing aligned on the same required inputs.
- Redis is treated as a first-class dependency for containerized runs rather than an optional external service, which is why every compose stack now depends on the local Redis container and exports the same `REDIS_URL`.
- The realtime container now owns a bundled socket-server artifact only. That reduces runtime image surface area and keeps the build/runtime split clear: bundle in the Dockerfile, execute the compiled artifact in the final image.
- Guardrails setup is isolated in a dedicated build stage so Python package installation does not leak into the app runtime image beyond the prepared virtualenv.
- The image workflow now publishes multi-platform images from one job instead of maintaining separate build, arm64, and manifest-push jobs. That keeps the publishing path simpler and avoids the previous AWS/ECR dependency chain.

### Shared Contracts and Helpers to Reuse
- Reuse `apps/tradinggoose/.env.example` for app-only local development and `apps/tradinggoose/.env.example.docker` for Docker Compose setups. Those files now define the canonical required env surface for each path.
- Reuse the explicit `:?` interpolation pattern in `docker-compose.local.yml`, `docker-compose.ollama.yml`, and `docker-compose.prod.yml` when adding new compose services that should fail fast on missing secrets or tags.
- Reuse `docker/app.Dockerfile`, `docker/realtime.Dockerfile`, and `docker/db.Dockerfile` as the canonical container build patterns for app, socket server, and migrations work instead of copying full source trees into new images.
- Reuse the single-job publishing model in `.github/workflows/images.yml` for future image variants so tag generation and platform coverage stay centralized.

### Removed or Replaced Items
- The old implicit env defaults for PostgreSQL credentials, auth secrets, encryption keys, and image tags were replaced. Future branches should not reintroduce silent fallbacks such as `postgres`, `latest`, or generated compose-time secrets for these paths.
- The previous separate AMD64/ARM64 build jobs, manifest assembly job, and AWS/ECR login path in `.github/workflows/images.yml` were replaced by the single GHCR/Docker Hub workflow.
- The realtime image no longer copies the full app and package trees into the runtime layer. The replacement is the bundled `socket-server.js` artifact produced during the Docker build.
- The db image no longer copies the whole `packages/db` directory. The replacement is the explicit migration input set copied in `docker/db.Dockerfile`.

### Future Branch Guardrails
- Do not add new compose services that depend on hidden defaults when the rest of the stack now expects explicit `:?`-guarded env vars.
- Do not reintroduce `latest`-only image references for production or compose-based local boot; use tagged images and make the tag requirement visible in the env file.
- Do not expand the realtime runtime image back into a full app image unless the socket-server bundling strategy changes with it.
- Do not move guardrails installation back into the main app runtime layer; keep the isolated build stage and copy only its prepared virtualenv forward.
- Do not split the image publishing path back into separate platform-specific jobs unless the registry or platform requirements genuinely change.

### Validation Notes
- Reviewed `git status --short --branch`, `git log --oneline origin/staging..HEAD`, `git diff --stat origin/staging...HEAD`, and `git diff --name-status --find-renames origin/staging...HEAD`.
- Verified patch hygiene with `git diff --check origin/staging...HEAD`.
- Validated all three compose files with `docker compose -f docker-compose.local.yml config`, `docker compose -f docker-compose.ollama.yml config`, and `docker compose -f docker-compose.prod.yml config` using explicit placeholder env values.
- No application tests were added or updated in this branch; the change set is Docker, compose, and documentation focused.

## fix/copilot-billing @ 6a60cc11 vs origin/staging

### Summary
- Expands the AI provider surface under `apps/tradinggoose/providers/ai/*` and `apps/tradinggoose/app/api/providers/ai/*` with Azure Anthropic, Vertex AI, AWS Bedrock, Fireworks, and vLLM support, while consolidating provider metadata and capability lookup behind `apps/tradinggoose/providers/ai/models.ts`.
- Normalizes hosted-model execution, billing, and discovery contracts across `apps/tradinggoose/providers/ai/{index.ts,utils.ts,types.ts}`, `apps/tradinggoose/app/api/providers/ai/handler.ts`, and the client model bootstrap/query paths in `apps/tradinggoose/stores/providers/*` and `apps/tradinggoose/hooks/queries/providers.ts`.
- Refreshes billing allowance propagation in `apps/tradinggoose/lib/billing/core/subscription.ts` and the admin query hooks so onboarding-allowance backfills update seeded free-user state without clobbering custom limits and the subscription cache refreshes after admin billing/system-setting saves.
- Aligns the watchlist and Copilot header selectors in `apps/tradinggoose/widgets/widgets/{watchlist,copilot}/*` with the shared widget-header sizing contract so header controls no longer overflow or collapse inconsistently.

### Branch Scope
- Compared `6b55b4609004c0e4c67c5fd2f00bb1b7bebe381b..fix/copilot-billing`.
- The range contains five branch-local commits from `6c61d909` through `6a60cc11`, covering billing allowance refresh, AI provider expansion, watchlist header sizing cleanup, and the follow-up Fireworks/vLLM service-config fix.
- The working tree was clean during this documentation pass, so no uncommitted local changes were included.
- Main areas touched: `apps/tradinggoose/providers/ai/*`, `apps/tradinggoose/app/api/providers/ai/*`, `apps/tradinggoose/lib/system-services/*`, `apps/tradinggoose/lib/billing/core/subscription.ts`, `apps/tradinggoose/app/api/copilot/usage/route.ts`, `apps/tradinggoose/lib/copilot/config.ts`, `apps/tradinggoose/stores/providers/*`, `apps/tradinggoose/hooks/queries/*`, `apps/tradinggoose/components/icons/*`, `apps/tradinggoose/widgets/widgets/watchlist/components/*`, `apps/tradinggoose/widgets/widgets/copilot/components/copilot/copilot-header.tsx`, and `apps/tradinggoose/package.json`.

### Key Changes
- `apps/tradinggoose/providers/ai/index.ts` now registers `azure-anthropic`, `vertex`, `bedrock`, `fireworks`, and `vllm`, and `apps/tradinggoose/providers/ai/types.ts` extends `ProviderId`, `ProviderRequest`, `TokenInfo`, and `ProviderResponse` so provider calls can carry Vertex project/location, Bedrock credentials/region, structured-output options, richer token accounting, and shared timing/error metadata.
- Provider execution was split into reusable cores plus thin backend adapters. `apps/tradinggoose/providers/ai/anthropic/core.ts` now owns the Anthropic tool loop, adaptive-thinking handling, max-token safety path, and native-structured-output fallback for both the standard Anthropic provider and `apps/tradinggoose/providers/ai/azure-anthropic/index.ts`. `apps/tradinggoose/providers/ai/gemini/core.ts` plus `apps/tradinggoose/providers/ai/google/utils.ts` now own Gemini/Vertex tool batching, usage conversion, stream handling, and thinking-level mapping for both the Google and `apps/tradinggoose/providers/ai/vertex/index.ts` backends. `apps/tradinggoose/providers/ai/bedrock/index.ts`, `apps/tradinggoose/providers/ai/fireworks/index.ts`, and `apps/tradinggoose/providers/ai/vllm/index.ts` add first-class runtime implementations for those providers.
- `apps/tradinggoose/providers/ai/models.ts` became the richer provider/model source of truth. The file now defines static provider metadata for Fireworks, vLLM, Azure Anthropic, Vertex, and Bedrock; attaches release dates, reseller flags, colors, max output token limits, deep-research and memory flags; and exports dynamic-provider helpers such as `DYNAMIC_MODEL_PROVIDERS`, `updateVLLMModels()`, and `updateFireworksModels()`. `apps/tradinggoose/providers/ai/utils.ts` now derives the browser-safe provider catalog from `PROVIDER_DEFINITIONS`, exposes `updateVLLMProviderModels()` and `updateFireworksProviderModels()`, routes hosted-model checks through `getHostedModels()`, and uses shared OpenAI-compatible stream/tool helpers for Fireworks and vLLM via `apps/tradinggoose/providers/ai/{fireworks,vllm}/utils.ts`.
- Client-side provider discovery was widened to match the new catalog. `apps/tradinggoose/stores/providers/types.ts`, `apps/tradinggoose/stores/providers/store.ts`, and `apps/tradinggoose/hooks/queries/providers.ts` now treat `fireworks` and `vllm` as dynamic model sources, fetch `/api/providers/ai/{fireworks,vllm}/models`, and push the returned model lists back through the catalog update helpers. `apps/tradinggoose/app/api/providers/ai/fireworks/models/route.ts` and `apps/tradinggoose/app/api/providers/ai/vllm/models/route.ts` add the discovery endpoints, while `apps/tradinggoose/components/icons/{index.ts,provider-icons.tsx}` wires the new provider icon exports into the same icon surface the model catalog uses.
- Provider request normalization and billing were tightened in the API layer. `apps/tradinggoose/app/api/providers/ai/handler.ts` now forwards Vertex and Bedrock request parameters plus `thinkingLevel` to provider implementations. `apps/tradinggoose/app/api/copilot/usage/route.ts` drops the old Claude model synonym table and now bills against the canonical dotted base model id returned by the request, while `apps/tradinggoose/lib/copilot/config.ts` replaces the hard-coded default Anthropic model string with `getProviderDefaultModel('anthropic')` so the Copilot default stays aligned with the catalog.
- Billing backfill behavior now refreshes subscription allowance state without wiping custom overrides. `apps/tradinggoose/lib/billing/core/subscription.ts` changed `backfillDefaultUserSubscriptions()` from a pure insert/no-op conflict path to an update path that always refreshes `grantedOnboardingAllowanceUsd` but only updates `customUsageLimit` when the current limit still matches the previous granted allowance. `apps/tradinggoose/hooks/queries/admin-billing.ts` and `apps/tradinggoose/hooks/queries/admin-system-settings.ts` now invalidate `subscriptionKeys.all` after billing and system-setting writes so the UI picks up that refreshed allowance state.
- Fireworks and vLLM runtime ownership now follows the system-service layer for outbound config. `apps/tradinggoose/lib/system-services/catalog.ts` and `apps/tradinggoose/lib/system-services/runtime.ts` add the `fireworks` and `vllm` service definitions plus `resolveFireworksServiceConfig()` / `resolveVllmServiceConfig()`. The Fireworks model route now reads its discovery API key from the system-service layer, and both the vLLM model route and `apps/tradinggoose/providers/ai/vllm/index.ts` now use the DB-backed vLLM base URL/API key defaults instead of raw env reads for model discovery and provider initialization.
- The watchlist selector/header layout was reworked to use the shared widget-header sizing contract instead of bespoke fixed widths. `apps/tradinggoose/widgets/widgets/watchlist/components/watchlist-header-controls.tsx` switches each header group from `shrink-0` wrappers to `min-w-0` groups plus bounded inner containers. `apps/tradinggoose/widgets/widgets/watchlist/components/watchlist-list-selector.tsx` replaces the old `min-w-[220px]`/`w-[240px]` trigger-and-menu layout with a full-width shared header control, a `w-[260px]` dropdown, inline checkmark placement, and grouped rename/delete actions. `apps/tradinggoose/widgets/widgets/copilot/components/copilot/copilot-header.tsx` also pins its chat-history trigger to `w-[240px] shrink-0` so it keeps a stable width beside the updated watchlist controls.
- The branch also refreshes a few model-facing UI affordances to match the new catalog. `apps/tradinggoose/blocks/blocks/{browser_use.ts,stt.ts}` rename the Gemini option to `gemini-3.1-pro-preview`, and the provider bootstrap plus icon plumbing keeps those surfaces aligned with the expanded provider definitions.

### Design Decisions
- Provider execution now shares backend-neutral cores instead of duplicating tool-loop logic per reseller backend. `executeAnthropicProviderRequest()` in `apps/tradinggoose/providers/ai/anthropic/core.ts` is the canonical Anthropic-compatible executor, and `executeGeminiRequest()` in `apps/tradinggoose/providers/ai/gemini/core.ts` is the canonical Gemini-compatible executor. Backend-specific files such as `apps/tradinggoose/providers/ai/azure-anthropic/index.ts` and `apps/tradinggoose/providers/ai/vertex/index.ts` only translate credentials, endpoint shape, and SDK client creation.
- `apps/tradinggoose/providers/ai/models.ts` is now the single source of truth for provider ids, icon ownership, pricing, temperature/tool/structured-output capability checks, dynamic-provider prefix matching, and default model lookup. Downstream surfaces such as `apps/tradinggoose/providers/ai/utils.ts`, `apps/tradinggoose/stores/providers/store.ts`, `apps/tradinggoose/lib/copilot/config.ts`, and model-discovery routes derive from that catalog instead of maintaining parallel provider lists.
- Hosted-model billing remains an explicit catalog decision instead of a provider-name heuristic. `apps/tradinggoose/providers/ai/utils.ts` now uses `getHostedModels()` from the catalog to decide whether usage should be billed as platform-hosted, which keeps reseller and self-hosted providers like Bedrock, Vertex, Fireworks, OpenRouter, vLLM, and Ollama out of the hosted-model path unless the catalog is intentionally widened.
- Billing allowance backfill treats seeded allowance state and custom admin overrides as different contracts. `backfillDefaultUserSubscriptions()` intentionally refreshes `grantedOnboardingAllowanceUsd` for all seeded free-tier users while preserving `customUsageLimit` unless it still matches the old seeded value, so admin-raised free-tier limits are not silently rolled back by later billing-settings saves.
- vLLM and Fireworks use different config ownership on purpose. Fireworks execution still requires the request-level API key path from `apps/tradinggoose/providers/ai/utils-server.ts`, but Fireworks model discovery and vLLM discovery/runtime defaults are now owned by the system-service layer through `apps/tradinggoose/lib/system-services/{catalog.ts,runtime.ts}`.

### Shared Contracts and Helpers to Reuse
- Reuse `PROVIDER_DEFINITIONS`, `getProviderDefaultModel()`, `getProviderFromModel()`, `getHostedModels()`, `updateVLLMModels()`, and `updateFireworksModels()` in `apps/tradinggoose/providers/ai/models.ts` for any future provider or model-catalog work. Do not create parallel provider metadata tables elsewhere.
- Reuse the browser-safe provider catalog and dynamic-provider update helpers in `apps/tradinggoose/providers/ai/utils.ts`, plus the bootstrap wiring in `apps/tradinggoose/stores/providers/store.ts`, when adding new model-discovery-backed providers. That is the canonical flow from API-discovered models into client-visible provider lists.
- Reuse `executeAnthropicProviderRequest()` in `apps/tradinggoose/providers/ai/anthropic/core.ts` for Anthropic-compatible backends and `executeGeminiRequest()` in `apps/tradinggoose/providers/ai/gemini/core.ts` with helpers from `apps/tradinggoose/providers/ai/google/utils.ts` for Gemini-compatible backends. Those files now own the shared tool iteration, streaming, structured-output, and thinking-mode behavior.
- Reuse `resolveFireworksServiceConfig()` and `resolveVllmServiceConfig()` in `apps/tradinggoose/lib/system-services/runtime.ts` together with their catalog entries in `apps/tradinggoose/lib/system-services/catalog.ts` for DB-backed outbound provider configuration. Future runtime-owned provider defaults should follow that pattern instead of reading env vars directly from routes or provider runtimes.
- Reuse the shared header-control sizing utilities in `apps/tradinggoose/widgets/widgets/components/widget-header-control` and the `min-w-0` bounded-wrapper pattern now applied in `apps/tradinggoose/widgets/widgets/watchlist/components/watchlist-header-controls.tsx` and `apps/tradinggoose/widgets/widgets/watchlist/components/watchlist-list-selector.tsx` when adding new header selectors.

### Removed or Replaced Items
- The old env-owned Fireworks/vLLM runtime reads in the discovery/runtime paths were replaced. Future branches should not restore direct `getEnv('FIREWORKS_API_KEY')`, `getEnv('VLLM_BASE_URL')`, or `getEnv('VLLM_API_KEY')` reads in `apps/tradinggoose/app/api/providers/ai/{fireworks,vllm}/models/route.ts` or `apps/tradinggoose/providers/ai/vllm/index.ts`; the replacement is `resolveFireworksServiceConfig()` / `resolveVllmServiceConfig()`.
- The model-synonym billing shim in `apps/tradinggoose/app/api/copilot/usage/route.ts` was removed. Future billing work should use the canonical base model id from the request/remote model rather than reviving a separate `MODEL_SYNONYMS` table.
- The hard-coded Copilot default Anthropic model strings in `apps/tradinggoose/lib/copilot/config.ts` were replaced by `getProviderDefaultModel('anthropic')`. Future config defaults should come from the provider catalog rather than duplicating literal model ids.
- The bespoke watchlist selector trigger/menu sizing in `apps/tradinggoose/widgets/widgets/watchlist/components/watchlist-list-selector.tsx` was replaced by the shared widget-header sizing pattern. Future branches should not restore the old `min-w-[220px]` trigger, `w-[240px]` menu, or detached trailing checkmark layout.
- No repository files were deleted in the current branch head; the branch mainly replaces inline provider, billing, and widget behaviors with shared contracts and catalog-driven lookups.

### Future Branch Guardrails
- Do not add new provider ids, icons, or model-capability checks in ad hoc UI files. Extend `apps/tradinggoose/providers/ai/{types.ts,models.ts,utils.ts,index.ts}` together so server execution, client catalog lookup, and billing/hosted-model behavior stay aligned.
- Do not fork the Anthropic or Gemini tool-loop logic for reseller backends. Extend `apps/tradinggoose/providers/ai/anthropic/core.ts` or `apps/tradinggoose/providers/ai/gemini/core.ts` and keep backend adapters thin.
- Do not mark reseller or self-hosted providers as hosted unless the platform truly owns and bills those requests. The current hosted-model contract in `apps/tradinggoose/providers/ai/models.ts` and `apps/tradinggoose/providers/ai/utils.ts` only treats OpenAI, Anthropic, and Google catalog models as hosted.
- Do not revert `backfillDefaultUserSubscriptions()` to `onConflictDoNothing()` or to an unconditional `customUsageLimit` overwrite. The branch relies on the current split contract: `grantedOnboardingAllowanceUsd` refreshes with billing settings, while custom limits survive unless they still equal the seeded default.
- Do not reintroduce direct env reads for Fireworks/vLLM discovery/runtime defaults when a DB-backed system service exists. Keep outbound runtime-owned provider config in `apps/tradinggoose/lib/system-services/*`.
- Do not reintroduce `shrink-0` header groups or unbounded fixed-width triggers for the watchlist selector stack. Keep header selectors inside `min-w-0` groups with bounded inner wrappers so adjacent header controls can coexist without clipping.

### Validation Notes
- Reviewed `git status --short`, `git fetch origin staging`, `git merge-base origin/staging fix/copilot-billing`, `git log --oneline 6b55b4609004c0e4c67c5fd2f00bb1b7bebe381b..fix/copilot-billing`, `git diff --stat 6b55b4609004c0e4c67c5fd2f00bb1b7bebe381b..fix/copilot-billing`, and `git diff --name-status --find-renames 6b55b4609004c0e4c67c5fd2f00bb1b7bebe381b..fix/copilot-billing`.
- Inspected the current owning files for the documented contracts, including `apps/tradinggoose/providers/ai/{index.ts,types.ts,utils.ts,utils-server.ts,models.ts,error.ts,constants.ts,anthropic/core.ts,anthropic/utils.ts,azure-anthropic/index.ts,gemini/core.ts,google/utils.ts,vertex/index.ts,bedrock/index.ts,fireworks/index.ts,fireworks/utils.ts,vllm/index.ts,vllm/utils.ts}`, `apps/tradinggoose/app/api/providers/ai/{handler.ts,fireworks/models/route.ts,vllm/models/route.ts}`, `apps/tradinggoose/lib/system-services/{catalog.ts,runtime.ts}`, `apps/tradinggoose/lib/billing/core/subscription.ts`, `apps/tradinggoose/hooks/queries/{admin-billing.ts,admin-system-settings.ts,providers.ts}`, `apps/tradinggoose/stores/providers/{store.ts,types.ts}`, `apps/tradinggoose/app/api/copilot/usage/route.ts`, `apps/tradinggoose/lib/copilot/config.ts`, `apps/tradinggoose/components/icons/{index.ts,provider-icons.tsx}`, `apps/tradinggoose/widgets/widgets/watchlist/components/{watchlist-header-controls.tsx,watchlist-list-selector.tsx}`, `apps/tradinggoose/widgets/widgets/copilot/components/copilot/copilot-header.tsx`, `apps/tradinggoose/blocks/blocks/{browser_use.ts,stt.ts}`, and `apps/tradinggoose/package.json`.
- No tests were executed during this documentation pass; validation for this changelog entry was source-review only.
