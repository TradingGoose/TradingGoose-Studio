{/*
  TEMPLATE: Integration Tool (auto-generated + manual content)
  OUTPUT:   apps/docs/content/docs/en/tools/{{SLUG}}.mdx
  SOURCE:   apps/tradinggoose/blocks/blocks/{{SLUG}}.ts

  Integration tools use JSON-driven rendering instead of screenshots.
  Copy the subBlocks array from the block's .ts file and pass as JSON props.
  The BlockConfigPreview component renders a static form preview.

  IMPORTANT: Sections between MANUAL-CONTENT markers survive regeneration.
*/}
---
title: {{TOOL_NAME}}
description: {{SHORT_DESCRIPTION}}
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"

<BlockInfoCard
  type="{{BLOCK_TYPE}}"
  color="{{BG_COLOR}}"
  icon={{{HAS_ICON}}}
  iconSvg={`{{ICON_SVG}}`}
/>

{/* MANUAL-CONTENT-START:intro */}
[{{TOOL_NAME}}]({{SERVICE_URL}}) is {{SERVICE_DESCRIPTION}}.

With {{TOOL_NAME}}, you can:

- **{{CAPABILITY_1}}**: {{CAPABILITY_1_DESC}}
- **{{CAPABILITY_2}}**: {{CAPABILITY_2_DESC}}
- **{{CAPABILITY_3}}**: {{CAPABILITY_3_DESC}}
{/* MANUAL-CONTENT-END */}

## Configuration

{/*
  Copy subBlocks from the block's .ts file.
  Strip: functions (value, condition as fn, fetchOptions, icon components)
  Keep: id, title, type, layout, placeholder, description, defaultValue, options, required
*/}

<ShowcaseCard caption="{{TOOL_NAME}} configuration">
<BlockConfigPreview
  name="{{TOOL_NAME}}"
  type="{{BLOCK_TYPE}}"
  color="{{BG_COLOR}}"
  subBlocks={[
    {
      "id": "{{FIELD_ID}}",
      "title": "{{FIELD_TITLE}}",
      "type": "{{FIELD_TYPE}}",
      "layout": "full",
      "placeholder": "{{PLACEHOLDER}}"
    }
  ]}
  outputs={[
    { "key": "{{OUTPUT_KEY}}", "type": "{{OUTPUT_TYPE}}", "description": "{{OUTPUT_DESC}}" }
  ]}
  tools={["{{TOOL_ID}}"]}
/>
</ShowcaseCard>

{/*
  ──────────────────────────────────────────────────────────
  Everything below this line is AUTO-GENERATED by generate-docs.ts.
  ──────────────────────────────────────────────────────────
*/}

## Tools

### `{{TOOL_ID}}`

{{TOOL_DESCRIPTION}}

#### Input

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `{{PARAM_NAME}}` | {{PARAM_TYPE}} | {{REQUIRED}} | {{PARAM_DESC}} |

#### Output

| Parameter | Type | Description |
|-----------|------|-------------|
| `{{OUTPUT_KEY}}` | {{OUTPUT_TYPE}} | {{OUTPUT_DESC}} |

## Notes

- Category: `{{CATEGORY}}`
- Type: `{{BLOCK_TYPE}}`
