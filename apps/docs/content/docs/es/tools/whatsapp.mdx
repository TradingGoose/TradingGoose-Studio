---
title: WhatsApp
description: Enviar mensajes de WhatsApp
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"

<BlockInfoCard
  type="whatsapp"
  color=""
  icon={true}
  iconSvg={``}
/>

## Configuración

<ShowcaseCard>
  <BlockConfigPreview
    name="WhatsApp"
    type="whatsapp"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "phoneNumber",
            "type": "short-input",
            "title": "Número de teléfono del destinatario",
            "layout": "full",
            "placeholder": "Ingrese el número de teléfono con código de país (ej., +1234567890)",
            "required": true
        },
        {
            "id": "message",
            "type": "long-input",
            "title": "Mensaje",
            "layout": "full",
            "placeholder": "Ingrese su mensaje",
            "required": true
        },
        {
            "id": "phoneNumberId",
            "type": "short-input",
            "title": "ID de número de teléfono de WhatsApp",
            "layout": "full",
            "placeholder": "ID de número de teléfono de WhatsApp Business",
            "required": true
        },
        {
            "id": "accessToken",
            "type": "short-input",
            "title": "Token de acceso",
            "layout": "full",
            "placeholder": "Token de acceso de la API de WhatsApp Business",
            "password": true,
            "required": true
        }
    ]}
  />
</ShowcaseCard>

## Herramientas

### `whatsapp_send_message`

Enviar mensajes de WhatsApp

#### Entrada

| Parámetro | Tipo | Requerido | Descripción |
| --------- | ---- | -------- | ----------- |
| `phoneNumber` | string | Sí | Número de teléfono del destinatario con código de país |
| `message` | string | Sí | Contenido del mensaje a enviar |
| `phoneNumberId` | string | Sí | ID del número de teléfono de WhatsApp Business |

#### Salida

| Parámetro | Tipo | Descripción |
| --------- | ---- | ----------- |
| `success` | boolean | Estado de éxito del envío del mensaje de WhatsApp |
| `messageId` | string | Identificador único del mensaje de WhatsApp |
| `phoneNumber` | string | Número de teléfono del destinatario |
| `status` | string | Estado de entrega del mensaje |

## Instrucciones de uso

Integra WhatsApp en el flujo de trabajo. Permite enviar mensajes.

## Notas

- Categoría: `tools`
- Tipo: `whatsapp`
