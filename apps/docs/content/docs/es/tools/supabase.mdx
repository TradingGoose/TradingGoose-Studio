---
title: Supabase
description: Usar la base de datos de Supabase
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"

<BlockInfoCard
  type="supabase"
  color=""
  icon={true}
  iconSvg={``}
/>

## Configuración

### Obtener Múltiples Filas

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operación",
            "layout": "full",
            "options": [
                {
                    "label": "Obtener Muchas Filas",
                    "id": "query"
                },
                {
                    "label": "Obtener una Fila",
                    "id": "get_row"
                },
                {
                    "label": "Crear una Fila",
                    "id": "insert"
                },
                {
                    "label": "Actualizar una Fila",
                    "id": "update"
                },
                {
                    "label": "Eliminar una Fila",
                    "id": "delete"
                },
                {
                    "label": "Upsert de una Fila",
                    "id": "upsert"
                },
                {
                    "label": "Búsqueda Vectorial",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "query"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "ID del Proyecto",
            "layout": "full",
            "placeholder": "ID de tu proyecto de Supabase (p. ej., jdrkgepadsdopsntdlom)",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "Tabla",
            "layout": "full",
            "placeholder": "Nombre de la tabla",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "Secreto del Rol de Servicio",
            "layout": "full",
            "placeholder": "Tu clave secreta del rol de servicio de Supabase",
            "password": true,
            "required": true
        },
        {
            "id": "filter",
            "type": "short-input",
            "title": "Filtro (sintaxis PostgREST)",
            "layout": "full",
            "placeholder": "status=eq.active"
        },
        {
            "id": "orderBy",
            "type": "short-input",
            "title": "Ordenar Por",
            "layout": "full",
            "placeholder": "nombre_columna (añadir DESC para orden descendente)"
        },
        {
            "id": "limit",
            "type": "short-input",
            "title": "Límite",
            "layout": "full",
            "placeholder": "100"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Obtener Múltiples Filas (<code>supabase_query</code>)</div>

Consultar datos de una tabla de Supabase

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Entrada</div>

| Parámetro | Tipo | Requerido | Descripción |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | Sí | El ID de tu proyecto de Supabase \(p. ej., jdrkgepadsdopsntdlom\) |
| `table` | string | Sí | El nombre de la tabla de Supabase a consultar |
| `filter` | string | No | Filtro PostgREST \(p. ej., "id=eq.123"\) |
| `orderBy` | string | No | Columna por la que ordenar \(añade DESC para orden descendente\) |
| `limit` | number | No | Número máximo de filas a devolver |
| `apiKey` | string | Sí | Tu clave secreta de rol de servicio de Supabase |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Salida</div>

| Parámetro | Tipo | Descripción |
| --------- | ---- | ----------- |
| `message` | string | Mensaje de estado de la operación |

</div>

---

### Obtener una Fila

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operación",
            "layout": "full",
            "options": [
                {
                    "label": "Obtener Muchas Filas",
                    "id": "query"
                },
                {
                    "label": "Obtener una Fila",
                    "id": "get_row"
                },
                {
                    "label": "Crear una Fila",
                    "id": "insert"
                },
                {
                    "label": "Actualizar una Fila",
                    "id": "update"
                },
                {
                    "label": "Eliminar una Fila",
                    "id": "delete"
                },
                {
                    "label": "Upsert de una Fila",
                    "id": "upsert"
                },
                {
                    "label": "Búsqueda Vectorial",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "get_row"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "ID del Proyecto",
            "layout": "full",
            "placeholder": "ID de tu proyecto de Supabase (p. ej., jdrkgepadsdopsntdlom)",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "Tabla",
            "layout": "full",
            "placeholder": "Nombre de la tabla",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "Secreto del Rol de Servicio",
            "layout": "full",
            "placeholder": "Tu clave secreta del rol de servicio de Supabase",
            "password": true,
            "required": true
        },
        {
            "id": "filter",
            "type": "short-input",
            "title": "Filtro (sintaxis PostgREST)",
            "layout": "full",
            "placeholder": "id=eq.123",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Obtener una Fila (<code>supabase_get_row</code>)</div>

Obtiene una única fila de una tabla de Supabase según criterios de filtrado

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Entrada</div>

| Parámetro | Tipo | Requerido | Descripción |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | Sí | El ID de tu proyecto Supabase \(p. ej., jdrkgepadsdopsntdlom\) |
| `table` | string | Sí | El nombre de la tabla de Supabase a consultar |
| `filter` | string | Sí | Filtro PostgREST para encontrar la fila específica \(p. ej., "id=eq.123"\) |
| `apiKey` | string | Sí | Tu clave secreta de rol de servicio de Supabase |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Salida</div>

| Parámetro | Tipo | Descripción |
| --------- | ---- | ----------- |
| `message` | string | Mensaje de estado de la operación |

</div>

---

### Crear una Fila

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operación",
            "layout": "full",
            "options": [
                {
                    "label": "Obtener múltiples filas",
                    "id": "query"
                },
                {
                    "label": "Obtener una fila",
                    "id": "get_row"
                },
                {
                    "label": "Crear una fila",
                    "id": "insert"
                },
                {
                    "label": "Actualizar una fila",
                    "id": "update"
                },
                {
                    "label": "Eliminar una fila",
                    "id": "delete"
                },
                {
                    "label": "Upsert de una fila",
                    "id": "upsert"
                },
                {
                    "label": "Búsqueda vectorial",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "insert"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "ID del proyecto",
            "layout": "full",
            "placeholder": "ID de tu proyecto de Supabase (p. ej., jdrkgepadsdopsntdlom)",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "Tabla",
            "layout": "full",
            "placeholder": "Nombre de la tabla",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "Secreto del rol de servicio",
            "layout": "full",
            "placeholder": "Tu clave secreta del rol de servicio de Supabase",
            "password": true,
            "required": true
        },
        {
            "id": "data",
            "type": "code",
            "title": "Datos",
            "layout": "full",
            "placeholder": "{\\n  \"column1\": \"value1\",\\n  \"column2\": \"value2\"\\n}",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Crear una fila (<code>supabase_insert</code>)</div>

Insertar datos en una tabla de Supabase

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Entrada</div>

| Parámetro | Tipo | Requerido | Descripción |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | Sí | ID de tu proyecto de Supabase \(p. ej., jdrkgepadsdopsntdlom\) |
| `table` | string | Sí | El nombre de la tabla de Supabase en la que se insertarán los datos |
| `data` | array | Sí | Los datos a insertar \(array de objetos o un objeto individual\) |
| `apiKey` | string | Sí | Tu clave secreta de rol de servicio de Supabase |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Salida</div>

| Parámetro | Tipo | Descripción |
| --------- | ---- | ----------- |
| `message` | string | Mensaje de estado de la operación |

</div>

---

### Actualizar una fila

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operación",
            "layout": "full",
            "options": [
                {
                    "label": "Obtener múltiples filas",
                    "id": "query"
                },
                {
                    "label": "Obtener una fila",
                    "id": "get_row"
                },
                {
                    "label": "Crear una fila",
                    "id": "insert"
                },
                {
                    "label": "Actualizar una fila",
                    "id": "update"
                },
                {
                    "label": "Eliminar una fila",
                    "id": "delete"
                },
                {
                    "label": "Upsert de una fila",
                    "id": "upsert"
                },
                {
                    "label": "Búsqueda vectorial",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "update"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "ID del proyecto",
            "layout": "full",
            "placeholder": "ID de tu proyecto de Supabase (p. ej., jdrkgepadsdopsntdlom)",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "Tabla",
            "layout": "full",
            "placeholder": "Nombre de la tabla",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "Secreto del rol de servicio",
            "layout": "full",
            "placeholder": "Tu clave secreta del rol de servicio de Supabase",
            "password": true,
            "required": true
        },
        {
            "id": "data",
            "type": "code",
            "title": "Datos",
            "layout": "full",
            "placeholder": "{\\n  \"column1\": \"value1\",\\n  \"column2\": \"value2\"\\n}",
            "language": "json",
            "required": true
        },
        {
            "id": "filter",
            "type": "short-input",
            "title": "Filtro (sintaxis PostgREST)",
            "layout": "full",
            "placeholder": "id=eq.123",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Actualizar una fila (<code>supabase_update</code>)</div>

Actualiza filas en una tabla de Supabase según criterios de filtrado

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Entrada</div>

| Parámetro | Tipo | Requerido | Descripción |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | Sí | ID de tu proyecto de Supabase \(p. ej., jdrkgepadsdopsntdlom\) |
| `table` | string | Sí | El nombre de la tabla de Supabase a actualizar |
| `filter` | string | Sí | Filtro PostgREST para identificar las filas a actualizar \(p. ej., "id=eq.123"\) |
| `data` | object | Sí | Datos a actualizar en las filas coincidentes |
| `apiKey` | string | Sí | Tu clave secreta de rol de servicio de Supabase |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Salida</div>

| Parámetro | Tipo | Descripción |
| --------- | ---- | ----------- |
| `message` | string | Mensaje de estado de la operación |

</div>

---

### Eliminar una fila

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operación",
            "layout": "full",
            "options": [
                {
                    "label": "Obtener Múltiples Filas",
                    "id": "query"
                },
                {
                    "label": "Obtener una Fila",
                    "id": "get_row"
                },
                {
                    "label": "Crear una Fila",
                    "id": "insert"
                },
                {
                    "label": "Actualizar una Fila",
                    "id": "update"
                },
                {
                    "label": "Eliminar una Fila",
                    "id": "delete"
                },
                {
                    "label": "Upsert de una Fila",
                    "id": "upsert"
                },
                {
                    "label": "Búsqueda Vectorial",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "delete"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "ID del Proyecto",
            "layout": "full",
            "placeholder": "ID de tu proyecto de Supabase (p. ej., jdrkgepadsdopsntdlom)",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "Tabla",
            "layout": "full",
            "placeholder": "Nombre de la tabla",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "Secreto del Rol de Servicio",
            "layout": "full",
            "placeholder": "Tu clave secreta del rol de servicio de Supabase",
            "password": true,
            "required": true
        },
        {
            "id": "filter",
            "type": "short-input",
            "title": "Filtro (sintaxis PostgREST)",
            "layout": "full",
            "placeholder": "id=eq.123",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Eliminar una Fila (<code>supabase_delete</code>)</div>

Eliminar filas de una tabla de Supabase según criterios de filtro

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Entrada</div>

| Parámetro | Tipo | Requerido | Descripción |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | Sí | El ID de tu proyecto Supabase \(p. ej., jdrkgepadsdopsntdlom\) |
| `table` | string | Sí | El nombre de la tabla de Supabase de la que se eliminará |
| `filter` | string | Sí | Filtro PostgREST para identificar las filas a eliminar \(p. ej., "id=eq.123"\) |
| `apiKey` | string | Sí | Tu clave secreta de rol de servicio de Supabase |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Salida</div>

| Parámetro | Tipo | Descripción |
| --------- | ---- | ----------- |
| `message` | string | Mensaje de estado de la operación |

</div>

---

### Insertar o Actualizar una Fila

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operación",
            "layout": "full",
            "options": [
                {
                    "label": "Obtener Múltiples Filas",
                    "id": "query"
                },
                {
                    "label": "Obtener una Fila",
                    "id": "get_row"
                },
                {
                    "label": "Crear una Fila",
                    "id": "insert"
                },
                {
                    "label": "Actualizar una Fila",
                    "id": "update"
                },
                {
                    "label": "Eliminar una Fila",
                    "id": "delete"
                },
                {
                    "label": "Upsert de una Fila",
                    "id": "upsert"
                },
                {
                    "label": "Búsqueda Vectorial",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "upsert"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "ID del Proyecto",
            "layout": "full",
            "placeholder": "ID de tu proyecto de Supabase (p. ej., jdrkgepadsdopsntdlom)",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "Tabla",
            "layout": "full",
            "placeholder": "Nombre de la tabla",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "Secreto del Rol de Servicio",
            "layout": "full",
            "placeholder": "Tu clave secreta del rol de servicio de Supabase",
            "password": true,
            "required": true
        },
        {
            "id": "data",
            "type": "code",
            "title": "Datos",
            "layout": "full",
            "placeholder": "{\\n  \"column1\": \"value1\",\\n  \"column2\": \"value2\"\\n}",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Insertar o actualizar una fila (<code>supabase_upsert</code>)</div>

Insertar o actualizar datos en una tabla de Supabase (operación upsert)

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Entrada</div>

| Parámetro | Tipo | Requerido | Descripción |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | Sí | ID de su proyecto Supabase \(p. ej., jdrkgepadsdopsntdlom\) |
| `table` | string | Sí | El nombre de la tabla de Supabase en la que se insertarán o actualizarán los datos |
| `data` | array | Sí | Los datos a insertar o actualizar (upsert) - array de objetos o un objeto individual |
| `apiKey` | string | Sí | Su clave secreta de rol de servicio de Supabase |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Salida</div>

| Parámetro | Tipo | Descripción |
| --------- | ---- | ----------- |
| `message` | string | Mensaje de estado de la operación |

</div>

---

### Búsqueda Vectorial

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operación",
            "layout": "full",
            "options": [
                {
                    "label": "Obtener múltiples filas",
                    "id": "query"
                },
                {
                    "label": "Obtener una fila",
                    "id": "get_row"
                },
                {
                    "label": "Crear una fila",
                    "id": "insert"
                },
                {
                    "label": "Actualizar una fila",
                    "id": "update"
                },
                {
                    "label": "Eliminar una fila",
                    "id": "delete"
                },
                {
                    "label": "Upsert de una fila",
                    "id": "upsert"
                },
                {
                    "label": "Búsqueda vectorial",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "vector_search"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "ID del proyecto",
            "layout": "full",
            "placeholder": "El ID de tu proyecto de Supabase (p. ej., jdrkgepadsdopsntdlom)",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "Tabla",
            "layout": "full",
            "placeholder": "Nombre de la tabla",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "Secreto del rol de servicio",
            "layout": "full",
            "placeholder": "Tu clave secreta del rol de servicio de Supabase",
            "password": true,
            "required": true
        },
        {
            "id": "functionName",
            "type": "short-input",
            "title": "Nombre de la función",
            "layout": "full",
            "placeholder": "match_documents",
            "required": true
        },
        {
            "id": "queryEmbedding",
            "type": "code",
            "title": "Embedding de consulta",
            "layout": "full",
            "placeholder": "[0.1, 0.2, 0.3, ...]",
            "language": "json",
            "required": true
        },
        {
            "id": "matchThreshold",
            "type": "short-input",
            "title": "Umbral de coincidencia (opcional)",
            "layout": "full",
            "placeholder": "0.78"
        },
        {
            "id": "matchCount",
            "type": "short-input",
            "title": "Cantidad de coincidencias (opcional)",
            "layout": "full",
            "placeholder": "10"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Búsqueda Vectorial (<code>supabase_vector_search</code>)</div>

Realiza búsqueda por similitud usando pgvector en una tabla de Supabase

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Entrada</div>

| Parámetro | Tipo | Requerido | Descripción |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | Sí | El ID de tu proyecto de Supabase \(p. ej., jdrkgepadsdopsntdlom\) |
| `functionName` | string | Sí | El nombre de la función PostgreSQL que realiza la búsqueda vectorial \(p. ej., match_documents\) |
| `queryEmbedding` | array | Sí | El vector/embedding de consulta para buscar elementos similares |
| `matchThreshold` | number | No | Umbral mínimo de similitud \(0-1\), típicamente 0.7-0.9 |
| `matchCount` | number | No | Número máximo de resultados a devolver \(por defecto: 10\) |
| `apiKey` | string | Sí | Tu clave secreta de rol de servicio de Supabase |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Salida</div>

| Parámetro | Tipo | Descripción |
| --------- | ---- | ----------- |
| `message` | string | Mensaje de estado de la operación |

</div>

---

## Instrucciones de Uso

Integra Supabase en el flujo de trabajo. Permite obtener múltiples filas, obtener, crear, actualizar, eliminar e insertar o actualizar una fila.

## Notas

- Categoría: `tools`
- Tipo: `supabase`
