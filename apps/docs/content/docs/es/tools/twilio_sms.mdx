---
title: Twilio SMS
description: Enviar mensajes SMS
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"

<BlockInfoCard
  type="twilio_sms"
  color=""
/>

## Configuración

<ShowcaseCard>
  <BlockConfigPreview
    name="Twilio SMS"
    type="twilio_sms"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "phoneNumbers",
            "type": "long-input",
            "title": "Números de teléfono de los destinatarios",
            "layout": "full",
            "placeholder": "Introduce números de teléfono con código de país (uno por línea, p. ej., +1234567890)",
            "required": true
        },
        {
            "id": "message",
            "type": "long-input",
            "title": "Mensaje",
            "layout": "full",
            "placeholder": "p. ej. \"¡Hola! Este es un mensaje de prueba.\"",
            "required": true
        },
        {
            "id": "accountSid",
            "type": "short-input",
            "title": "SID de cuenta de Twilio",
            "layout": "full",
            "placeholder": "Tu SID de cuenta de Twilio",
            "required": true
        },
        {
            "id": "authToken",
            "type": "short-input",
            "title": "Token de autenticación",
            "layout": "full",
            "placeholder": "Tu token de autenticación de Twilio",
            "password": true,
            "required": true
        },
        {
            "id": "fromNumber",
            "type": "short-input",
            "title": "Número de teléfono de origen de Twilio",
            "layout": "full",
            "placeholder": "p. ej. +1234567890",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

## Herramientas

### `twilio_send_sms`

Envía mensajes de texto a uno o varios destinatarios mediante la API de Twilio.

#### Entrada

| Parámetro | Tipo | Obligatorio | Descripción |
| --------- | ---- | -------- | ----------- |
| `phoneNumbers` | string | Sí | Números de teléfono a los que enviar el mensaje, separados por saltos de línea |
| `message` | string | Sí | Mensaje que se enviará |
| `accountSid` | string | Sí | SID de cuenta de Twilio |
| `authToken` | string | Sí | Token de autenticación de Twilio |
| `fromNumber` | string | Sí | Número de teléfono de Twilio desde el que se envía el mensaje |

#### Salida

| Parámetro | Tipo | Descripción |
| --------- | ---- | ----------- |
| `success` | boolean | Estado de éxito del envío de SMS |
| `messageId` | string | Identificador único del mensaje de Twilio \(SID\) |
| `status` | string | Estado de entrega del mensaje de Twilio |
| `fromNumber` | string | Número de teléfono desde el que se envió el mensaje |
| `toNumber` | string | Número de teléfono al que se envió el mensaje |

## Instrucciones de uso

Integra Twilio en el flujo de trabajo. Puede enviar mensajes SMS.

## Notas

- Categoría: `tools`
- Tipo: `twilio_sms`
