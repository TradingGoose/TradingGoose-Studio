---
title: Agente
---

import { Callout } from 'fumadocs-ui/components/callout'
import { Step, Steps } from 'fumadocs-ui/components/steps'
import { Tab, Tabs } from 'fumadocs-ui/components/tabs'
import { BlockConfigPreview } from '@/components/ui/block-config-preview'
import { ShowcaseCard } from '@/components/ui/showcase-card'

El bloque Agente actúa como interfaz entre su flujo de trabajo y los Modelos de Lenguaje de Gran Escala (LLMs). Ejecuta solicitudes de inferencia contra diversos proveedores de IA, procesa entradas en lenguaje natural según las instrucciones definidas y genera salidas estructuradas o no estructuradas para su consumo en etapas posteriores.

<ShowcaseCard>
  <BlockConfigPreview
    name="Agente"
    type="agent"
    color="#2873f6"
    hideHeader
    subBlocks={[
      { "id": "systemPrompt", "title": "Prompt del Sistema", "type": "long-input", "layout": "full", "placeholder": "Ingrese el prompt del sistema..." },
      { "id": "userPrompt", "title": "Prompt del Usuario", "type": "long-input", "layout": "full", "placeholder": "Ingrese el contexto o mensaje del usuario..." },
      { "id": "memories", "title": "Memorias", "type": "short-input", "layout": "full", "placeholder": "Conecte la salida del bloque de memoria..." },
      { "id": "model", "title": "Modelo", "type": "combobox", "layout": "half", "placeholder": "Escriba o seleccione un modelo...", "required": true },
      { "id": "temperature", "title": "Temperatura", "type": "slider", "layout": "half", "defaultValue": 0.5 },
      { "id": "reasoningEffort", "title": "Esfuerzo de Razonamiento", "type": "dropdown", "layout": "half", "placeholder": "Seleccione el esfuerzo de razonamiento...", "options": [{ "label": "mínimo", "id": "minimal" }, { "label": "bajo", "id": "low" }, { "label": "medio", "id": "medium" }, { "label": "alto", "id": "high" }] },
      { "id": "verbosity", "title": "Verbosidad", "type": "dropdown", "layout": "half", "placeholder": "Seleccione la verbosidad...", "options": [{ "label": "baja", "id": "low" }, { "label": "media", "id": "medium" }, { "label": "alta", "id": "high" }] },
      { "id": "apiKey", "title": "Clave API", "type": "short-input", "layout": "full", "placeholder": "Ingrese su clave API", "password": true, "required": true },
      { "id": "azureEndpoint", "title": "Endpoint de Azure OpenAI", "type": "short-input", "layout": "full", "password": true, "placeholder": "https://your-resource.openai.azure.com" },
      { "id": "azureApiVersion", "title": "Versión de la API de Azure", "type": "short-input", "layout": "full", "placeholder": "2024-07-01-preview" },
      { "id": "tools", "title": "Herramientas", "type": "tool-input", "layout": "full", "defaultValue": [] },
      { "id": "skills", "title": "Habilidades", "type": "skill-input", "layout": "full", "defaultValue": [] },
      { "id": "responseFormat", "title": "Formato de Respuesta", "type": "code", "layout": "full", "placeholder": "Ingrese el esquema JSON..." }
    ]}
  />
</ShowcaseCard>

## Descripción general

El bloque Agente le permite:

<Steps>
  <Step>
    <strong>Procesar lenguaje natural</strong>: Analizar la entrada del usuario y generar respuestas contextuales
  </Step>
  <Step>
    <strong>Ejecutar tareas impulsadas por IA</strong>: Realizar análisis de contenido, generación y toma de decisiones
  </Step>
  <Step>
    <strong>Invocar herramientas externas</strong>: Acceder a APIs, bases de datos y servicios durante el procesamiento
  </Step>
  <Step>
    <strong>Generar salida estructurada</strong>: Devolver datos JSON que coincidan con los requisitos de su esquema
  </Step>
</Steps> 

## Opciones de configuración

### Prompt del sistema

El prompt del sistema establece los parámetros operativos y las restricciones de comportamiento del agente. Esta configuración define el rol del agente, la metodología de respuesta y los límites de procesamiento para todas las solicitudes entrantes.

```markdown
You are a helpful assistant that specializes in financial analysis.
Always provide clear explanations and cite sources when possible.
When responding to questions about investments, include risk disclaimers.
```

### Prompt del usuario

El prompt del usuario representa los datos de entrada principales para el procesamiento de inferencia. Este parámetro acepta texto en lenguaje natural o datos estructurados que el agente analizará y a los que responderá. Las fuentes de entrada incluyen:

- **Configuración estática**: Texto de entrada directo especificado en la configuración del bloque
- **Entrada dinámica**: Datos transmitidos desde bloques anteriores a través de interfaces de conexión
- **Generación en tiempo de ejecución**: Contenido generado de forma programática durante la ejecución del flujo de trabajo

### Selección de modelo

El bloque Agente admite múltiples proveedores de LLM a través de una interfaz de inferencia unificada. Los modelos disponibles incluyen:

**Modelos OpenAI**: GPT-5, GPT-4o, o1, o3, o4-mini, gpt-4.1 (inferencia basada en API)
**Modelos Anthropic**: Claude 3.7 Sonnet (inferencia basada en API)
**Modelos Google**: Gemini 2.5 Pro, Gemini 2.0 Flash (inferencia basada en API)
**Proveedores alternativos**: Groq, Cerebras, xAI, DeepSeek (inferencia basada en API)
**Despliegue local**: Modelos compatibles con Ollama (inferencia autohospedada)

### Temperatura

Controle la creatividad y aleatoriedad de las respuestas:

<Tabs items={['Baja (0-0.3)', 'Media (0.3-0.7)', 'Alta (0.7-2.0)']}>
  <Tab>
    Respuestas más deterministas y enfocadas. Ideal para tareas factuales, soporte al cliente y
    situaciones donde la precisión es crítica.
  </Tab>
  <Tab>
    Creatividad y enfoque equilibrados. Adecuado para aplicaciones de uso general que requieren tanto
    precisión como cierta creatividad.
  </Tab>
  <Tab>
    Respuestas más creativas y variadas. Ideal para escritura creativa, lluvia de ideas y generación
    de ideas diversas.
  </Tab>
</Tabs>

<div className="mt-4 text-sm text-gray-600 dark:text-gray-400">
  El rango de temperatura (0-1 o 0-2) varía según el modelo seleccionado.
</div>

### Clave de API

Tu clave de API para el proveedor de LLM seleccionado. Se almacena de forma segura y se utiliza para la autenticación.

### Herramientas

Las herramientas amplían las capacidades del agente mediante integraciones con APIs externas y conexiones a servicios. El sistema de herramientas habilita la llamada a funciones, permitiendo al agente ejecutar operaciones más allá de la generación de texto.

**Proceso de integración de herramientas**:
1. Accede a la sección de configuración de Herramientas dentro del bloque de Agente
2. Selecciona entre más de 60 integraciones predefinidas o define funciones personalizadas
3. Configura los parámetros de autenticación y las restricciones operativas

**Categorías de herramientas disponibles**:
- **Comunicación**: Gmail, Slack, Telegram, WhatsApp, Microsoft Teams
- **Fuentes de datos**: Notion, Google Sheets, Airtable, Supabase, Pinecone
- **Servicios web**: Firecrawl, Google Search, Exa AI, automatización de navegador
- **Desarrollo**: GitHub, Jira, Linear — gestión de repositorios e incidencias
- **Servicios de IA**: OpenAI, Perplexity, Hugging Face, ElevenLabs

**Control de ejecución de herramientas**:
- **Auto**: El modelo determina la invocación de herramientas según el contexto y la necesidad
- **Requerido**: La herramienta debe invocarse en cada solicitud de inferencia
- **Ninguno**: La definición de la herramienta está disponible pero excluida del contexto del modelo

### Formato de respuesta

El parámetro de Formato de respuesta impone la generación de salidas estructuradas mediante validación de esquemas JSON. Esto garantiza respuestas consistentes y legibles por máquina que se ajustan a estructuras de datos predefinidas:

```json
{
  "name": "user_analysis",
  "schema": {
    "type": "object",
    "properties": {
      "sentiment": {
        "type": "string",
        "enum": ["positive", "negative", "neutral"]
      },
      "confidence": {
        "type": "number",
        "minimum": 0,
        "maximum": 1
      }
    },
    "required": ["sentiment", "confidence"]
  }
}
```

Esta configuración restringe la salida del modelo para que cumpla con el esquema especificado, evitando respuestas de texto libre y asegurando la generación de datos estructurados.

### Acceso a los resultados

Una vez que el agente finaliza, puedes acceder a sus salidas:

- **`<agent.content>`**: El texto de respuesta del agente o los datos estructurados
- **`<agent.tokens>`**: Estadísticas de uso de tokens (prompt, completado, total)
- **`<agent.tool_calls>`**: Detalles de las herramientas utilizadas por el agente durante la ejecución
- **`<agent.cost>`**: Costo estimado de la llamada a la API (si está disponible)

## Funciones Avanzadas

### Memoria + Agente: Historial de Conversación

Utilice un bloque `Memory` con un `id` consistente (por ejemplo, `chat`) para persistir mensajes entre ejecuciones e incluir ese historial en el prompt del Agente.

- Agregue el mensaje del usuario antes del Agente
- Lea el historial de conversación para obtener contexto
- Añada la respuesta del Agente después de su ejecución

Consulte la referencia del bloque [`Memory`](/tools/memory) para más detalles.

## Entradas y Salidas

<Tabs items={['Configuration', 'Variables', 'Results']}>
  <Tab>
    <ul className="list-disc space-y-2 pl-6">
      <li>
        <strong>System Prompt</strong>: Instrucciones que definen el comportamiento y rol del agente
      </li>
      <li>
        <strong>User Prompt</strong>: Texto de entrada o datos a procesar
      </li>
      <li>
        <strong>Model</strong>: Selección del modelo de IA (OpenAI, Anthropic, Google, etc.)
      </li>
      <li>
        <strong>Temperature</strong>: Control de aleatoriedad en las respuestas (0-2)
      </li>
      <li>
        <strong>Tools</strong>: Array de herramientas disponibles para la invocación de funciones
      </li>
      <li>
        <strong>Response Format</strong>: Esquema JSON para salida estructurada
      </li>
    </ul>
  </Tab>
  <Tab>
    <ul className="list-disc space-y-2 pl-6">
      <li>
        <strong>agent.content</strong>: Texto de respuesta o datos estructurados del Agente
      </li>
      <li>
        <strong>agent.tokens</strong>: Objeto con estadísticas de uso de tokens
      </li>
      <li>
        <strong>agent.tool_calls</strong>: Array con detalles de ejecución de herramientas
      </li>
      <li>
        <strong>agent.cost</strong>: Costo estimado de la llamada a la API (si está disponible)
      </li>
    </ul>
  </Tab>
  <Tab>
    <ul className="list-disc space-y-2 pl-6">
      <li>
        <strong>Content</strong>: Salida de respuesta principal del agente
      </li>
      <li>
        <strong>Metadata</strong>: Estadísticas de uso y detalles de ejecución
      </li>
      <li>
        <strong>Access</strong>: Disponible en bloques posteriores al agente
      </li>
    </ul>
  </Tab>
</Tabs>

## Casos de Uso de Ejemplo

### Automatización de Soporte al Cliente

<div className="mb-4 rounded-md border p-4">
  <h4 className="font-medium">Escenario: Gestión de consultas de clientes con acceso a base de datos</h4>
  <ol className="list-decimal pl-5 text-sm">
    <li>El usuario envía un ticket de soporte a través del bloque de API</li>
    <li>El Agente verifica pedidos/suscripciones en Postgres y busca orientación en la base de conocimiento</li>
    <li>Si se requiere escalación, el Agente crea un issue en Linear con el contexto relevante</li>
    <li>El Agente redacta una respuesta clara por correo electrónico</li>
    <li>Gmail envía la respuesta al cliente</li>
    <li>La conversación se guarda en la Memoria para mantener el historial en futuros mensajes</li>
  </ol>
</div>

### Análisis de Contenido con Múltiples Modelos

<div className="mb-4 rounded-md border p-4">
  <h4 className="font-medium">Escenario: Analizar contenido con diferentes modelos de IA</h4>
  <ol className="list-decimal pl-5 text-sm">
    <li>El bloque de función procesa el documento cargado</li>
    <li>El agente con GPT-4o realiza el análisis técnico</li>
    <li>El agente con Claude analiza el sentimiento y el tono</li>
    <li>El bloque de función combina los resultados para el informe final</li>
  </ol>
</div>

### Asistente de Investigación con Herramientas

<div className="mb-4 rounded-md border p-4">
  <h4 className="font-medium">Escenario: Asistente de investigación con búsqueda web y acceso a documentos</h4>
  <ol className="list-decimal pl-5 text-sm">
    <li>Consulta del usuario recibida a través de la entrada</li>
    <li>El agente realiza búsquedas web mediante la herramienta Google Search</li>
    <li>El agente accede a la base de datos de Notion para documentos internos</li>
    <li>El agente compila un informe de investigación exhaustivo</li>
  </ol>
</div>

## Mejores Prácticas

- **Sea específico en los prompts del sistema**: Defina claramente el rol, el tono y las limitaciones del agente. Cuanto más específicas sean sus instrucciones, mejor podrá el agente cumplir su propósito previsto.
- **Elija la configuración de temperatura adecuada**: Utilice valores de temperatura bajos (0-0.3) cuando la precisión sea importante, o aumente la temperatura (0.7-2.0) para obtener respuestas más creativas o variadas.
- **Aproveche las herramientas de manera efectiva**: Integre herramientas que complementen el propósito del agente y potencien sus capacidades. Sea selectivo con las herramientas que proporciona para evitar sobrecargar al agente. Para tareas con poca superposición, utilice otro bloque de Agente para obtener los mejores resultados.
