---
title: Series de datos
description: Datos de precios, series temporales, estado de las barras e información del símbolo integrados
---

import { Callout } from 'fumadocs-ui/components/callout'

PineTS proporciona series de datos integradas que están disponibles en todos los indicadores sin necesidad de importarlas. Representan los datos OHLCV del símbolo actual, la información temporal y el estado de las barras.

## Datos de precios (OHLCV)

| Serie | Tipo | Descripción |
|--------|------|-------------|
| `open` | `series<number>` | Precio de apertura de cada barra |
| `high` | `series<number>` | Precio máximo de cada barra |
| `low` | `series<number>` | Precio mínimo de cada barra |
| `close` | `series<number>` | Precio de cierre de cada barra |
| `volume` | `series<number>` | Volumen negociado de cada barra |

### Series de precios calculadas

| Serie | Fórmula | Descripción |
|--------|---------|-------------|
| `hl2` | `(high + low) / 2` | Precio medio |
| `hlc3` | `(high + low + close) / 3` | Precio típico |
| `ohlc4` | `(open + high + low + close) / 4` | Precio promedio |

```javascript
// Use any price series in calculations
const typicalPrice = hlc3;
const sma = ta.sma(typicalPrice, 20);
plot(sma, 'SMA of Typical Price');
```

## Series temporales

| Serie | Tipo | Descripción |
|--------|------|-------------|
| `openTime` | `series<number>` | Hora de apertura de la barra en milisegundos |
| `closeTime` | `series<number>` | Hora de cierre de la barra en milisegundos |

## Información de las barras

| Variable | Tipo | Descripción |
|----------|------|-------------|
| `bar_index` | `series<number>` | Índice de la barra actual (desde 0) |
| `last_bar_index` | `series<number>` | Índice de la última barra del conjunto de datos |
| `last_bar_time` | `series<number>` | Marca de tiempo de la última barra |

```javascript
// Only plot on the last 100 bars
if (bar_index > last_bar_index - 100) {
  plot(close, 'Recent Close');
}
```

## Estado de las barras

El objeto `barstate` proporciona indicadores sobre la posición de la barra actual en el conjunto de datos:

| Propiedad | Tipo | Descripción |
|----------|------|-------------|
| `barstate.isfirst` | `boolean` | Verdadero en la primera barra |
| `barstate.islast` | `boolean` | Verdadero en la última barra |
| `barstate.ishistory` | `boolean` | Verdadero para las barras históricas |
| `barstate.isrealtime` | `boolean` | Verdadero para las barras en vivo o en transmisión |
| `barstate.isnew` | `boolean` | Verdadero en el primer tick de una barra nueva |
| `barstate.isconfirmed` | `boolean` | Verdadero cuando la barra está confirmada (cerrada) |
| `barstate.islastconfirmedhistory` | `boolean` | Verdadero en la última barra histórica confirmada |

## Información del símbolo

El objeto `syminfo` proporciona información sobre el símbolo actual:

| Propiedad | Descripción |
|----------|-------------|
| `syminfo.ticker` | Nombre del símbolo (por ejemplo, `"BTCUSDT"`) |
| `syminfo.tickerid` | ID completo del ticker |
| `syminfo.description` | Descripción del símbolo |
| `syminfo.type` | Tipo de activo (por ejemplo, `"crypto"`, `"stock"`) |
| `syminfo.currency` | Moneda de cotización |
| `syminfo.basecurrency` | Moneda base |
| `syminfo.mintick` | Incremento mínimo del precio |
| `syminfo.timezone` | Zona horaria de la bolsa |

## Valores especiales

| Valor | Descripción |
|-------|-------------|
| `na` | Representa «no disponible», de forma similar a `null`. Úselo para comprobar si existe un valor |
| `nz(value, replacement?)` | Devuelve `replacement` (de forma predeterminada, `0`) si `value` es `na` |

```javascript
const prev = close[1];
// na check
if (!na(prev)) {
  const change = close - prev;
  plot(change, 'Change');
}

// nz shorthand — default to 0 if na
const safeChange = nz(close - close[1], 0);
```

## Constantes globales

| Espacio de nombres | Descripción |
|-----------|-------------|
| `timeframe` | Información del marco temporal actual (`timeframe.period`, `timeframe.multiplier`) |
| `order` | Constantes del orden de clasificación (`order.ascending`, `order.descending`) |
| `currency` | Constantes de códigos de moneda (59 monedas, incluidas `currency.BTC` y `currency.USD`) |
| `display` | Constantes del modo de visualización |
| `shape` | Constantes de tipos de formas para `plotshape()` |
| `location` | Constantes de ubicación (`location.abovebar`, `location.belowbar`, etc.) |
| `size` | Constantes de tamaño (`size.tiny`, `size.small`, `size.normal`, `size.large`, `size.huge`) |
| `format` | Constantes de formato (`format.price`, `format.volume`, `format.percent`) |
| `dayofweek` | Constantes de los días de la semana |

<Callout type="info">
  Todas las series de datos permiten acceder al historial mediante la notación de corchetes: `close[1]` devuelve el cierre de la barra anterior y `close[10]`, el cierre de hace 10 barras.
</Callout>
