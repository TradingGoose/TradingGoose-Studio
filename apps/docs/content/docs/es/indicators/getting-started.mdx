---
title: Primeros pasos
description: Escribe y despliega tu primer indicador personalizado
---

import { Callout } from 'fumadocs-ui/components/callout'
import { Step, Steps } from 'fumadocs-ui/components/steps'

## Crear tu primer indicador

<Steps>
  <Step>**Abre Indicator Editor** — Añade el widget o abre la lista de indicadores y haz clic en "New Indicator".</Step>
  <Step>**Escribe el código** — Empieza con `indicator()`, define entradas, calcula valores y representa resultados.</Step>
  <Step>**Previsualiza en el gráfico** — Data Chart muestra la salida en tiempo real mientras editas.</Step>
  <Step>**Guarda y despliega** — Asigna un nombre y guarda el indicador para usarlo en gráficos y workflows.</Step>
</Steps>

## Ejemplo mínimo

Todo indicador comienza con `indicator()` y al menos un `plot()`:

```javascript
indicator('Simple Moving Average', { overlay: true });

const length = input.int(20, 'Length');
const sma = ta.sma(close, length);

plot(sma, 'SMA', { color: '#2962FF' });
```

Este indicador incluye un parámetro `Length` configurable con valor 20, una media móvil simple de los cierres y una línea azul superpuesta al gráfico.

## Opciones del indicador

`indicator()` acepta un título y un objeto de opciones:

```javascript
indicator('My Indicator', {
  overlay: true,        // Plot on price chart (false = separate pane)
  precision: 2,         // Decimal places for values
  format: format.price, // Display format
});
```

| Opción | Tipo | Valor predeterminado | Descripción |
|---|---|---|---|
| `overlay` | boolean | `false` | Representa sobre el precio en lugar de un panel independiente. |
| `precision` | number | auto | Número de decimales. |
| `format` | string | `format.inherit` | Formato (`format.price`, `format.volume`, `format.percent`). |
| `scale` | string | auto | Tipo de escala. |
| `max_bars_back` | number | auto | Máximo de barras históricas referenciables. |
| `max_lines_count` | number | auto | Máximo de líneas dibujadas. |
| `max_labels_count` | number | auto | Máximo de etiquetas dibujadas. |
| `max_boxes_count` | number | auto | Máximo de cuadros dibujados. |

## Ejemplo más completo

Este indicador de bandas de Bollinger usa entradas, desestructuración de tuplas, varios gráficos y relleno:

```javascript
indicator('Bollinger Bands', { overlay: true });

const length = input.int(20, 'Length');
const mult = input.float(2, 'StdDev');

// ta.bb returns [middle, upper, lower] as a tuple
const [middle, upper, lower] = ta.bb(close, length, mult);

const upperPlot = plot(upper, 'Upper', { color: '#F23645' });
plot(middle, 'Middle', { color: '#2962FF' });
const lowerPlot = plot(lower, 'Lower', { color: '#089981' });

// Fill the area between upper and lower bands
fill(upperPlot, lowerPlot);
```

## Añadir un trigger de workflow

`trigger()` es una extensión de TradingGoose que conecta señales del indicador con la automatización:

```javascript
indicator('MACD Crossover Signal');

const [macdLine, signalLine, hist] = ta.macd(close, 12, 26, 9);

plot(hist, 'Histogram', { style: plot.style_histogram });
plot(macdLine, 'MACD');
plot(signalLine, 'Signal');

// Fire a workflow when MACD crosses above signal line
if (ta.crossover(macdLine, signalLine)) {
  trigger('macd_bullish_cross', {
    condition: ta.crossover(macdLine, signalLine),
    input: 'MACD bullish crossover detected',
    signal: 'long',
    position: 'belowBar',
    color: '#00FF00'
  });
}
```

<Callout type="info">La API `trigger()` es exclusiva de TradingGoose y no existe en Pine Script estándar. Consulta la [guía de triggers](/indicators/triggers).</Callout>

## Siguientes pasos

- Aprende las [reglas de sintaxis de PineTS](/indicators/syntax), especialmente `let` y `var`.
- Explora los [tipos de entrada](/indicators/inputs).
- Consulta la [referencia de análisis técnico](/indicators/ta) con más de 57 funciones.
