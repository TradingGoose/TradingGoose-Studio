---
title: Análisis técnico
description: Más de 57 funciones de análisis técnico integradas en el espacio de nombres ta
---

import { Callout } from 'fumadocs-ui/components/callout'

El espacio de nombres `ta` proporciona más de 57 funciones de análisis técnico que abarcan medias móviles, osciladores, volatilidad, volumen, detección de tendencias y estadística.

## Medias móviles

| Función | Descripción |
|----------|-------------|
| `ta.sma(source, length)` | Media móvil simple |
| `ta.ema(source, length)` | Media móvil exponencial |
| `ta.wma(source, length)` | Media móvil ponderada |
| `ta.vwma(source, length)` | Media móvil ponderada por volumen |
| `ta.rma(source, length)` | Media móvil suavizada (suavizado de Wilder) |
| `ta.hma(source, length)` | Media móvil de Hull |
| `ta.alma(source, length, offset?, sigma?)` | Media móvil de Arnaud Legoux |
| `ta.swma(source)` | Media móvil ponderada simétrica |
| `ta.linreg(source, length, offset?)` | Regresión lineal |
| `ta.vwap` | Precio promedio ponderado por volumen (variable) |
| `ta.vwap(source)` | Precio promedio ponderado por volumen (función) |

```javascript
const fast = ta.ema(close, 9);
const slow = ta.ema(close, 21);
plot(fast, 'Fast EMA', { color: '#2962FF' });
plot(slow, 'Slow EMA', { color: '#FF6D00' });
```

## Osciladores e impulso

| Función | Devuelve | Descripción |
|----------|---------|-------------|
| `ta.rsi(source, length)` | `number` | Índice de fuerza relativa |
| `ta.macd(source, fast, slow, signal)` | `[macd, signal, hist]` | MACD (devuelve una tupla) |
| `ta.stoch(source, high, low, length)` | `number` | Oscilador estocástico |
| `ta.cci(source, length)` | `number` | Índice del canal de materias primas |
| `ta.mfi(series, length)` | `number` | Índice de flujo de dinero |
| `ta.mom(source, length)` | `number` | Impulso |
| `ta.roc(source, length)` | `number` | Tasa de cambio |
| `ta.tsi(source, short, long)` | `number` | Índice de fuerza verdadera |
| `ta.cmo(source, length)` | `number` | Oscilador de impulso de Chande |
| `ta.cog(source, length)` | `number` | Centro de gravedad |
| `ta.wpr(length)` | `number` | Williams %R |
| `ta.change(source, length?)` | `number` | Variación del precio durante N barras |

```javascript
const rsi = ta.rsi(close, 14);
const [macdLine, signalLine, hist] = ta.macd(close, 12, 26, 9);
```

## Volatilidad y rango

| Función | Devuelve | Descripción |
|----------|---------|-------------|
| `ta.atr(length)` | `number` | Rango verdadero medio |
| `ta.bb(source, length, mult)` | `[middle, upper, lower]` | Bandas de Bollinger (tupla) |
| `ta.bbw(source, length, mult)` | `number` | Anchura de las bandas de Bollinger |
| `ta.kc(source, length, mult)` | `[middle, upper, lower]` | Canales de Keltner (tupla) |
| `ta.kcw(source, length, mult)` | `number` | Anchura de los canales de Keltner |
| `ta.stdev(source, length)` | `number` | Desviación estándar |
| `ta.dev(source, length)` | `number` | Desviación absoluta media |
| `ta.variance(source, length)` | `number` | Varianza |
| `ta.range(source, length)` | `number` | Rango (máximo - mínimo) |
| `ta.tr` | `number` | Rango verdadero (variable) |
| `ta.tr(handleNa?)` | `number` | Rango verdadero (función) |

```javascript
const [middle, upper, lower] = ta.bb(close, 20, 2);
const atr = ta.atr(14);
```

## Indicadores de volumen

Se accede a estos indicadores como variables, no como llamadas a funciones:

| Variable | Descripción |
|----------|-------------|
| `ta.obv` | Volumen en balance |
| `ta.accdist` | Acumulación/distribución |
| `ta.pvt` | Tendencia precio-volumen |
| `ta.wad` | Acumulación/distribución de Williams |
| `ta.wvad` | Acumulación/distribución variable de Williams |
| `ta.nvi` | Índice de volumen negativo |
| `ta.pvi` | Índice de volumen positivo |
| `ta.iii` | Índice de intensidad intradía |

```javascript
plot(ta.obv, 'OBV');
plot(ta.accdist, 'A/D');
```

## Análisis de tendencias

| Función | Devuelve | Descripción |
|----------|---------|-------------|
| `ta.crossover(a, b)` | `boolean` | Verdadero cuando `a` cruza por encima de `b` |
| `ta.crossunder(a, b)` | `boolean` | Verdadero cuando `a` cruza por debajo de `b` |
| `ta.cross(a, b)` | `boolean` | Verdadero cuando `a` cruza `b` en cualquier dirección |
| `ta.rising(source, length)` | `boolean` | Verdadero cuando la fuente ha subido durante `length` barras |
| `ta.falling(source, length)` | `boolean` | Verdadero cuando la fuente ha bajado durante `length` barras |
| `ta.supertrend(factor, atrLength)` | `[value, direction]` | SuperTrend (tupla) |
| `ta.sar(start, inc, max)` | `number` | SAR parabólico |
| `ta.dmi(diLength, adxSmoothing)` | `[plus, minus, adx]` | Índice de movimiento direccional (tupla) |

```javascript
if (ta.crossover(fast, slow)) {
  plotshape(close, 'Buy', { style: shape.triangleup, location: location.belowbar });
}
```

## Funciones estadísticas

| Función | Descripción |
|----------|-------------|
| `ta.highest(source, length)` | Valor máximo durante N barras |
| `ta.lowest(source, length)` | Valor mínimo durante N barras |
| `ta.highestbars(source, length)` | Barras desde el valor máximo |
| `ta.lowestbars(source, length)` | Barras desde el valor mínimo |
| `ta.median(source, length)` | Valor mediano |
| `ta.mode(source, length)` | Moda |
| `ta.percentile_linear_interpolation(source, length, pct)` | Percentil (interpolación lineal) |
| `ta.percentile_nearest_rank(source, length, pct)` | Percentil (rango más cercano) |
| `ta.percentrank(source, length)` | Rango percentil |
| `ta.correlation(a, b, length)` | Coeficiente de correlación |
| `ta.max(a, b)` | Máximo de dos valores |
| `ta.min(a, b)` | Mínimo de dos valores |

## Funciones auxiliares

| Función | Descripción |
|----------|-------------|
| `ta.barssince(condition)` | Número de barras desde que se cumplió la condición |
| `ta.valuewhen(condition, source, occurrence)` | Valor cuando se cumplió la condición |
| `ta.cum(source)` | Suma acumulada |
| `ta.rci(source, length)` | Índice de correlación de rangos |

## Soporte y resistencia

| Función | Descripción |
|----------|-------------|
| `ta.pivothigh(source, leftbars, rightbars)` | Detecta máximos pivote |
| `ta.pivotlow(source, leftbars, rightbars)` | Detecta mínimos pivote |
| `ta.pivot_point_levels(type, anchor)` | Calcula niveles de puntos pivote |
