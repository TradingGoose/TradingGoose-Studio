---
title: Generic Trigger
description: Recibir webhooks desde cualquier servicio o API
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"
import { SchemaTree } from "@/components/ui/schema-tree"
import { Callout } from 'fumadocs-ui/components/callout'


<BlockInfoCard
  type="generic"
  color=""
/>

Recibir webhooks desde cualquier servicio o API

<Callout type="info">
  Este trigger se basa en webhooks. Configura la URL del webhook en el servicio externo para enviar eventos a TradingGoose.
</Callout>


## Configuración

<ShowcaseCard>
  <BlockConfigPreview
    name="Generic Webhook"
    type="generic_webhook"
    hideHeader
    subBlocks={[
        {
            "id": "requireAuth",
            "type": "switch",
            "title": "Require Autenticación",
            "description": "Require autenticación para todos webhook solicitudes"
        },
        {
            "id": "token",
            "type": "short-input",
            "title": "Autenticación Token",
            "placeholder": "Enter an auth token",
            "description": "Token usado a authenticate webhook solicitudes via Bearer token o personalizado header",
            "password": true
        },
        {
            "id": "secretHeaderName",
            "type": "short-input",
            "title": "Secreto Header Nombre (Opcional)",
            "placeholder": "X-Secret-Key",
            "description": "Personalizado HTTP header nombre para el autenticación token. Si blank, usa \"Authorization: Bearer TOKEN\""
        },
        {
            "id": "inputFormat",
            "type": "input-format",
            "title": "Entrada Formato",
            "description": "Define el expected JSON entrada esquema para este webhook (opcional). Usar tipo \"archivos\" para file uploads."
        }
    ]}
  />
</ShowcaseCard>

#### Salida Esquema

El trigger pasa la carga útil completa del evento al workflow. Accede a sus campos mediante la sintaxis `<trigger.fieldName>`.

