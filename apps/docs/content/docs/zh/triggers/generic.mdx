---
title: Generic trigger
description: 接收 webhook，来源为任何服务或API
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"
import { SchemaTree } from "@/components/ui/schema-tree"
import { Callout } from 'fumadocs-ui/components/callout'


<BlockInfoCard
  type="generic"
  color=""
/>

接收 webhook，来源为任何服务或API

<Callout type="info">
  这是基于 webhook 的 trigger。请在外部服务中配置 webhook URL，以便向 TradingGoose 发送事件。
</Callout>


## 配置

<ShowcaseCard>
  <BlockConfigPreview
    name="Generic webhook"
    type="generic_webhook"
    hideHeader
    subBlocks={[
        {
            "id": "requireAuth",
            "type": "switch",
            "title": "Require 身份验证",
            "description": "Require 身份验证，用于所有 webhook 请求"
        },
        {
            "id": "token",
            "type": "short-input",
            "title": "身份验证 token",
            "placeholder": "Enter an auth token",
            "description": "token 使用的到authenticate webhook 请求 via Bearer token或自定义 header",
            "password": true
        },
        {
            "id": "secretHeaderName",
            "type": "short-input",
            "title": "密钥 Header 名称 (可选)",
            "placeholder": "X-Secret-Key",
            "description": "自定义 HTTP header 名称，用于身份验证 token. 如果 blank, 使用 \"Authorization: Bearer TOKEN\""
        },
        {
            "id": "inputFormat",
            "type": "input-format",
            "title": "输入格式",
            "description": "Define expected JSON 输入架构，用于此 webhook (可选). 使用类型 \"文件\"，用于file uploads."
        }
    ]}
  />
</ShowcaseCard>

#### 输出架构

触发器会将完整的事件负载传递给工作流。使用 `<trigger.fieldName>` 语法访问字段。

