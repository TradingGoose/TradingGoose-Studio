---
title: Grain 触发器
description: 当 Grain 中的录音更新时触发工作流
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"
import { SchemaTree } from "@/components/ui/schema-tree"
import { Callout } from 'fumadocs-ui/components/callout'

<BlockInfoCard
  type="grain"
  color=""
  icon={true}
  iconSvg={``}
/>

当 Grain 中的录音更新时触发工作流

<Callout type="info">
  这是一个**基于 Webhook** 的触发器。请在您的外部服务中配置 Webhook URL，以将事件发送至 TradingGoose。
</Callout>

## 事件

### Grain 录音已更新

当 Grain 中的录音更新时触发工作流

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain 录音已更新"
    type="grain_recording_updated"
    hideHeader
    subBlocks={[
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "输入您的 Grain API 密钥（个人访问令牌）",
            "description": "创建 Grain Webhook 所必需。",
            "required": true,
            "password": true
        }
    ]}
  />
</ShowcaseCard>

#### 输出 Schema

触发器将完整的事件负载传递至您的工作流。使用 `<trigger.fieldName>` 语法访问各字段。

---

### Grain 录音已创建

当 Grain 中添加新录音时触发工作流

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain Recording Created"
    type="grain_recording_created"
    hideHeader
    subBlocks={[
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "请输入您的 Grain API 密钥（个人访问令牌）",
            "description": "在 Grain 中创建 Webhook 时需要提供此密钥。",
            "required": true,
            "password": true
        }
    ]}
  />
</ShowcaseCard>

#### 输出模式

触发器将完整的事件载荷传递至您的工作流。使用 `<trigger.fieldName>` 语法访问各字段。

---

### Grain Webhook

适用于所有 Grain 事件的通用 Webhook 触发器

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain Webhook"
    type="grain_webhook"
    hideHeader
    subBlocks={[
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "请输入您的 Grain API 密钥（个人访问令牌）",
            "description": "在 Grain 中创建 Webhook 时需要提供此密钥。",
            "required": true,
            "password": true
        }
    ]}
  />
</ShowcaseCard>

#### 输出模式

触发器将完整的事件载荷传递至您的工作流。使用 `<trigger.fieldName>` 语法访问各字段。

---

### Grain 高亮片段已更新

当 Grain 中的高亮片段/剪辑被更新时触发工作流

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain Highlight Updated"
    type="grain_highlight_updated"
    hideHeader
    subBlocks={[
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "请输入您的 Grain API 密钥（个人访问令牌）",
            "description": "在 Grain 中创建 Webhook 时需要提供此密钥。",
            "required": true,
            "password": true
        }
    ]}
  />
</ShowcaseCard>

#### 输出模式

触发器将完整的事件载荷传递至您的工作流。使用 `<trigger.fieldName>` 语法访问各字段。

---

### Grain 故事已创建

当 Grain 中创建新故事时触发工作流

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain Story Created"
    type="grain_story_created"
    hideHeader
    subBlocks={[
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "请输入您的 Grain API 密钥（个人访问令牌）",
            "description": "在 Grain 中创建 Webhook 时需要提供此密钥。",
            "required": true,
            "password": true
        }
    ]}
  />
</ShowcaseCard>

#### 输出模式

触发器将完整的事件载荷传递给您的工作流。使用 `<trigger.fieldName>` 语法访问字段。

---

### Grain 高亮片段已创建

当在 Grain 中创建新的高亮片段/剪辑时触发工作流

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain Highlight Created"
    type="grain_highlight_created"
    hideHeader
    subBlocks={[
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "输入您的 Grain API 密钥（个人访问令牌）",
            "description": "创建 Grain Webhook 所必需。",
            "required": true,
            "password": true
        }
    ]}
  />
</ShowcaseCard>

#### 输出模式

触发器将完整的事件载荷传递给您的工作流。使用 `<trigger.fieldName>` 语法访问字段。

---
