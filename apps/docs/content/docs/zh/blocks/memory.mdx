---
title: 记忆
description: 在多次工作流执行之间持久保存对话消息
---

import { Callout } from 'fumadocs-ui/components/callout'
import { Step, Steps } from 'fumadocs-ui/components/steps'
import { Tab, Tabs } from 'fumadocs-ui/components/tabs'
import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"

<BlockInfoCard
  type="memory"
  color="#F64F9E"
  icon={true}
/>

记忆块旨在跨工作流执行存储和检索对话消息，并向 Agent 提供历史记录。当前 Add 操作不可用；读取和删除操作可用于管理已有记录。

<Callout type="warning">
  当前无法通过该块使用 Add Memory：请求缺少记忆端点要求的 `type` 和 `workflowId`，导致添加消息失败。配置 ID、Role 和 Content 无法解决此不匹配。修复前请勿依赖此块保存新的对话历史。
</Callout>

## 概览

<Steps>
  <Step>
    <strong>添加记忆</strong>：当前不可用；预期用途是将消息存入指定的记忆 ID
  </Step>
  <Step>
    <strong>获取记忆</strong>：按 ID 检索单条记忆条目
  </Step>
  <Step>
    <strong>获取全部记忆</strong>: 获取当前工作流最多 50 条记忆记录
  </Step>
  <Step>
    <strong>删除记忆</strong>：按 ID 删除记忆条目
  </Step>
</Steps>

## 工作原理

记忆条目以聊天风格的消息存储，包含三个必填字段：用于限定记忆范围的 **ID**、**Role**（`user`、`assistant` 或 `system`）以及 **Content** 文本。之后检索记忆并将其传入 Agent 块的 Memories 输入时，Agent 会将它们视为先前的对话轮次——从而支持跨独立执行的多轮对话。

预期的 Add → Agent → Add 流程目前受 Add 限制阻断。仍可通过 Get Memory 读取已有记录，并将返回的消息数组传入 Agent 的 Memories 字段。

<Callout type="info">
  记忆 ID 用于限定对话范围。在多次执行中使用同一 ID 可构建连续的对话历史，或使用不同 ID 来维护独立线程。
</Callout>

## 操作

<Tabs items={['添加记忆', '获取记忆', '获取所有记忆', '删除记忆']}>
  <Tab>
    当前不可用。下方展示预期的配置字段，但填写这些字段无法修复上述请求接口不一致的问题。

    | 字段 | 必填 | 描述 |
    |---|---|---|
    | **ID** | 是 | 记忆标识符——将消息分组到对话中 |
    | **角色** | 是 | `user`, `assistant`, 或 `system` |
    | **内容** | 是 | 要存储的消息文本 |
  </Tab>
  <Tab>
    检索存储在特定记忆 ID 下的所有消息。

    | 字段 | 必填 | 描述 |
    |---|---|---|
    | **ID** | 是 | 要检索的记忆标识符 |
  </Tab>
  <Tab>
    返回当前工作流最多 50 条记忆记录。该块不提供分页，因此不能用来导出完整历史。
  </Tab>
  <Tab>
    按 ID 删除记忆条目。

    | 字段 | 必填 | 描述 |
    |---|---|---|
    | **ID** | 是 | 要删除的记忆标识符 |
  </Tab>
</Tabs>

## 配置

<ShowcaseCard>
  <BlockConfigPreview
    name="记忆"
    type="memory"
    color="#F64F9E"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "placeholder": "选择操作",
            "options": [
                { "label": "添加记忆", "id": "add" },
                { "label": "获取所有记忆", "id": "getAll" },
                { "label": "获取记忆", "id": "get" },
                { "label": "删除记忆", "id": "delete" }
            ]
        },
        {
            "id": "id",
            "type": "short-input",
            "title": "ID",
            "layout": "full",
            "placeholder": "输入记忆标识符",
            "required": true,
            "condition": { "field": "operation", "value": "add" }
        },
        {
            "id": "role",
            "type": "dropdown",
            "title": "角色",
            "layout": "full",
            "placeholder": "选择代理角色",
            "required": true,
            "options": [
                { "label": "用户", "id": "user" },
                { "label": "助手", "id": "assistant" },
                { "label": "系统", "id": "system" }
            ],
            "condition": { "field": "operation", "value": "add" }
        },
        {
            "id": "content",
            "type": "short-input",
            "title": "内容",
            "layout": "full",
            "placeholder": "输入消息内容",
            "required": true,
            "condition": { "field": "operation", "value": "add" }
        }
    ]}
  />
</ShowcaseCard>

## 输出

| 操作 | 输出 |
|---|---|
| Get Memory | `memories`：所选 ID 的消息数组；`message`：状态文本 |
| Get All Memories | `memories`：最多 50 条 `{ key, type, data }` 记录；`message`：状态文本 |
| Delete Memory | `message`：删除确认 |
| Add Memory | 当前失败；请参阅上述限制 |

Get All Memories 不返回一个扁平消息数组。将对话用作 Agent 记忆前，请先选择所需记录的 `data`。输出顶层不包含单独的 `id`。

## 最佳实践

- **使用一致的 ID** -- 记忆 ID 是将消息分组为对话的键。使用稳定的标识符（例如用户 ID 或会话 ID），以便记忆在多次运行中正确累积。
- **需要时清理** -- 使用删除操作清除陈旧的对话，并防止记忆无限增长。
