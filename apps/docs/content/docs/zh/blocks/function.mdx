---
title: 函数
---

import { Callout } from 'fumadocs-ui/components/callout'
import { Step, Steps } from 'fumadocs-ui/components/steps'
import { Tab, Tabs } from 'fumadocs-ui/components/tabs'
import { BlockConfigPreview } from '@/components/ui/block-config-preview'
import { ShowcaseCard } from '@/components/ui/showcase-card'

函数模块允许您在工作流中执行自定义 JavaScript 或 TypeScript 代码。使用它可以转换数据、执行计算，或实现其他模块中不具备的自定义逻辑。

<ShowcaseCard>
  <BlockConfigPreview
    name="Function"
    type="function"
    color="#FF402F"
    hideHeader
    subBlocks={[
      { "id": "code", "type": "code", "layout": "full" }
    ]}
  />
</ShowcaseCard>

## 概述

函数模块支持您：

<Steps>
  <Step>
    <strong>转换数据</strong>：转换格式、解析文本、操作数组和对象
  </Step>
  <Step>
    <strong>执行计算</strong>：数学运算、统计分析、财务计算
  </Step>
  <Step>
    <strong>实现自定义逻辑</strong>：复杂条件判断、循环及算法
  </Step>
  <Step>
    <strong>处理外部数据</strong>：解析响应、格式化请求、处理身份验证
  </Step>
</Steps>

## 工作原理

函数模块在安全隔离的环境中运行您的代码：

1. **接收输入**：通过 `input` 对象访问前置模块的数据
2. **执行代码**：运行您的 JavaScript/Python 代码
3. **返回结果**：使用 `return` 将数据传递至下一个模块
4. **处理错误**：内置错误处理与日志记录

## 远程执行（E2B）

  - **支持语言**：在隔离的 E2B 沙箱中运行 JavaScript 和 Python。
  - **启用方式**：在函数模块中切换"远程代码执行"选项。
  - **适用场景**：较重的逻辑处理、外部库依赖或 Python 专属代码。
  - **性能**：由于沙箱启动和网络开销，速度慢于本地 JS。
  - **注意事项**：本地运行时需要 `E2B_API_KEY`。如需最低延迟，请使用原生本地 JS（快速模式）。

## 输入与输出

<Tabs items={['配置', '变量']}>
  <Tab>
    <ul className="list-disc space-y-2 pl-6">
      <li>
        <strong>代码</strong>：要执行的 JavaScript/Python 代码
      </li>
      <li>
        <strong>超时时间</strong>：最大执行时长（默认为 30 秒）
      </li>
      <li>
        <strong>输入数据</strong>：所有已连接模块的输出，均可通过变量访问
      </li>
    </ul>
  </Tab>
  <Tab>
    <ul className="list-disc space-y-2 pl-6">
      <li>
        <strong>function.result</strong>：函数返回的值
      </li>
      <li>
        <strong>function.stdout</strong>：代码中 Console.log() 的输出内容
      </li>
    </ul>
  </Tab>
</Tabs>

## 示例使用场景

### 数据处理流水线

<div className="mb-4 rounded-md border p-4">
  <h4 className="font-medium">场景：将 API 响应转换为结构化数据</h4>
  <ol className="list-decimal pl-5 text-sm">
    <li>API 模块获取原始客户数据</li>
    <li>函数模块对数据进行处理和验证</li>
    <li>函数模块计算衍生指标</li>
    <li>响应模块返回格式化结果</li>
  </ol>
</div>

### 业务逻辑实现

<div className="mb-4 rounded-md border p-4">
  <h4 className="font-medium">场景：计算忠诚度评分与等级</h4>
  <ol className="list-decimal pl-5 text-sm">
    <li>Agent 检索客户购买历史记录</li>
    <li>函数模块计算忠诚度指标</li>
    <li>函数模块确定客户等级</li>
    <li>条件模块根据等级进行路由分发</li>
  </ol>
</div>

### 数据验证与清洗

<div className="mb-4 rounded-md border p-4">
  <h4 className="font-medium">场景：验证并清洗用户输入</h4>
  <ol className="list-decimal pl-5 text-sm">
    <li>接收来自表单提交的用户输入</li>
    <li>函数模块验证电子邮件格式与电话号码</li>
    <li>函数模块对数据进行清洗与规范化</li>
    <li>API 模块将已验证的数据保存至数据库</li>
  </ol>
</div>

### 示例：忠诚度评分计算器

```javascript title="loyalty-calculator.js"
// Process customer data and calculate loyalty score
const { purchaseHistory, accountAge, supportTickets } = <agent>;

// Calculate metrics
const totalSpent = purchaseHistory.reduce((sum, purchase) => sum + purchase.amount, 0);
const purchaseFrequency = purchaseHistory.length / (accountAge / 365);
const ticketRatio = supportTickets.resolved / supportTickets.total;

// Calculate loyalty score (0-100)
const spendScore = Math.min(totalSpent / 1000 * 30, 30);
const frequencyScore = Math.min(purchaseFrequency * 20, 40);
const supportScore = ticketRatio * 30;

const loyaltyScore = Math.round(spendScore + frequencyScore + supportScore);

return {
  customer: <agent.name>,
  loyaltyScore,
  loyaltyTier: loyaltyScore >= 80 ? "Platinum" : loyaltyScore >= 60 ? "Gold" : "Silver",
  metrics: { spendScore, frequencyScore, supportScore }
};
```

## 最佳实践

- **保持函数职责单一**：编写专注于单一功能的函数，以提升可维护性和调试效率
- **优雅地处理错误**：使用 try/catch 块捕获潜在错误，并提供有意义的错误提示信息
- **测试边界情况**：确保代码能正确处理异常输入、空值及边界条件
- **优化性能**：针对大型数据集，注意控制计算复杂度与内存占用
- **使用 console.log() 进行调试**：充分利用标准输出对函数执行过程进行调试与监控
