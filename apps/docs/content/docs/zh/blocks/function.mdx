---
title: 函数
description: 运行自定义 JavaScript/TypeScript 代码
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { Callout } from 'fumadocs-ui/components/callout'
import { Step, Steps } from 'fumadocs-ui/components/steps'
import { Tab, Tabs } from 'fumadocs-ui/components/tabs'
import { BlockConfigPreview } from '@/components/ui/block-config-preview'
import { ShowcaseCard } from '@/components/ui/showcase-card'

<BlockInfoCard
  type="function"
  color="#FF402F"
/>

函数块让你在工作流中执行自定义 JavaScript 或 TypeScript 代码。用它来转换数据、执行计算，或实现其他块中没有的自定义逻辑。

<ShowcaseCard>
  <BlockConfigPreview
    name="Function"
    type="function"
    color="#FF402F"
    hideHeader
    subBlocks={[
      { "id": "code", "type": "code", "layout": "full" }
    ]}
  />
</ShowcaseCard>

## 概述

函数块让你可以：

<Steps>
  <Step>
    <strong>转换数据</strong>：转换格式、解析文本、操作数组和对象
  </Step>
  <Step>
    <strong>执行计算</strong>：数学运算、统计、财务计算
  </Step>
  <Step>
    <strong>实现自定义逻辑</strong>：复杂条件、循环和算法
  </Step>
  <Step>
    <strong>处理外部数据</strong>：解析响应、格式化请求、处理身份验证
  </Step>
</Steps>

## 工作原理

函数块在安全隔离的环境中运行你的代码：

1. **接收输入**：通过 `input` 对象访问上一个块的数据
2. **执行代码**：运行你的 JavaScript/Python 代码
3. **返回结果**：使用 `return` 将数据传递给下一个块
4. **处理错误**：内置错误处理和日志记录

## 远程执行（E2B）

  - **语言**：在隔离的 E2B 沙盒中运行 JavaScript 和 Python。
  - **如何启用**：在函数块中切换“Remote Code Execution”。
  - **何时使用**：更复杂的逻辑、外部库或 Python 专用代码。
  - **性能**：由于沙盒启动和网络开销，比本地 JS 慢。
  - **注意事项**：如果在本地运行，需要 `E2B_API_KEY`。为获得最低延迟，请使用原生本地 JS（Fast Mode）。

## 输入与输出

<Tabs items={['配置', '变量']}>
  <Tab>
    <ul className="list-disc space-y-2 pl-6">
      <li>
        <strong>代码</strong>：要执行的 JavaScript/Python 代码
      </li>
      <li>
        <strong>超时</strong>：最长执行时间（默认 30 秒）
      </li>
      <li>
        <strong>输入数据</strong>：所有已连接块的输出，可通过变量访问
      </li>
    </ul>
  </Tab>
  <Tab>
    <ul className="list-disc space-y-2 pl-6">
      <li>
        <strong>function.result</strong>：函数返回的值
      </li>
      <li>
        <strong>function.stdout</strong>：代码的 Console.log() 输出
      </li>
    </ul>
  </Tab>
</Tabs>

## 示例用例

### 数据处理流水线

<div className="mb-4 rounded-md border p-4">
  <h4 className="font-medium">场景：将 API 响应转换为结构化数据</h4>
  <ol className="list-decimal pl-5 text-sm">
    <li>API 块获取原始客户数据</li>
    <li>函数块处理并校验数据</li>
    <li>函数块计算派生指标</li>
    <li>响应块返回格式化后的结果</li>
  </ol>
</div>

### 业务逻辑实现

<div className="mb-4 rounded-md border p-4">
  <h4 className="font-medium">场景：计算忠诚度评分与等级</h4>
  <ol className="list-decimal pl-5 text-sm">
    <li>Agent 获取客户购买历史</li>
    <li>函数块计算忠诚度指标</li>
    <li>函数块判定客户等级</li>
    <li>条件块根据等级进行路由</li>
  </ol>
</div>

### 数据校验与清理

<div className="mb-4 rounded-md border p-4">
  <h4 className="font-medium">场景：校验并清理用户输入</h4>
  <ol className="list-decimal pl-5 text-sm">
    <li>从表单提交接收用户输入</li>
    <li>函数块校验邮箱格式与电话号码</li>
    <li>函数块清理并规范化数据</li>
    <li>API 块将校验后的数据保存到数据库</li>
  </ol>
</div>

### 示例：忠诚度评分计算器

```javascript title="loyalty-calculator.js"
// Process customer data and calculate loyalty score
const { purchaseHistory, accountAge, supportTickets } = <agent>;

// Calculate metrics
const totalSpent = purchaseHistory.reduce((sum, purchase) => sum + purchase.amount, 0);
const purchaseFrequency = purchaseHistory.length / (accountAge / 365);
const ticketRatio = supportTickets.resolved / supportTickets.total;

// Calculate loyalty score (0-100)
const spendScore = Math.min(totalSpent / 1000 * 30, 30);
const frequencyScore = Math.min(purchaseFrequency * 20, 40);
const supportScore = ticketRatio * 30;

const loyaltyScore = Math.round(spendScore + frequencyScore + supportScore);

return {
  customer: <agent.name>,
  loyaltyScore,
  loyaltyTier: loyaltyScore >= 80 ? "Platinum" : loyaltyScore >= 60 ? "Gold" : "Silver",
  metrics: { spendScore, frequencyScore, supportScore }
};
```

## 最佳实践

- **保持函数职责单一**：编写只做好一件事的函数，以提升可维护性和可调试性
- **妥善处理错误**：使用 try/catch 处理潜在错误，并给出有意义的错误信息
- **测试边界情况**：确保代码能正确处理异常输入、null 值和边界条件
- **优化性能**：注意计算复杂度和大数据集下的内存占用
- **使用 console.log() 调试**：利用 stdout 输出调试并监控函数执行
