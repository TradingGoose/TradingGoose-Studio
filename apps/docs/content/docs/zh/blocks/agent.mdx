---
title: Agent
description: 使用 AI 模型和工具集成构建智能体
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { Callout } from 'fumadocs-ui/components/callout'
import { Step, Steps } from 'fumadocs-ui/components/steps'
import { Tab, Tabs } from 'fumadocs-ui/components/tabs'
import { BlockConfigPreview } from '@/components/ui/block-config-preview'
import { ShowcaseCard } from '@/components/ui/showcase-card'

<BlockInfoCard
  type="agent"
  color="#2873f6"
/>

Agent 模块充当您的工作流与大型语言模型 (LLMs) 之间的接口。它针对各种 AI 提供商执行推理请求，根据定义的指令处理自然语言输入，并生成结构化或非结构化的输出供下游使用。

<ShowcaseCard>
  <BlockConfigPreview
    name="Agent"
    type="agent"
    color="#2873f6"
    hideHeader
    subBlocks={[
      { "id": "systemPrompt", "title": "系统 prompt", "type": "long-input", "layout": "full", "placeholder": "输入系统 prompt..." },
      { "id": "userPrompt", "title": "用户 prompt", "type": "long-input", "layout": "full", "placeholder": "输入上下文或用户消息..." },
      { "id": "memories", "title": "记忆", "type": "short-input", "layout": "full", "placeholder": "连接 Memory block 的输出..." },
      { "id": "model", "title": "模型", "type": "combobox", "layout": "half", "placeholder": "输入或选择模型...", "required": true },
      { "id": "temperature", "title": "温度", "type": "slider", "layout": "half", "defaultValue": 0.5 },
      { "id": "reasoningEffort", "title": "推理强度", "type": "dropdown", "layout": "half", "placeholder": "选择推理强度...", "options": [{ "label": "minimal", "id": "minimal" }, { "label": "low", "id": "low" }, { "label": "medium", "id": "medium" }, { "label": "high", "id": "high" }] },
      { "id": "verbosity", "title": "详细程度", "type": "dropdown", "layout": "half", "placeholder": "选择详细程度...", "options": [{ "label": "low", "id": "low" }, { "label": "medium", "id": "medium" }, { "label": "high", "id": "high" }] },
      { "id": "apiKey", "title": "API 密钥", "type": "short-input", "layout": "full", "placeholder": "输入你的 API 密钥", "password": true, "required": true },
      { "id": "azureEndpoint", "title": "Azure OpenAI 端点", "type": "short-input", "layout": "full", "password": true, "placeholder": "https://your-resource.openai.azure.com" },
      { "id": "azureApiVersion", "title": "Azure API 版本", "type": "short-input", "layout": "full", "placeholder": "2024-07-01-preview" },
      { "id": "tools", "title": "工具", "type": "tool-input", "layout": "full", "defaultValue": [] },
      { "id": "skills", "title": "技能", "type": "skill-input", "layout": "full", "defaultValue": [] },
      { "id": "responseFormat", "title": "响应格式", "type": "code", "layout": "full", "placeholder": "输入 JSON schema..." }
    ]}
  />
</ShowcaseCard>

## 概述

Agent 模块使您能够：

<Steps>
  <Step>
    <strong>处理自然语言</strong>：分析用户输入并生成符合上下文的响应
  </Step>
  <Step>
    <strong>执行 AI 任务</strong>：分析和生成内容，并作出决策
  </Step>
  <Step>
    <strong>调用外部工具</strong>：在处理过程中访问 API、数据库和服务
  </Step>
  <Step>
    <strong>生成结构化输出</strong>：返回符合 schema 要求的 JSON 数据
  </Step>
</Steps> 

## 配置选项

### 系统提示

系统提示建立了 Agent 的操作参数和行为约束。此配置定义了 Agent 的角色、响应方法和所有传入请求的处理边界。

```markdown
You are a helpful assistant that specializes in financial analysis.
Always provide clear explanations and cite sources when possible.
When responding to questions about investments, include risk disclaimers.
```

### 用户提示

用户提示是推理处理的主要输入数据。此参数接受自然语言文本或结构化数据，Agent 将对其进行分析并作出响应。输入来源包括：

- **静态配置**：在模块配置中指定的直接文本输入
- **动态输入**：通过连接接口从上游模块传递的数据
- **运行时生成**：在工作流执行期间以编程方式生成的内容

### 模型选择

Agent 模块通过统一的推理接口支持多个 LLM 提供商。可用模型包括：

**OpenAI 模型**：GPT-5、GPT-4o、o1、o3、o4-mini、gpt-4.1（基于 API 的推理）
**Anthropic 模型**：Claude 3.7 Sonnet（基于 API 的推理）
**Google 模型**：Gemini 2.5 Pro、Gemini 2.0 Flash（基于 API 的推理）
**其他提供商**：Groq、Cerebras、xAI、DeepSeek（基于 API 的推理）
**本地部署**：兼容 Ollama 的模型（自托管推理）

### 温度

控制响应的创造性和随机性：

<Tabs items={['低 (0-0.3)', '中 (0.3-0.7)', '高 (0.7-2.0)']}>
  <Tab>
响应更确定、更专注。最适合事实性任务、客户支持以及
准确性至关重要的场景。
  </Tab>
  <Tab>
兼顾创造力和专注度。适合既要求准确性又需要一定
创造力的通用应用。
  </Tab>
  <Tab>
响应更具创造力且更加多样。非常适合创意写作、头脑风暴以及生成
多元想法。
  </Tab>
</Tabs>

<div className="mt-4 text-sm text-gray-600 dark:text-gray-400">
温度范围（0-1 或 0-2）因所选模型而异。
</div>

### API 密钥

您所选 LLM 提供商的 API 密钥。此密钥会被安全存储并用于身份验证。

### 工具

工具通过外部 API 集成和服务连接扩展 Agent 的能力。工具系统支持函数调用，使 Agent 能够执行文本生成以外的操作。

**工具集成流程**：
1. 打开 Agent block 中的工具配置部分
2. 从 60 多种预构建集成中选择，或定义自定义函数
3. 配置身份验证参数和操作约束

**可用工具类别**：
- **通信**：Gmail、Slack、Telegram、WhatsApp、Microsoft Teams
- **数据源**：Notion、Google Sheets、Airtable、Supabase、Pinecone
- **网络服务**：Firecrawl、Google Search、Exa AI、浏览器自动化
- **开发**：GitHub、Jira、Linear 仓库和问题管理
- **AI 服务**：OpenAI、Perplexity、Hugging Face、ElevenLabs

**工具执行控制**：
- **自动**：模型根据上下文和必要性决定是否调用工具
- **必需**：每次推理请求都必须调用工具
- **无**：工具定义可用，但不会包含在模型上下文中

### 响应格式

响应格式参数通过 JSON Schema 验证强制生成结构化输出。这确保了符合预定义数据结构的一致、机器可读的响应：

```json
{
  "name": "user_analysis",
  "schema": {
    "type": "object",
    "properties": {
      "sentiment": {
        "type": "string",
        "enum": ["positive", "negative", "neutral"]
      },
      "confidence": {
        "type": "number",
        "minimum": 0,
        "maximum": 1
      }
    },
    "required": ["sentiment", "confidence"]
  }
}
```

此配置限制模型的输出以符合指定的模式，防止生成自由格式的文本响应，并确保生成结构化数据。

### 访问结果

Agent 完成后，你可以访问其输出：

- **`<agent.content>`**：Agent 的响应文本或结构化数据
- **`<agent.tokens>`**：token 使用情况统计（prompt、completion、总计）
- **`<agent.tool_calls>`**：Agent 在执行过程中使用的工具详情
- **`<agent.cost>`**：API 调用的估计成本（如果可用）

## 高级功能

### Memory + Agent：对话历史

使用一个 `Memory` block 和固定的 `id`（例如 `chat`）在多次运行之间持久保存消息，并将历史记录包含在 Agent 的 prompt 中。

- 在 Agent 之前添加用户消息
- 读取对话历史以获取上下文
- Agent 运行后追加其回复

有关详细信息，请参阅 [`Memory`](/blocks/memory) 块参考。

## 输入和输出

<Tabs items={['配置', '变量', '结果']}>
  <Tab>
    <ul className="list-disc space-y-2 pl-6">
      <li>
        <strong>系统 prompt</strong>：定义 Agent 行为和角色的指令
      </li>
      <li>
        <strong>用户 prompt</strong>：要处理的输入文本或数据
      </li>
      <li>
        <strong>模型</strong>：选择 AI 模型（OpenAI、Anthropic、Google 等）
      </li>
      <li>
        <strong>温度</strong>：控制响应的随机性（0-2）
      </li>
      <li>
        <strong>工具</strong>：可用于函数调用的工具数组
      </li>
      <li>
        <strong>响应格式</strong>：用于结构化输出的 JSON Schema
      </li>
    </ul>
  </Tab>
  <Tab>
    <ul className="list-disc space-y-2 pl-6">
      <li>
        <strong>agent.content</strong>：Agent 的响应文本或结构化数据
      </li>
      <li>
        <strong>agent.tokens</strong>：token 使用情况统计对象
      </li>
      <li>
        <strong>agent.tool_calls</strong>：工具执行详情数组
      </li>
      <li>
        <strong>agent.cost</strong>：API 调用的估算成本（如有）
      </li>
    </ul>
  </Tab>
  <Tab>
    <ul className="list-disc space-y-2 pl-6">
      <li>
        <strong>内容</strong>：Agent 的主要响应输出
      </li>
      <li>
        <strong>元数据</strong>：使用情况统计和执行详情
      </li>
      <li>
        <strong>访问</strong>：可在 Agent 后续的 block 中使用
      </li>
    </ul>
  </Tab>
</Tabs>

## 示例用例

### 客户支持自动化

<div className="mb-4 rounded-md border p-4">
  <h4 className="font-medium">场景：通过数据库访问处理客户咨询</h4>
  <ol className="list-decimal pl-5 text-sm">
    <li>用户通过 API block 提交支持工单</li>
    <li>Agent 在 Postgres 中检查订单和订阅，并在知识库中查找处理指南</li>
    <li>如果需要升级处理，Agent 会在 Linear 中创建包含相关上下文的 issue</li>
    <li>Agent 起草清晰的电子邮件回复</li>
    <li>Gmail 将回复发送给客户</li>
    <li>对话保存到 Memory 中，以便为后续消息保留历史记录</li>
  </ol>
</div>

### 多模型内容分析

<div className="mb-4 rounded-md border p-4">
  <h4 className="font-medium">场景：使用不同的 AI 模型分析内容</h4>
  <ol className="list-decimal pl-5 text-sm">
    <li>Function block 处理上传的文档</li>
    <li>使用 GPT-4o 的 Agent 执行技术分析</li>
    <li>使用 Claude 的 Agent 分析情感和语气</li>
    <li>Function block 汇总结果并生成最终报告</li>
  </ol>
</div>

### 工具驱动的研究助手

<div className="mb-4 rounded-md border p-4">
  <h4 className="font-medium">场景：能够搜索网页和访问文档的研究助手</h4>
  <ol className="list-decimal pl-5 text-sm">
    <li>通过输入接收用户查询</li>
    <li>Agent 使用 Google Search 工具搜索网页</li>
    <li>Agent 访问 Notion 数据库以查阅内部文档</li>
    <li>Agent 编写完整的研究报告</li>
  </ol>
</div>

## 最佳实践

- **提供具体的系统提示词**：清晰定义 Agent 的角色、语气和限制。指令越具体，Agent 就越能达到预期目的。
- **选择合适的温度设置**：当准确性很重要时，使用较低的温度设置（0-0.3）；当需要更具创意或多样化的响应时，可将温度提高（0.7-2.0）。
- **有效使用工具**：集成符合 Agent 用途并能增强其能力的工具。谨慎选择提供的工具，避免给 Agent 增加不必要的负担。对于重叠较少的任务，请使用另一个 Agent block。
