---
title: Telegram
description: 与 Telegram 交互
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"

<BlockInfoCard
  type="telegram"
  color=""
  icon={true}
  iconSvg={``}
/>

## 配置

### 发送消息

<ShowcaseCard>
  <BlockConfigPreview
    name="Telegram"
    type="telegram"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "发送消息",
                    "id": "telegram_message"
                },
                {
                    "label": "发送图片",
                    "id": "telegram_send_photo"
                },
                {
                    "label": "发送视频",
                    "id": "telegram_send_video"
                },
                {
                    "label": "发送音频",
                    "id": "telegram_send_audio"
                },
                {
                    "label": "发送动画",
                    "id": "telegram_send_animation"
                },
                {
                    "label": "发送文档",
                    "id": "telegram_send_document"
                },
                {
                    "label": "删除消息",
                    "id": "telegram_delete_message"
                }
            ],
            "defaultValue": "telegram_message"
        },
        {
            "id": "botToken",
            "type": "short-input",
            "title": "Bot Token",
            "layout": "full",
            "placeholder": "请输入您的 Telegram Bot Token",
            "password": true,
            "required": true
        },
        {
            "id": "chatId",
            "type": "short-input",
            "title": "Chat ID",
            "layout": "full",
            "placeholder": "请输入 Telegram Chat ID",
            "required": true
        },
        {
            "id": "text",
            "type": "long-input",
            "title": "消息",
            "layout": "full",
            "placeholder": "请输入要发送的消息内容",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">发送消息 (<code>telegram_message</code>)</div>

通过 Telegram Bot API 向 Telegram 频道或用户发送消息，支持直接通信与通知推送，并提供消息追踪与聊天确认功能。

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `botToken` | string | 是 | 您的 Telegram Bot API Token |
| `chatId` | string | 是 | 目标 Telegram 聊天 ID |
| `text` | string | 是 | 要发送的消息文本 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 成功或错误消息 |
| `message_id` | number | Telegram 消息唯一标识符 |
| `id` | number | 聊天 ID |
| `is_bot` | boolean | 该聊天是否为机器人 |
| `first_name` | string | 聊天用户名 \(如有\) |

</div>

---

### 发送图片

<ShowcaseCard>
  <BlockConfigPreview
    name="Telegram"
    type="telegram"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "发送消息",
                    "id": "telegram_message"
                },
                {
                    "label": "发送图片",
                    "id": "telegram_send_photo"
                },
                {
                    "label": "发送视频",
                    "id": "telegram_send_video"
                },
                {
                    "label": "发送音频",
                    "id": "telegram_send_audio"
                },
                {
                    "label": "发送动画",
                    "id": "telegram_send_animation"
                },
                {
                    "label": "发送文档",
                    "id": "telegram_send_document"
                },
                {
                    "label": "删除消息",
                    "id": "telegram_delete_message"
                }
            ],
            "defaultValue": "telegram_send_photo"
        },
        {
            "id": "botToken",
            "type": "short-input",
            "title": "Bot Token",
            "layout": "full",
            "placeholder": "请输入您的 Telegram Bot Token",
            "password": true,
            "required": true
        },
        {
            "id": "chatId",
            "type": "short-input",
            "title": "Chat ID",
            "layout": "full",
            "placeholder": "请输入 Telegram Chat ID",
            "required": true
        },
        {
            "id": "photo",
            "type": "short-input",
            "title": "图片",
            "layout": "full",
            "placeholder": "请输入图片 URL 或 file_id",
            "description": "要发送的图片，请提供 file_id 或 HTTP URL",
            "required": true
        },
        {
            "id": "caption",
            "type": "long-input",
            "title": "说明文字",
            "layout": "full",
            "placeholder": "请输入可选的说明文字",
            "description": "媒体说明文字（可选）"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">发送图片（<code>telegram_send_photo</code>）</div>

通过 Telegram Bot API 向 Telegram 频道或用户发送图片。

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `botToken` | string | 是 | 您的 Telegram Bot API Token |
| `chatId` | string | 是 | 目标 Telegram 聊天 ID |
| `photo` | string | 是 | 要发送的图片，传入 file_id 或 HTTP URL |
| `caption` | string | 否 | 图片说明文字 \(可选\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 成功或错误消息 |
| `message_id` | number | Telegram 消息唯一标识符 |
| `id` | number | 聊天 ID |
| `is_bot` | boolean | 该聊天是否为机器人 |
| `first_name` | string | 聊天用户名 \(如有\) |

</div>

---

### 发送视频

<ShowcaseCard>
  <BlockConfigPreview
    name="Telegram"
    type="telegram"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "发送消息",
                    "id": "telegram_message"
                },
                {
                    "label": "发送图片",
                    "id": "telegram_send_photo"
                },
                {
                    "label": "发送视频",
                    "id": "telegram_send_video"
                },
                {
                    "label": "发送音频",
                    "id": "telegram_send_audio"
                },
                {
                    "label": "发送动画",
                    "id": "telegram_send_animation"
                },
                {
                    "label": "发送文档",
                    "id": "telegram_send_document"
                },
                {
                    "label": "删除消息",
                    "id": "telegram_delete_message"
                }
            ],
            "defaultValue": "telegram_send_video"
        },
        {
            "id": "botToken",
            "type": "short-input",
            "title": "Bot Token",
            "layout": "full",
            "placeholder": "请输入您的 Telegram Bot Token",
            "password": true,
            "required": true
        },
        {
            "id": "chatId",
            "type": "short-input",
            "title": "Chat ID",
            "layout": "full",
            "placeholder": "请输入 Telegram Chat ID",
            "required": true
        },
        {
            "id": "video",
            "type": "short-input",
            "title": "视频",
            "layout": "full",
            "placeholder": "请输入视频 URL 或 file_id",
            "description": "要发送的视频，请传入 file_id 或 HTTP URL",
            "required": true
        },
        {
            "id": "caption",
            "type": "long-input",
            "title": "说明文字",
            "layout": "full",
            "placeholder": "请输入可选的说明文字",
            "description": "媒体说明文字（可选）"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">发送视频 (<code>telegram_send_video</code>)</div>

通过 Telegram Bot API 向 Telegram 频道或用户发送视频。

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `botToken` | string | 是 | 您的 Telegram Bot API Token |
| `chatId` | string | 是 | 目标 Telegram 聊天 ID |
| `video` | string | 是 | 要发送的视频，传入 file_id 或 HTTP URL |
| `caption` | string | 否 | 视频说明文字 \(可选\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 成功或错误信息 |
| `message_id` | number | Telegram 消息唯一标识符 |
| `id` | number | 发送者 ID |
| `is_bot` | boolean | 聊天对象是否为机器人 |
| `first_name` | string | 发送者 |

</div>

---

### 发送音频

<ShowcaseCard>
  <BlockConfigPreview
    name="Telegram"
    type="telegram"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "发送消息",
                    "id": "telegram_message"
                },
                {
                    "label": "发送图片",
                    "id": "telegram_send_photo"
                },
                {
                    "label": "发送视频",
                    "id": "telegram_send_video"
                },
                {
                    "label": "发送音频",
                    "id": "telegram_send_audio"
                },
                {
                    "label": "发送动画",
                    "id": "telegram_send_animation"
                },
                {
                    "label": "发送文档",
                    "id": "telegram_send_document"
                },
                {
                    "label": "删除消息",
                    "id": "telegram_delete_message"
                }
            ],
            "defaultValue": "telegram_send_audio"
        },
        {
            "id": "botToken",
            "type": "short-input",
            "title": "Bot Token",
            "layout": "full",
            "placeholder": "请输入您的 Telegram Bot Token",
            "password": true,
            "required": true
        },
        {
            "id": "chatId",
            "type": "short-input",
            "title": "Chat ID",
            "layout": "full",
            "placeholder": "请输入 Telegram Chat ID",
            "required": true
        },
        {
            "id": "audio",
            "type": "short-input",
            "title": "音频",
            "layout": "full",
            "placeholder": "请输入音频 URL 或 file_id",
            "description": "要发送的音频文件，请传入 file_id 或 HTTP URL",
            "required": true
        },
        {
            "id": "caption",
            "type": "long-input",
            "title": "说明文字",
            "layout": "full",
            "placeholder": "请输入可选的说明文字",
            "description": "媒体说明文字（可选）"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">发送音频 (<code>telegram_send_audio</code>)</div>

通过 Telegram Bot API 向 Telegram 频道或用户发送音频文件。

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `botToken` | string | 是 | 您的 Telegram Bot API Token |
| `chatId` | string | 是 | 目标 Telegram 聊天 ID |
| `audio` | string | 是 | 要发送的音频文件，传入 file_id 或 HTTP URL |
| `caption` | string | 否 | 音频说明文字 \(可选\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 成功或错误消息 |
| `message_id` | number | Telegram 消息唯一标识符 |
| `id` | number | 发送者 ID |
| `is_bot` | boolean | 该聊天是否为机器人 |
| `first_name` | string | 发送者 |

</div>

---

### 发送动画

<ShowcaseCard>
  <BlockConfigPreview
    name="Telegram"
    type="telegram"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "发送消息",
                    "id": "telegram_message"
                },
                {
                    "label": "发送图片",
                    "id": "telegram_send_photo"
                },
                {
                    "label": "发送视频",
                    "id": "telegram_send_video"
                },
                {
                    "label": "发送音频",
                    "id": "telegram_send_audio"
                },
                {
                    "label": "发送动画",
                    "id": "telegram_send_animation"
                },
                {
                    "label": "发送文档",
                    "id": "telegram_send_document"
                },
                {
                    "label": "删除消息",
                    "id": "telegram_delete_message"
                }
            ],
            "defaultValue": "telegram_send_animation"
        },
        {
            "id": "botToken",
            "type": "short-input",
            "title": "Bot Token",
            "layout": "full",
            "placeholder": "请输入您的 Telegram Bot Token",
            "password": true,
            "required": true
        },
        {
            "id": "chatId",
            "type": "short-input",
            "title": "Chat ID",
            "layout": "full",
            "placeholder": "请输入 Telegram Chat ID",
            "required": true
        },
        {
            "id": "animation",
            "type": "short-input",
            "title": "动画",
            "layout": "full",
            "placeholder": "请输入动画 URL 或 file_id",
            "description": "要发送的动画（GIF）。请传入 file_id 或 HTTP URL",
            "required": true
        },
        {
            "id": "caption",
            "type": "long-input",
            "title": "说明文字",
            "layout": "full",
            "placeholder": "请输入可选的说明文字",
            "description": "媒体说明文字（可选）"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">发送动画（<code>telegram_send_animation</code>）</div>

通过 Telegram Bot API 向 Telegram 频道或用户发送动画（GIF）。

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `botToken` | string | 是 | 您的 Telegram Bot API Token |
| `chatId` | string | 是 | 目标 Telegram 聊天 ID |
| `animation` | string | 是 | 要发送的动画，传入 file_id 或 HTTP URL |
| `caption` | string | 否 | 动画说明文字（可选）|

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 成功或错误消息 |
| `message_id` | number | Telegram 消息唯一标识符 |
| `id` | number | 发送者 ID |
| `is_bot` | boolean | 该聊天是否为机器人 |
| `first_name` | string | 发送者 |

</div>

---

### 发送文档

<ShowcaseCard>
  <BlockConfigPreview
    name="Telegram"
    type="telegram"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "发送消息",
                    "id": "telegram_message"
                },
                {
                    "label": "发送图片",
                    "id": "telegram_send_photo"
                },
                {
                    "label": "发送视频",
                    "id": "telegram_send_video"
                },
                {
                    "label": "发送音频",
                    "id": "telegram_send_audio"
                },
                {
                    "label": "发送动画",
                    "id": "telegram_send_animation"
                },
                {
                    "label": "发送文档",
                    "id": "telegram_send_document"
                },
                {
                    "label": "删除消息",
                    "id": "telegram_delete_message"
                }
            ],
            "defaultValue": "telegram_send_document"
        },
        {
            "id": "botToken",
            "type": "short-input",
            "title": "Bot Token",
            "layout": "full",
            "placeholder": "请输入您的 Telegram Bot Token",
            "password": true,
            "required": true
        },
        {
            "id": "chatId",
            "type": "short-input",
            "title": "Chat ID",
            "layout": "full",
            "placeholder": "请输入 Telegram Chat ID",
            "required": true
        },
        {
            "id": "attachmentFiles",
            "type": "file-upload",
            "title": "文档",
            "layout": "full",
            "placeholder": "上传文档文件",
            "description": "要发送的文档文件（PDF、ZIP、DOC 等）。最大大小：50MB"
        },
        {
            "id": "files",
            "type": "short-input",
            "title": "文档",
            "layout": "full",
            "placeholder": "引用前一模块中的文档",
            "description": "引用前一模块中的文档文件"
        },
        {
            "id": "caption",
            "type": "long-input",
            "title": "说明文字",
            "layout": "full",
            "placeholder": "请输入可选的说明文字",
            "description": "媒体说明文字（可选）"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">发送文档 (<code>telegram_send_document</code>)</div>

通过 Telegram Bot API 向 Telegram 频道或用户发送文档（PDF、ZIP、DOC 等）。

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `botToken` | string | 是 | 您的 Telegram Bot API Token |
| `chatId` | string | 是 | 目标 Telegram 聊天 ID |
| `files` | file[] | 否 | 要发送的文档文件 \(PDF、ZIP、DOC 等\)。最大大小：50MB |
| `caption` | string | 否 | 文档说明文字 \(可选\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 成功或错误消息 |
| `message_id` | number | Telegram 消息唯一标识符 |
| `id` | number | 发送者 ID |
| `is_bot` | boolean | 该聊天是否为机器人 |
| `first_name` | string | 发送者 |

</div>

---

### 删除消息

<ShowcaseCard>
  <BlockConfigPreview
    name="Telegram"
    type="telegram"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "发送消息",
                    "id": "telegram_message"
                },
                {
                    "label": "发送图片",
                    "id": "telegram_send_photo"
                },
                {
                    "label": "发送视频",
                    "id": "telegram_send_video"
                },
                {
                    "label": "发送音频",
                    "id": "telegram_send_audio"
                },
                {
                    "label": "发送动画",
                    "id": "telegram_send_animation"
                },
                {
                    "label": "发送文档",
                    "id": "telegram_send_document"
                },
                {
                    "label": "删除消息",
                    "id": "telegram_delete_message"
                }
            ],
            "defaultValue": "telegram_delete_message"
        },
        {
            "id": "botToken",
            "type": "short-input",
            "title": "Bot Token",
            "layout": "full",
            "placeholder": "请输入您的 Telegram Bot Token",
            "password": true,
            "required": true
        },
        {
            "id": "chatId",
            "type": "short-input",
            "title": "聊天 ID",
            "layout": "full",
            "placeholder": "请输入 Telegram 聊天 ID",
            "required": true
        },
        {
            "id": "messageId",
            "type": "short-input",
            "title": "消息 ID",
            "layout": "full",
            "placeholder": "请输入要删除的消息 ID",
            "description": "您希望删除的消息的唯一标识符",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">删除消息（<code>telegram_delete_message</code>）</div>

通过 Telegram Bot API 删除 Telegram 频道或聊天中的消息。需要提供待删除消息的消息 ID。

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `botToken` | string | 是 | 您的 Telegram Bot API Token |
| `chatId` | string | 是 | 目标 Telegram 聊天 ID |
| `messageId` | string | 是 | 待删除消息的消息 ID |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 成功或错误消息 |
| `ok` | boolean | API 响应成功状态 |

</div>

---

## 使用说明

将 Telegram 集成到工作流中，可发送和删除消息。可在触发模式下使用，当消息发送至聊天时触发工作流。

## 注意事项

- 类别：`tools`
- 类型：`telegram`
