---
title: Supabase
description: 使用 Supabase 数据库
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"

<BlockInfoCard
  type="supabase"
  color=""
  icon={true}
  iconSvg={``}
/>

## 配置

### 获取多行数据

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "获取多行",
                    "id": "query"
                },
                {
                    "label": "获取单行",
                    "id": "get_row"
                },
                {
                    "label": "创建行",
                    "id": "insert"
                },
                {
                    "label": "更新行",
                    "id": "update"
                },
                {
                    "label": "删除行",
                    "id": "delete"
                },
                {
                    "label": "更新插入行",
                    "id": "upsert"
                },
                {
                    "label": "向量搜索",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "query"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "项目 ID",
            "layout": "full",
            "placeholder": "您的 Supabase 项目 ID（例如：jdrkgepadsdopsntdlom）",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "数据表",
            "layout": "full",
            "placeholder": "数据表名称",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "服务角色密钥",
            "layout": "full",
            "placeholder": "您的 Supabase 服务角色密钥",
            "password": true,
            "required": true
        },
        {
            "id": "filter",
            "type": "short-input",
            "title": "过滤条件（PostgREST 语法）",
            "layout": "full",
            "placeholder": "status=eq.active"
        },
        {
            "id": "orderBy",
            "type": "short-input",
            "title": "排序方式",
            "layout": "full",
            "placeholder": "列名（添加 DESC 表示降序）"
        },
        {
            "id": "limit",
            "type": "short-input",
            "title": "限制条数",
            "layout": "full",
            "placeholder": "100"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取多行数据 (<code>supabase_query</code>)</div>

从 Supabase 表中查询数据

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | 是 | 您的 Supabase 项目 ID \(例如：jdrkgepadsdopsntdlom\) |
| `table` | string | 是 | 要查询的 Supabase 表名称 |
| `filter` | string | 否 | PostgREST 过滤条件 \(例如："id=eq.123"\) |
| `orderBy` | string | 否 | 排序列名 \(添加 DESC 表示降序\) |
| `limit` | number | 否 | 最大返回行数 |
| `apiKey` | string | 是 | 您的 Supabase 服务角色密钥 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 操作状态信息 |

</div>

---

### 获取单行数据

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "获取多行",
                    "id": "query"
                },
                {
                    "label": "获取单行",
                    "id": "get_row"
                },
                {
                    "label": "创建行",
                    "id": "insert"
                },
                {
                    "label": "更新行",
                    "id": "update"
                },
                {
                    "label": "删除行",
                    "id": "delete"
                },
                {
                    "label": "更新插入行",
                    "id": "upsert"
                },
                {
                    "label": "向量搜索",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "get_row"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "项目 ID",
            "layout": "full",
            "placeholder": "您的 Supabase 项目 ID（例如：jdrkgepadsdopsntdlom）",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "数据表",
            "layout": "full",
            "placeholder": "数据表名称",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "服务角色密钥",
            "layout": "full",
            "placeholder": "您的 Supabase 服务角色密钥",
            "password": true,
            "required": true
        },
        {
            "id": "filter",
            "type": "short-input",
            "title": "过滤条件（PostgREST 语法）",
            "layout": "full",
            "placeholder": "id=eq.123",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取单行记录 (<code>supabase_get_row</code>)</div>

根据过滤条件从 Supabase 表中获取单行记录

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | 是 | 您的 Supabase 项目 ID \(例如：jdrkgepadsdopsntdlom\) |
| `table` | string | 是 | 要查询的 Supabase 表名称 |
| `filter` | string | 是 | 用于定位特定行的 PostgREST 过滤条件 \(例如："id=eq.123"\) |
| `apiKey` | string | 是 | 您的 Supabase 服务角色密钥 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 操作状态消息 |

</div>

---

### 创建行记录

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "获取多行",
                    "id": "query"
                },
                {
                    "label": "获取单行",
                    "id": "get_row"
                },
                {
                    "label": "创建行",
                    "id": "insert"
                },
                {
                    "label": "更新行",
                    "id": "update"
                },
                {
                    "label": "删除行",
                    "id": "delete"
                },
                {
                    "label": "更新插入行",
                    "id": "upsert"
                },
                {
                    "label": "向量搜索",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "insert"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "项目 ID",
            "layout": "full",
            "placeholder": "您的 Supabase 项目 ID（例如：jdrkgepadsdopsntdlom）",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "数据表",
            "layout": "full",
            "placeholder": "数据表名称",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "服务角色密钥",
            "layout": "full",
            "placeholder": "您的 Supabase 服务角色密钥",
            "password": true,
            "required": true
        },
        {
            "id": "data",
            "type": "code",
            "title": "数据",
            "layout": "full",
            "placeholder": "{\\n  \"column1\": \"value1\",\\n  \"column2\": \"value2\"\\n}",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">创建行（<code>supabase_insert</code>）</div>

向 Supabase 表中插入数据

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | 是 | 您的 Supabase 项目 ID \(例如：jdrkgepadsdopsntdlom\) |
| `table` | string | 是 | 要插入数据的 Supabase 表名称 |
| `data` | array | 是 | 要插入的数据（对象数组或单个对象） |
| `apiKey` | string | 是 | 您的 Supabase 服务角色密钥 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `message` | string | 操作状态信息 |

</div>

---

### 更新行

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "获取多行",
                    "id": "query"
                },
                {
                    "label": "获取单行",
                    "id": "get_row"
                },
                {
                    "label": "创建行",
                    "id": "insert"
                },
                {
                    "label": "更新行",
                    "id": "update"
                },
                {
                    "label": "删除行",
                    "id": "delete"
                },
                {
                    "label": "更新插入行",
                    "id": "upsert"
                },
                {
                    "label": "向量搜索",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "update"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "项目 ID",
            "layout": "full",
            "placeholder": "您的 Supabase 项目 ID（例如：jdrkgepadsdopsntdlom）",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "数据表",
            "layout": "full",
            "placeholder": "数据表名称",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "服务角色密钥",
            "layout": "full",
            "placeholder": "您的 Supabase 服务角色密钥",
            "password": true,
            "required": true
        },
        {
            "id": "data",
            "type": "code",
            "title": "数据",
            "layout": "full",
            "placeholder": "{\\n  \"column1\": \"value1\",\\n  \"column2\": \"value2\"\\n}",
            "language": "json",
            "required": true
        },
        {
            "id": "filter",
            "type": "short-input",
            "title": "筛选条件（PostgREST 语法）",
            "layout": "full",
            "placeholder": "id=eq.123",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">更新行（<code>supabase_update</code>）</div>

根据筛选条件更新 Supabase 表中的行

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | 是 | 您的 Supabase 项目 ID \(例如：jdrkgepadsdopsntdlom\) |
| `table` | string | 是 | 要更新的 Supabase 表名称 |
| `filter` | string | 是 | 用于定位待更新行的 PostgREST 筛选条件 \(例如："id=eq.123"\) |
| `data` | object | 是 | 要写入匹配行的更新数据 |
| `apiKey` | string | 是 | 您的 Supabase 服务角色密钥 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 操作状态消息 |

</div>

---

### 删除行

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "获取多行",
                    "id": "query"
                },
                {
                    "label": "获取单行",
                    "id": "get_row"
                },
                {
                    "label": "创建行",
                    "id": "insert"
                },
                {
                    "label": "更新行",
                    "id": "update"
                },
                {
                    "label": "删除行",
                    "id": "delete"
                },
                {
                    "label": "Upsert 行",
                    "id": "upsert"
                },
                {
                    "label": "向量搜索",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "delete"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "项目 ID",
            "layout": "full",
            "placeholder": "您的 Supabase 项目 ID（例如：jdrkgepadsdopsntdlom）",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "数据表",
            "layout": "full",
            "placeholder": "数据表名称",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "服务角色密钥",
            "layout": "full",
            "placeholder": "您的 Supabase 服务角色密钥",
            "password": true,
            "required": true
        },
        {
            "id": "filter",
            "type": "short-input",
            "title": "筛选条件（PostgREST 语法）",
            "layout": "full",
            "placeholder": "id=eq.123",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">删除行 (<code>supabase_delete</code>)</div>

根据筛选条件从 Supabase 表中删除行

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | 是 | 您的 Supabase 项目 ID \(例如：jdrkgepadsdopsntdlom\) |
| `table` | string | 是 | 要删除数据的 Supabase 表名称 |
| `filter` | string | 是 | 用于标识要删除行的 PostgREST 过滤条件 \(例如："id=eq.123"\) |
| `apiKey` | string | 是 | 您的 Supabase 服务角色密钥 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 操作状态消息 |

</div>

---

### 更新插入行

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "获取多行",
                    "id": "query"
                },
                {
                    "label": "获取单行",
                    "id": "get_row"
                },
                {
                    "label": "创建行",
                    "id": "insert"
                },
                {
                    "label": "更新行",
                    "id": "update"
                },
                {
                    "label": "删除行",
                    "id": "delete"
                },
                {
                    "label": "Upsert 行",
                    "id": "upsert"
                },
                {
                    "label": "向量搜索",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "upsert"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "项目 ID",
            "layout": "full",
            "placeholder": "您的 Supabase 项目 ID（例如：jdrkgepadsdopsntdlom）",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "数据表",
            "layout": "full",
            "placeholder": "数据表名称",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "服务角色密钥",
            "layout": "full",
            "placeholder": "您的 Supabase 服务角色密钥",
            "password": true,
            "required": true
        },
        {
            "id": "data",
            "type": "code",
            "title": "数据",
            "layout": "full",
            "placeholder": "{\\n  \"column1\": \"value1\",\\n  \"column2\": \"value2\"\\n}",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">插入或更新行（<code>supabase_upsert</code>）</div>

在 Supabase 表中插入或更新数据（upsert 操作）

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | 是 | 您的 Supabase 项目 ID（例如：jdrkgepadsdopsntdlom） |
| `table` | string | 是 | 要执行 upsert 操作的 Supabase 表名称 |
| `data` | array | 是 | 要 upsert（插入或更新）的数据——对象数组或单个对象 |
| `apiKey` | string | 是 | 您的 Supabase 服务角色密钥 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 操作状态消息 |

</div>

---

### 向量搜索

<ShowcaseCard>
  <BlockConfigPreview
    name="Supabase"
    type="supabase"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "获取多行",
                    "id": "query"
                },
                {
                    "label": "获取单行",
                    "id": "get_row"
                },
                {
                    "label": "创建行",
                    "id": "insert"
                },
                {
                    "label": "更新行",
                    "id": "update"
                },
                {
                    "label": "删除行",
                    "id": "delete"
                },
                {
                    "label": "Upsert 行",
                    "id": "upsert"
                },
                {
                    "label": "向量搜索",
                    "id": "vector_search"
                }
            ],
            "defaultValue": "vector_search"
        },
        {
            "id": "projectId",
            "type": "short-input",
            "title": "项目 ID",
            "layout": "full",
            "placeholder": "您的 Supabase 项目 ID（例如：jdrkgepadsdopsntdlom）",
            "password": true,
            "required": true
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "数据表",
            "layout": "full",
            "placeholder": "数据表名称",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "服务角色密钥",
            "layout": "full",
            "placeholder": "您的 Supabase 服务角色密钥",
            "password": true,
            "required": true
        },
        {
            "id": "functionName",
            "type": "short-input",
            "title": "函数名称",
            "layout": "full",
            "placeholder": "match_documents",
            "required": true
        },
        {
            "id": "queryEmbedding",
            "type": "code",
            "title": "查询嵌入向量",
            "layout": "full",
            "placeholder": "[0.1, 0.2, 0.3, ...]",
            "language": "json",
            "required": true
        },
        {
            "id": "matchThreshold",
            "type": "short-input",
            "title": "匹配阈值（可选）",
            "layout": "full",
            "placeholder": "0.78"
        },
        {
            "id": "matchCount",
            "type": "short-input",
            "title": "匹配数量（可选）",
            "layout": "full",
            "placeholder": "10"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">向量搜索（<code>supabase_vector_search</code>）</div>

使用 pgvector 在 Supabase 表中执行相似度搜索

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `projectId` | string | 是 | 您的 Supabase 项目 ID \(例如：jdrkgepadsdopsntdlom\) |
| `functionName` | string | 是 | 执行向量搜索的 PostgreSQL 函数名称 \(例如：match_documents\) |
| `queryEmbedding` | array | 是 | 用于搜索相似项的查询向量/嵌入 |
| `matchThreshold` | number | 否 | 最低相似度阈值 \(0-1\)，通常为 0.7-0.9 |
| `matchCount` | number | 否 | 返回结果的最大数量 \(默认值：10\) |
| `apiKey` | string | 是 | 您的 Supabase 服务角色密钥 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | string | 操作状态信息 |

</div>

---

## 使用说明

将 Supabase 集成到工作流中。支持获取多行数据、获取、创建、更新、删除及更新插入单行数据。

## 注意事项

- 类别：`tools`
- 类型：`supabase`
