---
title: Circleback
description: 由 AI 生成会议记录和行动项
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"
import { Card, Cards } from 'fumadocs-ui/components/card'

<BlockInfoCard
  type="circleback"
  color=""
/>

<Cards>
  <Card title="Use as a Trigger" href="/triggers/circleback">
    See 触发器 events, setup, 和 event payloads 用于 Circleback.
  </Card>
</Cards>


## 配置

### General Webhook

<ShowcaseCard>
  <BlockConfigPreview
    name="Circleback"
    type="circleback"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "selectedTriggerId",
            "type": "dropdown",
            "title": "触发器类型",
            "required": true,
            "options": [
                {
                    "label": "General Webhook",
                    "id": "circleback_webhook"
                },
                {
                    "label": "Meeting Completed",
                    "id": "circleback_meeting_completed"
                },
                {
                    "label": "Meeting Notes Ready",
                    "id": "circleback_meeting_notes"
                }
            ],
            "defaultValue": "circleback_webhook"
        },
        {
            "id": "webhookUrlDisplay",
            "type": "short-input",
            "title": "Webhook URL",
            "placeholder": "Webhook URL 将 be generated"
        },
        {
            "id": "webhookSecret",
            "type": "short-input",
            "title": "Signing Secret",
            "placeholder": "Paste signing secret 从 Circleback (可选)",
            "description": "Validates 该 Webhook deliveries originate 从 Circleback 使用 HMAC-SHA256.",
            "password": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-sm font-medium text-fd-muted-foreground mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `id` | 数字 | Circleback 会议 ID |
| `name` | 字符串 | 会议 title |
| `url` | 字符串 | Virtual 会议 URL \(Zoom, Meet, Teams\) |
| `createdAt` | 字符串 | 会议 creation 时间戳 |
| `duration` | 数字 | 时长在 seconds |
| `recordingUrl` | 字符串 | Recording URL \(有效 24 hours\) |
| `tags` | json | 数组的标签 |
| `icalUid` | 字符串 | 日历 event ID |
| `attendees` | json | 数组的 attendee objects |
| `notes` | 字符串 | 会议备注在 Markdown |
| `actionItems` | json | 数组的 action 项目 |
| `transcript` | json | 数组的转写文本 segments |
| `insights` | json | 用户-created insights |
| `meeting` | json | 完整会议有效负载 |

</div>



---

### 会议已完成

<ShowcaseCard>
  <BlockConfigPreview
    name="Circleback"
    type="circleback"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "selectedTriggerId",
            "type": "dropdown",
            "title": "触发器类型",
            "required": true,
            "options": [
                {
                    "label": "General Webhook",
                    "id": "circleback_webhook"
                },
                {
                    "label": "Meeting Completed",
                    "id": "circleback_meeting_completed"
                },
                {
                    "label": "Meeting Notes Ready",
                    "id": "circleback_meeting_notes"
                }
            ],
            "defaultValue": "circleback_meeting_completed"
        },
        {
            "id": "webhookUrlDisplay",
            "type": "short-input",
            "title": "Webhook URL",
            "placeholder": "Webhook URL 将 be generated"
        },
        {
            "id": "webhookSecret",
            "type": "short-input",
            "title": "Signing Secret",
            "placeholder": "Paste signing secret 从 Circleback (可选)",
            "description": "Validates 该 Webhook deliveries originate 从 Circleback 使用 HMAC-SHA256.",
            "password": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-sm font-medium text-fd-muted-foreground mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `id` | 数字 | Circleback 会议 ID |
| `name` | 字符串 | 会议 title |
| `url` | 字符串 | Virtual 会议 URL \(Zoom, Meet, Teams\) |
| `createdAt` | 字符串 | 会议 creation 时间戳 |
| `duration` | 数字 | 时长在 seconds |
| `recordingUrl` | 字符串 | Recording URL \(有效 24 hours\) |
| `tags` | json | 数组的标签 |
| `icalUid` | 字符串 | 日历 event ID |
| `attendees` | json | 数组的 attendee objects |
| `notes` | 字符串 | 会议备注在 Markdown |
| `actionItems` | json | 数组的 action 项目 |
| `transcript` | json | 数组的转写文本 segments |
| `insights` | json | 用户-created insights |
| `meeting` | json | 完整会议有效负载 |

</div>



---

### 会议备注 Ready

<ShowcaseCard>
  <BlockConfigPreview
    name="Circleback"
    type="circleback"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "selectedTriggerId",
            "type": "dropdown",
            "title": "触发器类型",
            "required": true,
            "options": [
                {
                    "label": "General Webhook",
                    "id": "circleback_webhook"
                },
                {
                    "label": "Meeting Completed",
                    "id": "circleback_meeting_completed"
                },
                {
                    "label": "Meeting Notes Ready",
                    "id": "circleback_meeting_notes"
                }
            ],
            "defaultValue": "circleback_meeting_notes"
        },
        {
            "id": "webhookUrlDisplay",
            "type": "short-input",
            "title": "Webhook URL",
            "placeholder": "Webhook URL 将 be generated"
        },
        {
            "id": "webhookSecret",
            "type": "short-input",
            "title": "Signing Secret",
            "placeholder": "Paste signing secret 从 Circleback (可选)",
            "description": "Validates 该 Webhook deliveries originate 从 Circleback 使用 HMAC-SHA256.",
            "password": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-sm font-medium text-fd-muted-foreground mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `id` | 数字 | Circleback 会议 ID |
| `name` | 字符串 | 会议 title |
| `url` | 字符串 | Virtual 会议 URL \(Zoom, Meet, Teams\) |
| `createdAt` | 字符串 | 会议 creation 时间戳 |
| `duration` | 数字 | 时长在 seconds |
| `recordingUrl` | 字符串 | Recording URL \(有效 24 hours\) |
| `tags` | json | 数组的标签 |
| `icalUid` | 字符串 | 日历 event ID |
| `attendees` | json | 数组的 attendee objects |
| `notes` | 字符串 | 会议备注在 Markdown |
| `actionItems` | json | 数组的 action 项目 |
| `transcript` | json | 数组的转写文本 segments |
| `insights` | json | 用户-created insights |
| `meeting` | json | 完整会议有效负载 |

</div>



---


## 用量说明

会议处理完成后，接收会议纪要、待办事项、转录和录音。Circleback 使用 Webhook 将数据推送到你的工作流。



## 备注

- Category: `tools`
- Type: `circleback`
