---
title: Amazon RDS
description: 通过 Data API 连接 Amazon RDS
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="rds"
  color="#527FFF"
/>


## 配置

### 查询 (SELECT)

<ShowcaseCard>
  <BlockConfigPreview
    name="Amazon RDS"
    type="rds"
    color="#527FFF"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Query (SELECT)",
                    "id": "query"
                },
                {
                    "label": "Insert Data",
                    "id": "insert"
                },
                {
                    "label": "Update Data",
                    "id": "update"
                },
                {
                    "label": "Delete Data",
                    "id": "delete"
                },
                {
                    "label": "Execute Raw SQL",
                    "id": "execute"
                }
            ],
            "defaultValue": "query"
        },
        {
            "id": "region",
            "type": "short-input",
            "title": "AWS 区域",
            "placeholder": "us-east-1",
            "required": true
        },
        {
            "id": "accessKeyId",
            "type": "short-input",
            "title": "AWS 访问键 ID",
            "placeholder": "AKIA...",
            "password": true,
            "required": true
        },
        {
            "id": "secretAccessKey",
            "type": "short-input",
            "title": "AWS 密钥访问键",
            "placeholder": "Your secret access key",
            "password": true,
            "required": true
        },
        {
            "id": "resourceArn",
            "type": "short-input",
            "title": "资源 ARN",
            "placeholder": "arn:aws:rds:us-east-1:123456789:cluster:my-cluster",
            "required": true
        },
        {
            "id": "secretArn",
            "type": "short-input",
            "title": "密钥 ARN",
            "placeholder": "arn:aws:secretsmanager:us-east-1:123456789:secret:my-secret",
            "required": true
        },
        {
            "id": "database",
            "type": "short-input",
            "title": "数据库名称",
            "placeholder": "your_database"
        },
        {
            "id": "query",
            "type": "code",
            "title": "SQL 查询",
            "placeholder": "SELECT * FROM users WHERE active = true",
            "language": "sql",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Query (SELECT) (<code>rds_query</code>)</div>

执行一个 SELECT 查询在 Amazon RDS 使用数据 API

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `region` | 字符串 | 是 | AWS 区域 \(例如： us-east-1\) |
| `accessKeyId` | 字符串 | 是 | AWS 访问键 ID |
| `secretAccessKey` | 字符串 | 是 | AWS 密钥访问键 |
| `resourceArn` | 字符串 | 是 | ARN的Aurora DB cluster |
| `secretArn` | 字符串 | 是 | ARN的Secrets Manager 密钥包含 DB credentials |
| `database` | 字符串 | 否 | 数据库名称 \(可选\) |
| `query` | 字符串 | 是 | SQL SELECT 查询到执行 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作状态消息 |
| `rows` | 数组 | 以下内容的数组：行已返回，来源为查询 |

</div>



---

### Insert 数据

<ShowcaseCard>
  <BlockConfigPreview
    name="Amazon RDS"
    type="rds"
    color="#527FFF"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Query (SELECT)",
                    "id": "query"
                },
                {
                    "label": "Insert Data",
                    "id": "insert"
                },
                {
                    "label": "Update Data",
                    "id": "update"
                },
                {
                    "label": "Delete Data",
                    "id": "delete"
                },
                {
                    "label": "Execute Raw SQL",
                    "id": "execute"
                }
            ],
            "defaultValue": "insert"
        },
        {
            "id": "region",
            "type": "short-input",
            "title": "AWS 区域",
            "placeholder": "us-east-1",
            "required": true
        },
        {
            "id": "accessKeyId",
            "type": "short-input",
            "title": "AWS 访问键 ID",
            "placeholder": "AKIA...",
            "password": true,
            "required": true
        },
        {
            "id": "secretAccessKey",
            "type": "short-input",
            "title": "AWS 密钥访问键",
            "placeholder": "Your secret access key",
            "password": true,
            "required": true
        },
        {
            "id": "resourceArn",
            "type": "short-input",
            "title": "资源 ARN",
            "placeholder": "arn:aws:rds:us-east-1:123456789:cluster:my-cluster",
            "required": true
        },
        {
            "id": "secretArn",
            "type": "short-input",
            "title": "密钥 ARN",
            "placeholder": "arn:aws:secretsmanager:us-east-1:123456789:secret:my-secret",
            "required": true
        },
        {
            "id": "database",
            "type": "short-input",
            "title": "数据库名称",
            "placeholder": "your_database"
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "表名称",
            "placeholder": "users",
            "required": true
        },
        {
            "id": "data",
            "type": "code",
            "title": "数据 (JSON)",
            "placeholder": "{\\n  \"name\": \"John Doe\",\\n  \"email\": \"john@example.com\",\\n  \"active\": true\\n}",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Insert Data (<code>rds_insert</code>)</div>

Insert 数据到 一个 Amazon RDS 表使用数据 API

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `region` | 字符串 | 是 | AWS 区域 \(例如： us-east-1\) |
| `accessKeyId` | 字符串 | 是 | AWS 访问键 ID |
| `secretAccessKey` | 字符串 | 是 | AWS 密钥访问键 |
| `resourceArn` | 字符串 | 是 | ARN的Aurora DB cluster |
| `secretArn` | 字符串 | 是 | ARN的Secrets Manager 密钥包含 DB credentials |
| `database` | 字符串 | 否 | 数据库名称 \(可选\) |
| `table` | 字符串 | 是 | 表名称到insert 到 |
| `data` | 对象 | 是 | 数据到insert 作为 key-value pairs |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作状态消息 |
| `rows` | 数组 | 以下内容的数组：inserted 行 |

</div>



---

### 更新数据

<ShowcaseCard>
  <BlockConfigPreview
    name="Amazon RDS"
    type="rds"
    color="#527FFF"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Query (SELECT)",
                    "id": "query"
                },
                {
                    "label": "Insert Data",
                    "id": "insert"
                },
                {
                    "label": "Update Data",
                    "id": "update"
                },
                {
                    "label": "Delete Data",
                    "id": "delete"
                },
                {
                    "label": "Execute Raw SQL",
                    "id": "execute"
                }
            ],
            "defaultValue": "update"
        },
        {
            "id": "region",
            "type": "short-input",
            "title": "AWS 区域",
            "placeholder": "us-east-1",
            "required": true
        },
        {
            "id": "accessKeyId",
            "type": "short-input",
            "title": "AWS 访问键 ID",
            "placeholder": "AKIA...",
            "password": true,
            "required": true
        },
        {
            "id": "secretAccessKey",
            "type": "short-input",
            "title": "AWS 密钥访问键",
            "placeholder": "Your secret access key",
            "password": true,
            "required": true
        },
        {
            "id": "resourceArn",
            "type": "short-input",
            "title": "资源 ARN",
            "placeholder": "arn:aws:rds:us-east-1:123456789:cluster:my-cluster",
            "required": true
        },
        {
            "id": "secretArn",
            "type": "short-input",
            "title": "密钥 ARN",
            "placeholder": "arn:aws:secretsmanager:us-east-1:123456789:secret:my-secret",
            "required": true
        },
        {
            "id": "database",
            "type": "short-input",
            "title": "数据库名称",
            "placeholder": "your_database"
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "表名称",
            "placeholder": "users",
            "required": true
        },
        {
            "id": "data",
            "type": "code",
            "title": "更新数据 (JSON)",
            "placeholder": "{\\n  \"name\": \"Jane Doe\",\\n  \"email\": \"jane@example.com\"\\n}",
            "language": "json",
            "required": true
        },
        {
            "id": "conditions",
            "type": "code",
            "title": "条件 (JSON)",
            "placeholder": "{\\n  \"id\": 1\\n}",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Update Data (<code>rds_update</code>)</div>

更新数据在一个 Amazon RDS 表使用数据 API

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `region` | 字符串 | 是 | AWS 区域 \(例如： us-east-1\) |
| `accessKeyId` | 字符串 | 是 | AWS 访问键 ID |
| `secretAccessKey` | 字符串 | 是 | AWS 密钥访问键 |
| `resourceArn` | 字符串 | 是 | ARN的Aurora DB cluster |
| `secretArn` | 字符串 | 是 | ARN的Secrets Manager 密钥包含 DB credentials |
| `database` | 字符串 | 否 | 数据库名称 \(可选\) |
| `table` | 字符串 | 是 | 表名称到更新 |
| `data` | 对象 | 是 | 数据到更新作为 key-value pairs |
| `conditions` | 对象 | 是 | 条件，用于更新 \(例如： \{"id": 1\}\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作状态消息 |
| `rows` | 数组 | 以下内容的数组：已更新行 |

</div>



---

### 删除数据

<ShowcaseCard>
  <BlockConfigPreview
    name="Amazon RDS"
    type="rds"
    color="#527FFF"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Query (SELECT)",
                    "id": "query"
                },
                {
                    "label": "Insert Data",
                    "id": "insert"
                },
                {
                    "label": "Update Data",
                    "id": "update"
                },
                {
                    "label": "Delete Data",
                    "id": "delete"
                },
                {
                    "label": "Execute Raw SQL",
                    "id": "execute"
                }
            ],
            "defaultValue": "delete"
        },
        {
            "id": "region",
            "type": "short-input",
            "title": "AWS 区域",
            "placeholder": "us-east-1",
            "required": true
        },
        {
            "id": "accessKeyId",
            "type": "short-input",
            "title": "AWS 访问键 ID",
            "placeholder": "AKIA...",
            "password": true,
            "required": true
        },
        {
            "id": "secretAccessKey",
            "type": "short-input",
            "title": "AWS 密钥访问键",
            "placeholder": "Your secret access key",
            "password": true,
            "required": true
        },
        {
            "id": "resourceArn",
            "type": "short-input",
            "title": "资源 ARN",
            "placeholder": "arn:aws:rds:us-east-1:123456789:cluster:my-cluster",
            "required": true
        },
        {
            "id": "secretArn",
            "type": "short-input",
            "title": "密钥 ARN",
            "placeholder": "arn:aws:secretsmanager:us-east-1:123456789:secret:my-secret",
            "required": true
        },
        {
            "id": "database",
            "type": "short-input",
            "title": "数据库名称",
            "placeholder": "your_database"
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "表名称",
            "placeholder": "users",
            "required": true
        },
        {
            "id": "conditions",
            "type": "code",
            "title": "条件 (JSON)",
            "placeholder": "{\\n  \"id\": 1\\n}",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Delete Data (<code>rds_delete</code>)</div>

删除数据，来源为一个 Amazon RDS 表使用数据 API

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `region` | 字符串 | 是 | AWS 区域 \(例如： us-east-1\) |
| `accessKeyId` | 字符串 | 是 | AWS 访问键 ID |
| `secretAccessKey` | 字符串 | 是 | AWS 密钥访问键 |
| `resourceArn` | 字符串 | 是 | ARN的Aurora DB cluster |
| `secretArn` | 字符串 | 是 | ARN的Secrets Manager 密钥包含 DB credentials |
| `database` | 字符串 | 否 | 数据库名称 \(可选\) |
| `table` | 字符串 | 是 | 表名称到delete，来源为|
| `conditions` | 对象 | 是 | 条件，用于delete \(例如： \{"id": 1\}\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作状态消息 |
| `rows` | 数组 | 以下内容的数组：已删除行 |

</div>



---

### 执行原始 SQL

<ShowcaseCard>
  <BlockConfigPreview
    name="Amazon RDS"
    type="rds"
    color="#527FFF"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Query (SELECT)",
                    "id": "query"
                },
                {
                    "label": "Insert Data",
                    "id": "insert"
                },
                {
                    "label": "Update Data",
                    "id": "update"
                },
                {
                    "label": "Delete Data",
                    "id": "delete"
                },
                {
                    "label": "Execute Raw SQL",
                    "id": "execute"
                }
            ],
            "defaultValue": "execute"
        },
        {
            "id": "region",
            "type": "short-input",
            "title": "AWS 区域",
            "placeholder": "us-east-1",
            "required": true
        },
        {
            "id": "accessKeyId",
            "type": "short-input",
            "title": "AWS 访问键 ID",
            "placeholder": "AKIA...",
            "password": true,
            "required": true
        },
        {
            "id": "secretAccessKey",
            "type": "short-input",
            "title": "AWS 密钥访问键",
            "placeholder": "Your secret access key",
            "password": true,
            "required": true
        },
        {
            "id": "resourceArn",
            "type": "short-input",
            "title": "资源 ARN",
            "placeholder": "arn:aws:rds:us-east-1:123456789:cluster:my-cluster",
            "required": true
        },
        {
            "id": "secretArn",
            "type": "short-input",
            "title": "密钥 ARN",
            "placeholder": "arn:aws:secretsmanager:us-east-1:123456789:secret:my-secret",
            "required": true
        },
        {
            "id": "database",
            "type": "short-input",
            "title": "数据库名称",
            "placeholder": "your_database"
        },
        {
            "id": "query",
            "type": "code",
            "title": "SQL 查询",
            "placeholder": "SELECT * FROM table_name",
            "language": "sql",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Execute Raw SQL (<code>rds_execute</code>)</div>

执行原始 SQL 在 Amazon RDS 使用数据 API

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `region` | 字符串 | 是 | AWS 区域 \(例如： us-east-1\) |
| `accessKeyId` | 字符串 | 是 | AWS 访问键 ID |
| `secretAccessKey` | 字符串 | 是 | AWS 密钥访问键 |
| `resourceArn` | 字符串 | 是 | ARN的Aurora DB cluster |
| `secretArn` | 字符串 | 是 | ARN的Secrets Manager 密钥包含 DB credentials |
| `database` | 字符串 | 否 | 数据库名称 \(可选\) |
| `query` | 字符串 | 是 | 原始 SQL 查询到执行 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作状态消息 |
| `rows` | 数组 | 以下内容的数组：行已返回或affected |

</div>



---


## 用法说明

将 Amazon RDS Aurora Serverless 集成到 workflow 中使用数据 API. 可查询, insert, 更新, delete,和执行原始 SQL 不含 managing 数据库连接.



## 笔记

- 类别: `tools`
- 类型: `rds`
