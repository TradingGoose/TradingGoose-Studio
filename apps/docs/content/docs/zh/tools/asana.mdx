---
title: Asana
description: 与 Asana 交互
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"

<BlockInfoCard
  type="asana"
  color="#E0E0E0"
  icon={true}
  iconSvg={`<svg className="block-icon"  xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none'>
      <circle cx='18' cy='16' r='4' fill='#F06A6A' />
      <circle cx='6' cy='16' r='4' fill='#F06A6A' />
      <circle cx='12' cy='6.5' r='4' fill='#F06A6A' />
    </svg>`}
/>

## 配置

### 获取任务

<ShowcaseCard>
  <BlockConfigPreview
    name="Asana"
    type="asana"
    color="#E0E0E0"
    iconSvg={`<svg className="block-icon"  xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none'>
      <circle cx='18' cy='16' r='4' fill='#F06A6A' />
      <circle cx='6' cy='16' r='4' fill='#F06A6A' />
      <circle cx='12' cy='6.5' r='4' fill='#F06A6A' />
    </svg>`}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "获取任务",
                    "id": "get_task"
                },
                {
                    "label": "创建任务",
                    "id": "create_task"
                },
                {
                    "label": "更新任务",
                    "id": "update_task"
                },
                {
                    "label": "获取项目",
                    "id": "get_projects"
                },
                {
                    "label": "搜索任务",
                    "id": "search_tasks"
                },
                {
                    "label": "添加评论",
                    "id": "add_comment"
                }
            ],
            "defaultValue": "get_task"
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Asana 账户",
            "placeholder": "选择 Asana 账户",
            "provider": "asana",
            "required": true
        },
        {
            "id": "taskGid",
            "type": "short-input",
            "title": "任务 GID",
            "placeholder": "留空以使用以下筛选条件获取所有任务"
        },
        {
            "id": "getTasks_workspace",
            "type": "short-input",
            "title": "工作区 GID",
            "placeholder": "输入工作区 GID"
        },
        {
            "id": "getTasks_project",
            "type": "short-input",
            "title": "项目 GID",
            "placeholder": "输入项目 GID"
        },
        {
            "id": "getTasks_limit",
            "type": "short-input",
            "title": "限制数量",
            "placeholder": "返回的最大任务数（默认：50）"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取任务（<code>asana_get_task</code>）</div>

通过 GID 检索单个任务，或使用筛选条件获取多个任务

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `taskGid` | string | 否 | 任务的全局唯一标识符（GID）。如未提供，将获取多个任务。 |
| `workspace` | string | 否 | 用于筛选任务的工作区 GID（不使用 taskGid 时必填） |
| `project` | string | 否 | 用于筛选任务的项目 GID |
| `limit` | number | 否 | 返回任务的最大数量（默认值：50） |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `success` | boolean | 操作成功状态 |
| `ts` | string | 响应时间戳 |
| `gid` | string | 负责人 GID |
| `resource_type` | string | 资源类型（任务） |
| `resource_subtype` | string | 资源子类型 |
| `name` | string | 任务名称 |
| `notes` | string | 任务备注或描述 |
| `completed` | boolean | 任务是否已完成 |

</div>

---

### 创建任务

<ShowcaseCard>
  <BlockConfigPreview
    name="Asana"
    type="asana"
    color="#E0E0E0"
    iconSvg={`<svg className="block-icon"  xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none'>
      <circle cx='18' cy='16' r='4' fill='#F06A6A' />
      <circle cx='6' cy='16' r='4' fill='#F06A6A' />
      <circle cx='12' cy='6.5' r='4' fill='#F06A6A' />
    </svg>`}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "options": [
                {
                    "label": "Get Task",
                    "id": "get_task"
                },
                {
                    "label": "Create Task",
                    "id": "create_task"
                },
                {
                    "label": "Update Task",
                    "id": "update_task"
                },
                {
                    "label": "Get Projects",
                    "id": "get_projects"
                },
                {
                    "label": "Search Tasks",
                    "id": "search_tasks"
                },
                {
                    "label": "Add Comment",
                    "id": "add_comment"
                }
            ],
            "defaultValue": "create_task"
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Asana Account",
            "placeholder": "Select Asana account",
            "provider": "asana",
            "required": true
        },
        {
            "id": "workspace",
            "type": "short-input",
            "title": "Workspace GID",
            "placeholder": "Enter Asana workspace GID",
            "required": true
        },
        {
            "id": "name",
            "type": "short-input",
            "title": "Task Name",
            "placeholder": "Enter task name",
            "required": true
        },
        {
            "id": "notes",
            "type": "long-input",
            "title": "Task Notes",
            "placeholder": "Enter task notes or description"
        },
        {
            "id": "assignee",
            "type": "short-input",
            "title": "Assignee GID",
            "placeholder": "Enter assignee user GID"
        },
        {
            "id": "due_on",
            "type": "datetime-input",
            "title": "Due Date",
            "placeholder": "YYYY-MM-DD"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">创建任务（<code>asana_create_task</code>）</div>

在 Asana 中创建新任务

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `workspace` | string | 是 | 创建任务所在的工作区 GID |
| `name` | string | 是 | 任务名称 |
| `notes` | string | 否 | 任务备注或描述 |
| `assignee` | string | 否 | 指派任务的用户 GID |
| `due_on` | string | 否 | 截止日期，格式为 YYYY-MM-DD |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `success` | boolean | 操作成功状态 |
| `ts` | string | 响应时间戳 |
| `gid` | string | 任务全局唯一标识符 |
| `name` | string | 任务名称 |
| `notes` | string | 任务备注或描述 |
| `completed` | boolean | 任务是否已完成 |
| `created_at` | string | 任务创建时间戳 |

</div>

---

### 更新任务

<ShowcaseCard>
  <BlockConfigPreview
    name="Asana"
    type="asana"
    color="#E0E0E0"
    iconSvg={`<svg className="block-icon"  xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none'>
      <circle cx='18' cy='16' r='4' fill='#F06A6A' />
      <circle cx='6' cy='16' r='4' fill='#F06A6A' />
      <circle cx='12' cy='6.5' r='4' fill='#F06A6A' />
    </svg>`}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "获取任务",
                    "id": "get_task"
                },
                {
                    "label": "创建任务",
                    "id": "create_task"
                },
                {
                    "label": "更新任务",
                    "id": "update_task"
                },
                {
                    "label": "获取项目",
                    "id": "get_projects"
                },
                {
                    "label": "搜索任务",
                    "id": "search_tasks"
                },
                {
                    "label": "添加评论",
                    "id": "add_comment"
                }
            ],
            "defaultValue": "update_task"
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Asana 账户",
            "placeholder": "选择 Asana 账户",
            "provider": "asana",
            "required": true
        },
        {
            "id": "taskGid",
            "type": "short-input",
            "title": "任务 GID",
            "placeholder": "输入 Asana 任务 GID",
            "required": true
        },
        {
            "id": "name",
            "type": "short-input",
            "title": "任务名称",
            "placeholder": "输入任务名称",
            "required": true
        },
        {
            "id": "notes",
            "type": "long-input",
            "title": "任务备注",
            "placeholder": "输入任务备注或描述"
        },
        {
            "id": "assignee",
            "type": "short-input",
            "title": "负责人 GID",
            "placeholder": "输入负责人用户 GID"
        },
        {
            "id": "due_on",
            "type": "datetime-input",
            "title": "截止日期",
            "placeholder": "YYYY-MM-DD"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">更新任务 (<code>asana_update_task</code>)</div>

更新 Asana 中的现有任务

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `taskGid` | string | 是 | 待更新任务的全局唯一标识符 \(GID\) |
| `name` | string | 否 | 任务的更新名称 |
| `notes` | string | 否 | 任务的更新备注或描述 |
| `assignee` | string | 否 | 更新后的受让人用户 GID |
| `completed` | boolean | 否 | 标记任务为已完成或未完成 |
| `due_on` | string | 否 | 更新后的截止日期，格式为 YYYY-MM-DD |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `success` | boolean | 操作成功状态 |
| `ts` | string | 响应时间戳 |
| `gid` | string | 任务全局唯一标识符 |
| `name` | string | 任务名称 |
| `notes` | string | 任务备注或描述 |
| `completed` | boolean | 任务是否已完成 |

</div>

---

### 获取项目

<ShowcaseCard>
  <BlockConfigPreview
    name="Asana"
    type="asana"
    color="#E0E0E0"
    iconSvg={`<svg className="block-icon"  xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none'>
      <circle cx='18' cy='16' r='4' fill='#F06A6A' />
      <circle cx='6' cy='16' r='4' fill='#F06A6A' />
      <circle cx='12' cy='6.5' r='4' fill='#F06A6A' />
    </svg>`}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "获取任务",
                    "id": "get_task"
                },
                {
                    "label": "创建任务",
                    "id": "create_task"
                },
                {
                    "label": "更新任务",
                    "id": "update_task"
                },
                {
                    "label": "获取项目",
                    "id": "get_projects"
                },
                {
                    "label": "搜索任务",
                    "id": "search_tasks"
                },
                {
                    "label": "添加评论",
                    "id": "add_comment"
                }
            ],
            "defaultValue": "get_projects"
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Asana 账户",
            "placeholder": "选择 Asana 账户",
            "provider": "asana",
            "required": true
        },
        {
            "id": "workspace",
            "type": "short-input",
            "title": "工作区 GID",
            "placeholder": "输入 Asana 工作区 GID",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取项目 (<code>asana_get_projects</code>)</div>

从 Asana 工作区检索所有项目

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `workspace` | string | 是 | 用于检索项目的工作区 GID |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `success` | boolean | 操作成功状态 |
| `ts` | string | 响应时间戳 |
| `gid` | string | 项目 GID |
| `name` | string | 项目名称 |

</div>

---

### 搜索任务

<ShowcaseCard>
  <BlockConfigPreview
    name="Asana"
    type="asana"
    color="#E0E0E0"
    iconSvg={`<svg className="block-icon"  xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none'>
      <circle cx='18' cy='16' r='4' fill='#F06A6A' />
      <circle cx='6' cy='16' r='4' fill='#F06A6A' />
      <circle cx='12' cy='6.5' r='4' fill='#F06A6A' />
    </svg>`}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "获取任务",
                    "id": "get_task"
                },
                {
                    "label": "创建任务",
                    "id": "create_task"
                },
                {
                    "label": "更新任务",
                    "id": "update_task"
                },
                {
                    "label": "获取项目",
                    "id": "get_projects"
                },
                {
                    "label": "搜索任务",
                    "id": "search_tasks"
                },
                {
                    "label": "添加评论",
                    "id": "add_comment"
                }
            ],
            "defaultValue": "search_tasks"
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Asana 账户",
            "placeholder": "选择 Asana 账户",
            "provider": "asana",
            "required": true
        },
        {
            "id": "workspace",
            "type": "short-input",
            "title": "工作区 GID",
            "placeholder": "输入 Asana 工作区 GID",
            "required": true
        },
        {
            "id": "assignee",
            "type": "short-input",
            "title": "负责人 GID",
            "placeholder": "输入负责人用户 GID"
        },
        {
            "id": "searchText",
            "type": "short-input",
            "title": "搜索文本",
            "placeholder": "输入搜索文本"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">搜索任务 (<code>asana_search_tasks</code>)</div>

在 Asana 工作区中搜索任务

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `workspace` | string | 是 | 用于搜索任务的工作区 GID |
| `text` | string | 否 | 在任务名称中搜索的文本 |
| `assignee` | string | 否 | 按指派人用户 GID 筛选任务 |
| `projects` | array | 否 | 用于筛选任务的项目 GID 数组 |
| `completed` | boolean | 否 | 按完成状态筛选 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `success` | boolean | 操作成功状态 |
| `ts` | string | 响应时间戳 |
| `gid` | string | 指派人 GID |
| `resource_type` | string | 资源类型 |
| `resource_subtype` | string | 资源子类型 |
| `name` | string | 任务名称 |
| `notes` | string | 任务备注 |
| `completed` | boolean | 完成状态 |

</div>

---

### 添加评论

<ShowcaseCard>
  <BlockConfigPreview
    name="Asana"
    type="asana"
    color="#E0E0E0"
    iconSvg={`<svg className="block-icon"  xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none'>
      <circle cx='18' cy='16' r='4' fill='#F06A6A' />
      <circle cx='6' cy='16' r='4' fill='#F06A6A' />
      <circle cx='12' cy='6.5' r='4' fill='#F06A6A' />
    </svg>`}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "获取任务",
                    "id": "get_task"
                },
                {
                    "label": "创建任务",
                    "id": "create_task"
                },
                {
                    "label": "更新任务",
                    "id": "update_task"
                },
                {
                    "label": "获取项目",
                    "id": "get_projects"
                },
                {
                    "label": "搜索任务",
                    "id": "search_tasks"
                },
                {
                    "label": "添加评论",
                    "id": "add_comment"
                }
            ],
            "defaultValue": "add_comment"
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Asana 账户",
            "placeholder": "选择 Asana 账户",
            "provider": "asana",
            "required": true
        },
        {
            "id": "taskGid",
            "type": "short-input",
            "title": "任务 GID",
            "placeholder": "输入 Asana 任务 GID",
            "required": true
        },
        {
            "id": "commentText",
            "type": "long-input",
            "title": "评论内容",
            "placeholder": "输入评论内容",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">添加评论（<code>asana_add_comment</code>）</div>

向 Asana 任务添加评论（动态）

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `taskGid` | string | 是 | 任务的全局唯一标识符（GID） |
| `text` | string | 是 | 评论的文本内容 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `success` | boolean | 操作成功状态 |
| `ts` | string | 响应时间戳 |
| `gid` | string | 作者 GID |
| `text` | string | 评论文本内容 |
| `created_at` | string | 评论创建时间戳 |

</div>

---

## 使用说明

将 Asana 集成到工作流中，可读取、创建和更新任务。

## 注意事项

- 类别：`tools`
- 类型：`asana`
