---
title: MySQL
description: 连接 MySQL 数据库
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="mysql"
  color=""
/>


## 配置

### 查询 (SELECT)

<ShowcaseCard>
  <BlockConfigPreview
    name="MySQL"
    type="mysql"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "Query (SELECT)",
                    "id": "query"
                },
                {
                    "label": "Insert Data",
                    "id": "insert"
                },
                {
                    "label": "Update Data",
                    "id": "update"
                },
                {
                    "label": "Delete Data",
                    "id": "delete"
                },
                {
                    "label": "Execute Raw SQL",
                    "id": "execute"
                }
            ],
            "defaultValue": "query"
        },
        {
            "id": "host",
            "type": "short-input",
            "title": "主机",
            "layout": "full",
            "placeholder": "localhost 或你的.database.主机",
            "required": true
        },
        {
            "id": "port",
            "type": "short-input",
            "title": "Port",
            "layout": "full",
            "placeholder": "3306",
            "required": true
        },
        {
            "id": "database",
            "type": "short-input",
            "title": "数据库名称",
            "layout": "full",
            "placeholder": "your_database",
            "required": true
        },
        {
            "id": "username",
            "type": "short-input",
            "title": "用户名",
            "layout": "full",
            "placeholder": "root",
            "required": true
        },
        {
            "id": "password",
            "type": "short-input",
            "title": "密码",
            "layout": "full",
            "placeholder": "你的 database 密码",
            "password": true,
            "required": true
        },
        {
            "id": "ssl",
            "type": "dropdown",
            "title": "SSL 模式",
            "layout": "full",
            "options": [
                {
                    "label": "Disabled",
                    "id": "disabled"
                },
                {
                    "label": "Required",
                    "id": "required"
                },
                {
                    "label": "Preferred",
                    "id": "preferred"
                }
            ]
        },
        {
            "id": "query",
            "type": "code",
            "title": "SQL 查询",
            "layout": "full",
            "placeholder": "SELECT * FROM users WHERE active = true",
            "language": "sql",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">查询 (SELECT) (<code>mysql_query</code>)</div>

执行 SELECT query 在 MySQL database

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `host` | 字符串 | 是 | MySQL 服务器 hostname 或 IP address |
| `port` | 数字 | 是 | MySQL 服务器 port \(默认: 3306\) |
| `database` | 字符串 | 是 | 数据库 名称 到 connect 到 |
| `username` | 字符串 | 是 | 数据库用户名 |
| `password` | 字符串 | 是 | 数据库密码 |
| `ssl` | 字符串 | 否 | SSL connection 模式 \(已禁用, required, preferred\) |
| `query` | 字符串 | 是 | SQL SELECT query 到 execute |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作 status message |
| `rows` | 数组 | 数组的 rows returned 从该 query |

</div>



---

### 插入数据

<ShowcaseCard>
  <BlockConfigPreview
    name="MySQL"
    type="mysql"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "Query (SELECT)",
                    "id": "query"
                },
                {
                    "label": "Insert Data",
                    "id": "insert"
                },
                {
                    "label": "Update Data",
                    "id": "update"
                },
                {
                    "label": "Delete Data",
                    "id": "delete"
                },
                {
                    "label": "Execute Raw SQL",
                    "id": "execute"
                }
            ],
            "defaultValue": "insert"
        },
        {
            "id": "host",
            "type": "short-input",
            "title": "主机",
            "layout": "full",
            "placeholder": "localhost 或你的.database.主机",
            "required": true
        },
        {
            "id": "port",
            "type": "short-input",
            "title": "Port",
            "layout": "full",
            "placeholder": "3306",
            "required": true
        },
        {
            "id": "database",
            "type": "short-input",
            "title": "数据库名称",
            "layout": "full",
            "placeholder": "your_database",
            "required": true
        },
        {
            "id": "username",
            "type": "short-input",
            "title": "用户名",
            "layout": "full",
            "placeholder": "root",
            "required": true
        },
        {
            "id": "password",
            "type": "short-input",
            "title": "密码",
            "layout": "full",
            "placeholder": "你的 database 密码",
            "password": true,
            "required": true
        },
        {
            "id": "ssl",
            "type": "dropdown",
            "title": "SSL 模式",
            "layout": "full",
            "options": [
                {
                    "label": "Disabled",
                    "id": "disabled"
                },
                {
                    "label": "Required",
                    "id": "required"
                },
                {
                    "label": "Preferred",
                    "id": "preferred"
                }
            ]
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "表名称",
            "layout": "full",
            "placeholder": "用户",
            "required": true
        },
        {
            "id": "data",
            "type": "code",
            "title": "数据 (JSON)",
            "layout": "full",
            "placeholder": "{\\n  \"name\": \"John Doe\",\\n  \"email\": \"john@example.com\",\\n  \"active\": true\\n}",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">插入数据 (<code>mysql_insert</code>)</div>

插入新 record 到 MySQL database

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `host` | 字符串 | 是 | MySQL 服务器 hostname 或 IP address |
| `port` | 数字 | 是 | MySQL 服务器 port \(默认: 3306\) |
| `database` | 字符串 | 是 | 数据库 名称 到 connect 到 |
| `username` | 字符串 | 是 | 数据库用户名 |
| `password` | 字符串 | 是 | 数据库密码 |
| `ssl` | 字符串 | 否 | SSL connection 模式 \(已禁用, required, preferred\) |
| `table` | 字符串 | 是 | 表 名称 到 insert 到 |
| `data` | 对象 | 是 | 数据到 insert 作为 key-value pairs |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作 status message |
| `rows` | 数组 | 数组的 inserted rows |

</div>



---

### 更新数据

<ShowcaseCard>
  <BlockConfigPreview
    name="MySQL"
    type="mysql"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "Query (SELECT)",
                    "id": "query"
                },
                {
                    "label": "Insert Data",
                    "id": "insert"
                },
                {
                    "label": "Update Data",
                    "id": "update"
                },
                {
                    "label": "Delete Data",
                    "id": "delete"
                },
                {
                    "label": "Execute Raw SQL",
                    "id": "execute"
                }
            ],
            "defaultValue": "update"
        },
        {
            "id": "host",
            "type": "short-input",
            "title": "主机",
            "layout": "full",
            "placeholder": "localhost 或你的.database.主机",
            "required": true
        },
        {
            "id": "port",
            "type": "short-input",
            "title": "Port",
            "layout": "full",
            "placeholder": "3306",
            "required": true
        },
        {
            "id": "database",
            "type": "short-input",
            "title": "数据库名称",
            "layout": "full",
            "placeholder": "your_database",
            "required": true
        },
        {
            "id": "username",
            "type": "short-input",
            "title": "用户名",
            "layout": "full",
            "placeholder": "root",
            "required": true
        },
        {
            "id": "password",
            "type": "short-input",
            "title": "密码",
            "layout": "full",
            "placeholder": "你的 database 密码",
            "password": true,
            "required": true
        },
        {
            "id": "ssl",
            "type": "dropdown",
            "title": "SSL 模式",
            "layout": "full",
            "options": [
                {
                    "label": "Disabled",
                    "id": "disabled"
                },
                {
                    "label": "Required",
                    "id": "required"
                },
                {
                    "label": "Preferred",
                    "id": "preferred"
                }
            ]
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "表名称",
            "layout": "full",
            "placeholder": "用户",
            "required": true
        },
        {
            "id": "data",
            "type": "code",
            "title": "更新数据 (JSON)",
            "layout": "full",
            "placeholder": "{\\n  \"name\": \"Jane Doe\",\\n  \"email\": \"jane@example.com\"\\n}",
            "language": "json",
            "required": true
        },
        {
            "id": "where",
            "type": "short-input",
            "title": "WHERE Condition",
            "layout": "full",
            "placeholder": "id = 1",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">更新数据 (<code>mysql_update</code>)</div>

更新现有 records 在 MySQL database

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `host` | 字符串 | 是 | MySQL 服务器 hostname 或 IP address |
| `port` | 数字 | 是 | MySQL 服务器 port \(默认: 3306\) |
| `database` | 字符串 | 是 | 数据库 名称 到 connect 到 |
| `username` | 字符串 | 是 | 数据库用户名 |
| `password` | 字符串 | 是 | 数据库密码 |
| `ssl` | 字符串 | 否 | SSL connection 模式 \(已禁用, required, preferred\) |
| `table` | 字符串 | 是 | 表 名称 到 update |
| `data` | 对象 | 是 | 数据到 update 作为 key-value pairs |
| `where` | 字符串 | 是 | WHERE 子句条件 \(不含 WHERE 关键字\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作 status message |
| `rows` | 数组 | 数组的 updated rows |

</div>



---

### 删除数据

<ShowcaseCard>
  <BlockConfigPreview
    name="MySQL"
    type="mysql"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "Query (SELECT)",
                    "id": "query"
                },
                {
                    "label": "Insert Data",
                    "id": "insert"
                },
                {
                    "label": "Update Data",
                    "id": "update"
                },
                {
                    "label": "Delete Data",
                    "id": "delete"
                },
                {
                    "label": "Execute Raw SQL",
                    "id": "execute"
                }
            ],
            "defaultValue": "delete"
        },
        {
            "id": "host",
            "type": "short-input",
            "title": "主机",
            "layout": "full",
            "placeholder": "localhost 或你的.database.主机",
            "required": true
        },
        {
            "id": "port",
            "type": "short-input",
            "title": "Port",
            "layout": "full",
            "placeholder": "3306",
            "required": true
        },
        {
            "id": "database",
            "type": "short-input",
            "title": "数据库名称",
            "layout": "full",
            "placeholder": "your_database",
            "required": true
        },
        {
            "id": "username",
            "type": "short-input",
            "title": "用户名",
            "layout": "full",
            "placeholder": "root",
            "required": true
        },
        {
            "id": "password",
            "type": "short-input",
            "title": "密码",
            "layout": "full",
            "placeholder": "你的 database 密码",
            "password": true,
            "required": true
        },
        {
            "id": "ssl",
            "type": "dropdown",
            "title": "SSL 模式",
            "layout": "full",
            "options": [
                {
                    "label": "Disabled",
                    "id": "disabled"
                },
                {
                    "label": "Required",
                    "id": "required"
                },
                {
                    "label": "Preferred",
                    "id": "preferred"
                }
            ]
        },
        {
            "id": "table",
            "type": "short-input",
            "title": "表名称",
            "layout": "full",
            "placeholder": "用户",
            "required": true
        },
        {
            "id": "where",
            "type": "short-input",
            "title": "WHERE Condition",
            "layout": "full",
            "placeholder": "id = 1",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">删除数据 (<code>mysql_delete</code>)</div>

删除 records 从 MySQL database

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `host` | 字符串 | 是 | MySQL 服务器 hostname 或 IP address |
| `port` | 数字 | 是 | MySQL 服务器 port \(默认: 3306\) |
| `database` | 字符串 | 是 | 数据库 名称 到 connect 到 |
| `username` | 字符串 | 是 | 数据库用户名 |
| `password` | 字符串 | 是 | 数据库密码 |
| `ssl` | 字符串 | 否 | SSL connection 模式 \(已禁用, required, preferred\) |
| `table` | 字符串 | 是 | 表 名称 到 delete 从 |
| `where` | 字符串 | 是 | WHERE 子句条件 \(不含 WHERE 关键字\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作 status message |
| `rows` | 数组 | 数组的 deleted rows |

</div>



---

### 执行原始 SQL

<ShowcaseCard>
  <BlockConfigPreview
    name="MySQL"
    type="mysql"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "Query (SELECT)",
                    "id": "query"
                },
                {
                    "label": "Insert Data",
                    "id": "insert"
                },
                {
                    "label": "Update Data",
                    "id": "update"
                },
                {
                    "label": "Delete Data",
                    "id": "delete"
                },
                {
                    "label": "Execute Raw SQL",
                    "id": "execute"
                }
            ],
            "defaultValue": "execute"
        },
        {
            "id": "host",
            "type": "short-input",
            "title": "主机",
            "layout": "full",
            "placeholder": "localhost 或你的.database.主机",
            "required": true
        },
        {
            "id": "port",
            "type": "short-input",
            "title": "Port",
            "layout": "full",
            "placeholder": "3306",
            "required": true
        },
        {
            "id": "database",
            "type": "short-input",
            "title": "数据库名称",
            "layout": "full",
            "placeholder": "your_database",
            "required": true
        },
        {
            "id": "username",
            "type": "short-input",
            "title": "用户名",
            "layout": "full",
            "placeholder": "root",
            "required": true
        },
        {
            "id": "password",
            "type": "short-input",
            "title": "密码",
            "layout": "full",
            "placeholder": "你的 database 密码",
            "password": true,
            "required": true
        },
        {
            "id": "ssl",
            "type": "dropdown",
            "title": "SSL 模式",
            "layout": "full",
            "options": [
                {
                    "label": "Disabled",
                    "id": "disabled"
                },
                {
                    "label": "Required",
                    "id": "required"
                },
                {
                    "label": "Preferred",
                    "id": "preferred"
                }
            ]
        },
        {
            "id": "query",
            "type": "code",
            "title": "SQL 查询",
            "layout": "full",
            "placeholder": "SELECT * FROM table_name",
            "language": "sql",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">执行原始 SQL (<code>mysql_execute</code>)</div>

执行原始 SQL query 在 MySQL database

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `host` | 字符串 | 是 | MySQL 服务器 hostname 或 IP address |
| `port` | 数字 | 是 | MySQL 服务器 port \(默认: 3306\) |
| `database` | 字符串 | 是 | 数据库 名称 到 connect 到 |
| `username` | 字符串 | 是 | 数据库用户名 |
| `password` | 字符串 | 是 | 数据库密码 |
| `ssl` | 字符串 | 否 | SSL connection 模式 \(已禁用, required, preferred\) |
| `query` | 字符串 | 是 | 原始 SQL query 到 execute |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作 status message |
| `rows` | 数组 | 数组的 rows returned 从该 query |

</div>



---


## 用量说明

将 MySQL 集成到工作流中。你可以查询、插入、更新和删除数据，也可执行原始 SQL。



## 备注

- Category: `tools`
- Type: `mysql`
