---
title: Grain
description: 访问会议录音、转写和 AI 摘要
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"
import { Card, Cards } from 'fumadocs-ui/components/card'

<BlockInfoCard
  type="grain"
  color="#00B96C"
/>

<Cards>
  <Card title="Use as a Trigger" href="/triggers/grain">
    See 触发器 events, setup, 和 event payloads 用于 Grain.
  </Card>
</Cards>


## 配置

### 列出 Recordings

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain"
    type="grain"
    color="#00B96C"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "List Recordings",
                    "id": "grain_list_recordings"
                },
                {
                    "label": "Get Recording",
                    "id": "grain_get_recording"
                },
                {
                    "label": "Get Transcript",
                    "id": "grain_get_transcript"
                },
                {
                    "label": "List Teams",
                    "id": "grain_list_teams"
                },
                {
                    "label": "List Meeting Types",
                    "id": "grain_list_meeting_types"
                },
                {
                    "label": "Create Webhook",
                    "id": "grain_create_hook"
                },
                {
                    "label": "List Webhooks",
                    "id": "grain_list_hooks"
                },
                {
                    "label": "Delete Webhook",
                    "id": "grain_delete_hook"
                }
            ],
            "defaultValue": "grain_list_recordings"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "输入你的 Grain API 密钥",
            "password": true,
            "required": true
        },
        {
            "id": "cursor",
            "type": "short-input",
            "title": "分页 Cursor",
            "placeholder": "Cursor 用于下一个页面 (可选)"
        },
        {
            "id": "beforeDatetime",
            "type": "datetime-input",
            "title": "之前日期",
            "placeholder": "ISO8601 时间戳 (例如, 2024-01-01T00:00:00Z)"
        },
        {
            "id": "afterDatetime",
            "type": "datetime-input",
            "title": "之后日期",
            "placeholder": "ISO8601 时间戳 (例如, 2024-01-01T00:00:00Z)"
        },
        {
            "id": "participantScope",
            "type": "dropdown",
            "title": "Participant Scope",
            "options": [
                {
                    "label": "Internal",
                    "id": "internal"
                },
                {
                    "label": "External",
                    "id": "external"
                }
            ]
        },
        {
            "id": "titleSearch",
            "type": "short-input",
            "title": "标题搜索",
            "placeholder": "搜索按 recording title"
        },
        {
            "id": "teamId",
            "type": "short-input",
            "title": "团队 ID",
            "placeholder": "按条件筛选团队 UUID (可选)"
        },
        {
            "id": "meetingTypeId",
            "type": "short-input",
            "title": "会议类型 ID",
            "placeholder": "按条件筛选会议 type UUID (可选)"
        },
        {
            "id": "includeHighlights",
            "type": "switch",
            "title": "包含 Highlights"
        },
        {
            "id": "includeParticipants",
            "type": "switch",
            "title": "包含参与者"
        },
        {
            "id": "includeAiSummary",
            "type": "switch",
            "title": "包含 AI 摘要"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">列出 Recordings (<code>grain_list_recordings</code>)</div>

列出 recordings 从 Grain 使用可选 filters 和分页

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Grain API 密钥 \(个人访问令牌\) |
| `cursor` | 字符串 | 否 | 分页 cursor 用于下一个页面 |
| `beforeDatetime` | 字符串 | 否 | 仅 recordings 之前此 ISO8601 时间戳 |
| `afterDatetime` | 字符串 | 否 | 仅 recordings 之后此 ISO8601 时间戳 |
| `participantScope` | 字符串 | 否 | 筛选条件: "内部" 或 "外部" |
| `titleSearch` | 字符串 | 否 | 搜索 term 到按条件筛选 recording title |
| `teamId` | 字符串 | 否 | 按条件筛选团队 UUID |
| `meetingTypeId` | 字符串 | 否 | 按条件筛选会议 type UUID |
| `includeHighlights` | 布尔值 | 否 | 包含 highlights/clips 在 response |
| `includeParticipants` | 布尔值 | 否 | 包含 participant 列表在 response |
| `includeAiSummary` | 布尔值 | 否 | 包含 AI-generated summary |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `id` | 字符串 | Recording UUID |
| `title` | 字符串 | Recording title |
| `start_datetime` | 字符串 | ISO8601 start 时间戳 |
| `end_datetime` | 字符串 | ISO8601 end 时间戳 |
| `duration_ms` | 数字 | 时长在 milliseconds |
| `media_type` | 字符串 | 音频, 转写文本, 或视频 |
| `source` | 字符串 | Recording 来源 |
| `url` | 字符串 | URL 到 view 在 Grain |
| `thumbnail_url` | 字符串 | Thumbnail URL |
| `tags` | 数组 | 数组的标签 |
| `teams` | 数组 | Teams 该 recording belongs 到 |

</div>



---

### 获取 Recording

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain"
    type="grain"
    color="#00B96C"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "List Recordings",
                    "id": "grain_list_recordings"
                },
                {
                    "label": "Get Recording",
                    "id": "grain_get_recording"
                },
                {
                    "label": "Get Transcript",
                    "id": "grain_get_transcript"
                },
                {
                    "label": "List Teams",
                    "id": "grain_list_teams"
                },
                {
                    "label": "List Meeting Types",
                    "id": "grain_list_meeting_types"
                },
                {
                    "label": "Create Webhook",
                    "id": "grain_create_hook"
                },
                {
                    "label": "List Webhooks",
                    "id": "grain_list_hooks"
                },
                {
                    "label": "Delete Webhook",
                    "id": "grain_delete_hook"
                }
            ],
            "defaultValue": "grain_get_recording"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "输入你的 Grain API 密钥",
            "password": true,
            "required": true
        },
        {
            "id": "recordingId",
            "type": "short-input",
            "title": "Recording ID",
            "placeholder": "输入 recording UUID",
            "required": true
        },
        {
            "id": "includeHighlights",
            "type": "switch",
            "title": "包含 Highlights"
        },
        {
            "id": "includeParticipants",
            "type": "switch",
            "title": "包含参与者"
        },
        {
            "id": "includeAiSummary",
            "type": "switch",
            "title": "包含 AI 摘要"
        },
        {
            "id": "includeCalendarEvent",
            "type": "switch",
            "title": "包含日历事件"
        },
        {
            "id": "includeHubspot",
            "type": "switch",
            "title": "包含 HubSpot 数据"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取 Recording (<code>grain_get_recording</code>)</div>

获取详细信息的一个单个 recording 按 ID

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Grain API 密钥 \(个人访问令牌\) |
| `recordingId` | 字符串 | 是 | 该 recording UUID |
| `includeHighlights` | 布尔值 | 否 | 包含 highlights/clips |
| `includeParticipants` | 布尔值 | 否 | 包含 participant 列表 |
| `includeAiSummary` | 布尔值 | 否 | 包含 AI summary |
| `includeCalendarEvent` | 布尔值 | 否 | 包含日历 event data |
| `includeHubspot` | 布尔值 | 否 | 包含 HubSpot associations |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `id` | 字符串 | Recording UUID |
| `title` | 字符串 | Recording title |
| `start_datetime` | 字符串 | ISO8601 start 时间戳 |
| `end_datetime` | 字符串 | ISO8601 end 时间戳 |
| `duration_ms` | 数字 | 时长在 milliseconds |
| `media_type` | 字符串 | 音频, 转写文本, 或视频 |
| `source` | 字符串 | Recording 来源 \(zoom, meet, teams, etc.\) |
| `url` | 字符串 | URL 到 view 在 Grain |
| `thumbnail_url` | 字符串 | Thumbnail 图像 URL |
| `tags` | 数组 | 数组的标签 strings |
| `teams` | 数组 | Teams 该 recording belongs 到 |
| `meeting_type` | 对象 | 会议 type info \(id, 名称, scope\) |
| `highlights` | 数组 | Highlights \(if included\) |
| `participants` | 数组 | 参与者 \(if included\) |
| `ai_summary` | 对象 | AI summary text \(if included\) |
| `calendar_event` | 对象 | 日历 event data \(if included\) |

</div>



---

### 获取转写文本

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain"
    type="grain"
    color="#00B96C"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "List Recordings",
                    "id": "grain_list_recordings"
                },
                {
                    "label": "Get Recording",
                    "id": "grain_get_recording"
                },
                {
                    "label": "Get Transcript",
                    "id": "grain_get_transcript"
                },
                {
                    "label": "List Teams",
                    "id": "grain_list_teams"
                },
                {
                    "label": "List Meeting Types",
                    "id": "grain_list_meeting_types"
                },
                {
                    "label": "Create Webhook",
                    "id": "grain_create_hook"
                },
                {
                    "label": "List Webhooks",
                    "id": "grain_list_hooks"
                },
                {
                    "label": "Delete Webhook",
                    "id": "grain_delete_hook"
                }
            ],
            "defaultValue": "grain_get_transcript"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "输入你的 Grain API 密钥",
            "password": true,
            "required": true
        },
        {
            "id": "recordingId",
            "type": "short-input",
            "title": "Recording ID",
            "placeholder": "输入 recording UUID",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取转写文本 (<code>grain_get_transcript</code>)</div>

获取该完整转写文本的一个 recording

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Grain API 密钥 \(个人访问令牌\) |
| `recordingId` | 字符串 | 是 | 该 recording UUID |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `participant_id` | 字符串 | Participant UUID \(nullable\) |
| `speaker` | 字符串 | Speaker 名称 |
| `start` | 数字 | 启动时间戳在 ms |
| `end` | 数字 | End 时间戳在 ms |

</div>



---

### 列出 Teams

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain"
    type="grain"
    color="#00B96C"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "List Recordings",
                    "id": "grain_list_recordings"
                },
                {
                    "label": "Get Recording",
                    "id": "grain_get_recording"
                },
                {
                    "label": "Get Transcript",
                    "id": "grain_get_transcript"
                },
                {
                    "label": "List Teams",
                    "id": "grain_list_teams"
                },
                {
                    "label": "List Meeting Types",
                    "id": "grain_list_meeting_types"
                },
                {
                    "label": "Create Webhook",
                    "id": "grain_create_hook"
                },
                {
                    "label": "List Webhooks",
                    "id": "grain_list_hooks"
                },
                {
                    "label": "Delete Webhook",
                    "id": "grain_delete_hook"
                }
            ],
            "defaultValue": "grain_list_teams"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "输入你的 Grain API 密钥",
            "password": true,
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">列出 Teams (<code>grain_list_teams</code>)</div>

列出全部 teams 在该 工作区

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Grain API 密钥 \(个人访问令牌\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `id` | 字符串 | 团队 UUID |

</div>



---

### 列出会议类型

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain"
    type="grain"
    color="#00B96C"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "List Recordings",
                    "id": "grain_list_recordings"
                },
                {
                    "label": "Get Recording",
                    "id": "grain_get_recording"
                },
                {
                    "label": "Get Transcript",
                    "id": "grain_get_transcript"
                },
                {
                    "label": "List Teams",
                    "id": "grain_list_teams"
                },
                {
                    "label": "List Meeting Types",
                    "id": "grain_list_meeting_types"
                },
                {
                    "label": "Create Webhook",
                    "id": "grain_create_hook"
                },
                {
                    "label": "List Webhooks",
                    "id": "grain_list_hooks"
                },
                {
                    "label": "Delete Webhook",
                    "id": "grain_delete_hook"
                }
            ],
            "defaultValue": "grain_list_meeting_types"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "输入你的 Grain API 密钥",
            "password": true,
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">列出会议类型 (<code>grain_list_meeting_types</code>)</div>

列出全部会议类型在该 工作区

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Grain API 密钥 \(个人访问令牌\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `id` | 字符串 | 会议 type UUID |
| `name` | 字符串 | 会议 type 名称 |

</div>



---

### 创建 Webhook

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain"
    type="grain"
    color="#00B96C"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "List Recordings",
                    "id": "grain_list_recordings"
                },
                {
                    "label": "Get Recording",
                    "id": "grain_get_recording"
                },
                {
                    "label": "Get Transcript",
                    "id": "grain_get_transcript"
                },
                {
                    "label": "List Teams",
                    "id": "grain_list_teams"
                },
                {
                    "label": "List Meeting Types",
                    "id": "grain_list_meeting_types"
                },
                {
                    "label": "Create Webhook",
                    "id": "grain_create_hook"
                },
                {
                    "label": "List Webhooks",
                    "id": "grain_list_hooks"
                },
                {
                    "label": "Delete Webhook",
                    "id": "grain_delete_hook"
                }
            ],
            "defaultValue": "grain_create_hook"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "输入你的 Grain API 密钥",
            "password": true,
            "required": true
        },
        {
            "id": "beforeDatetime",
            "type": "datetime-input",
            "title": "之前日期",
            "placeholder": "ISO8601 时间戳 (例如, 2024-01-01T00:00:00Z)"
        },
        {
            "id": "afterDatetime",
            "type": "datetime-input",
            "title": "之后日期",
            "placeholder": "ISO8601 时间戳 (例如, 2024-01-01T00:00:00Z)"
        },
        {
            "id": "participantScope",
            "type": "dropdown",
            "title": "Participant Scope",
            "options": [
                {
                    "label": "Internal",
                    "id": "internal"
                },
                {
                    "label": "External",
                    "id": "external"
                }
            ]
        },
        {
            "id": "teamId",
            "type": "short-input",
            "title": "团队 ID",
            "placeholder": "按条件筛选团队 UUID (可选)"
        },
        {
            "id": "meetingTypeId",
            "type": "short-input",
            "title": "会议类型 ID",
            "placeholder": "按条件筛选会议 type UUID (可选)"
        },
        {
            "id": "includeHighlights",
            "type": "switch",
            "title": "包含 Highlights"
        },
        {
            "id": "includeParticipants",
            "type": "switch",
            "title": "包含参与者"
        },
        {
            "id": "includeAiSummary",
            "type": "switch",
            "title": "包含 AI 摘要"
        },
        {
            "id": "hookUrl",
            "type": "short-input",
            "title": "Webhook URL",
            "placeholder": "输入 Webhook 端点 URL",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">创建 Webhook (<code>grain_create_hook</code>)</div>

创建一个 Webhook 到 receive recording events

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Grain API 密钥 \(个人访问令牌\) |
| `hookUrl` | 字符串 | 是 | Webhook 端点 URL \(必须 respond 2xx\) |
| `filterBeforeDatetime` | 字符串 | 否 | 筛选条件: recordings 之前此 date |
| `filterAfterDatetime` | 字符串 | 否 | 筛选条件: recordings 之后此 date |
| `filterParticipantScope` | 字符串 | 否 | 筛选条件: "内部" 或 "外部" |
| `filterTeamId` | 字符串 | 否 | 筛选条件: 指定团队 UUID |
| `filterMeetingTypeId` | 字符串 | 否 | 筛选条件: 指定会议 type |
| `includeHighlights` | 布尔值 | 否 | 包含 highlights 在 Webhook 有效负载 |
| `includeParticipants` | 布尔值 | 否 | 包含参与者在 Webhook 有效负载 |
| `includeAiSummary` | 布尔值 | 否 | 包含 AI summary 在 Webhook 有效负载 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `id` | 字符串 | Hook UUID |
| `enabled` | 布尔值 | 是否 hook is 活跃 |
| `hook_url` | 字符串 | 该 Webhook URL |
| `filter` | 对象 | Applied filters |
| `include` | 对象 | Included 字段 |

</div>



---

### 列出 Webhook

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain"
    type="grain"
    color="#00B96C"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "List Recordings",
                    "id": "grain_list_recordings"
                },
                {
                    "label": "Get Recording",
                    "id": "grain_get_recording"
                },
                {
                    "label": "Get Transcript",
                    "id": "grain_get_transcript"
                },
                {
                    "label": "List Teams",
                    "id": "grain_list_teams"
                },
                {
                    "label": "List Meeting Types",
                    "id": "grain_list_meeting_types"
                },
                {
                    "label": "Create Webhook",
                    "id": "grain_create_hook"
                },
                {
                    "label": "List Webhooks",
                    "id": "grain_list_hooks"
                },
                {
                    "label": "Delete Webhook",
                    "id": "grain_delete_hook"
                }
            ],
            "defaultValue": "grain_list_hooks"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "输入你的 Grain API 密钥",
            "password": true,
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">列出 Webhook (<code>grain_list_hooks</code>)</div>

列出全部 Webhook 用于该 account

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Grain API 密钥 \(个人访问令牌\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `id` | 字符串 | Hook UUID |
| `enabled` | 布尔值 | 是否 hook is 活跃 |
| `hook_url` | 字符串 | Webhook URL |
| `filter` | 对象 | Applied filters |
| `include` | 对象 | Included 字段 |

</div>



---

### 删除 Webhook

<ShowcaseCard>
  <BlockConfigPreview
    name="Grain"
    type="grain"
    color="#00B96C"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "List Recordings",
                    "id": "grain_list_recordings"
                },
                {
                    "label": "Get Recording",
                    "id": "grain_get_recording"
                },
                {
                    "label": "Get Transcript",
                    "id": "grain_get_transcript"
                },
                {
                    "label": "List Teams",
                    "id": "grain_list_teams"
                },
                {
                    "label": "List Meeting Types",
                    "id": "grain_list_meeting_types"
                },
                {
                    "label": "Create Webhook",
                    "id": "grain_create_hook"
                },
                {
                    "label": "List Webhooks",
                    "id": "grain_list_hooks"
                },
                {
                    "label": "Delete Webhook",
                    "id": "grain_delete_hook"
                }
            ],
            "defaultValue": "grain_delete_hook"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "placeholder": "输入你的 Grain API 密钥",
            "password": true,
            "required": true
        },
        {
            "id": "hookId",
            "type": "short-input",
            "title": "Webhook ID",
            "placeholder": "输入 Webhook UUID 到 delete",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">删除 Webhook (<code>grain_delete_hook</code>)</div>

删除一个 Webhook 按 ID

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Grain API 密钥 \(个人访问令牌\) |
| `hookId` | 字符串 | 是 | 该 hook UUID 到 delete |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

请参阅此操作的块输出。

</div>



---


## 用量说明

集成 Grain 到你的 workflow. 访问权限会议 recordings, transcripts, highlights, 和 AI-generated summaries. 可以 also 触发器工作流根据 Grain Webhook events.



## 备注

- Category: `tools`
- Type: `grain`
