---
title: Typeform
description: 与 Typeform 交互
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"

<BlockInfoCard
  type="typeform"
  color=""
  icon={true}
  iconSvg={``}
/>

## 配置

### 获取响应

<ShowcaseCard>
  <BlockConfigPreview
    name="Typeform"
    type="typeform"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "获取响应",
                    "id": "typeform_responses"
                },
                {
                    "label": "下载文件",
                    "id": "typeform_files"
                },
                {
                    "label": "表单洞察",
                    "id": "typeform_insights"
                }
            ],
            "defaultValue": "typeform_responses"
        },
        {
            "id": "formId",
            "type": "short-input",
            "title": "表单 ID",
            "layout": "full",
            "placeholder": "请输入您的 Typeform 表单 ID",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "个人访问令牌",
            "layout": "full",
            "placeholder": "请输入您的 Typeform 个人访问令牌",
            "password": true,
            "required": true
        },
        {
            "id": "pageSize",
            "type": "short-input",
            "title": "每页数量",
            "layout": "half",
            "placeholder": "每页响应数量（默认：25）"
        },
        {
            "id": "since",
            "type": "datetime-input",
            "title": "开始时间",
            "layout": "half",
            "placeholder": "YYYY-MM-DD 或 YYYY-MM-DDTHH:mm:ssZ"
        },
        {
            "id": "until",
            "type": "datetime-input",
            "title": "结束时间",
            "layout": "half",
            "placeholder": "YYYY-MM-DD 或 YYYY-MM-DDTHH:mm:ssZ"
        },
        {
            "id": "completed",
            "type": "dropdown",
            "title": "完成状态",
            "layout": "half",
            "options": [
                {
                    "label": "所有响应",
                    "id": "all"
                },
                {
                    "label": "仅已完成",
                    "id": "true"
                },
                {
                    "label": "仅未完成",
                    "id": "false"
                }
            ]
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取响应（<code>typeform_responses</code>）</div>

从 Typeform 检索表单响应

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `formId` | string | 是 | Typeform 表单 ID |
| `apiKey` | string | 是 | Typeform 个人访问令牌 |
| `pageSize` | number | 否 | 要检索的响应数量 \(默认：25\) |
| `since` | string | 否 | 检索此日期之后提交的响应 \(ISO 8601 格式\) |
| `until` | string | 否 | 检索此日期之前提交的响应 \(ISO 8601 格式\) |
| `completed` | string | 否 | 按完成状态筛选 \(true/false\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

请参阅此操作的模块输出。

</div>

---

### 下载文件

<ShowcaseCard>
  <BlockConfigPreview
    name="Typeform"
    type="typeform"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "获取响应",
                    "id": "typeform_responses"
                },
                {
                    "label": "下载文件",
                    "id": "typeform_files"
                },
                {
                    "label": "表单洞察",
                    "id": "typeform_insights"
                }
            ],
            "defaultValue": "typeform_files"
        },
        {
            "id": "formId",
            "type": "short-input",
            "title": "表单 ID",
            "layout": "full",
            "placeholder": "请输入您的 Typeform 表单 ID",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "个人访问令牌",
            "layout": "full",
            "placeholder": "请输入您的 Typeform 个人访问令牌",
            "password": true,
            "required": true
        },
        {
            "id": "responseId",
            "type": "short-input",
            "title": "响应 ID",
            "layout": "full",
            "placeholder": "请输入响应 ID（令牌）"
        },
        {
            "id": "fieldId",
            "type": "short-input",
            "title": "字段 ID",
            "layout": "half",
            "placeholder": "请输入文件上传字段 ID"
        },
        {
            "id": "filename",
            "type": "short-input",
            "title": "文件名",
            "layout": "half",
            "placeholder": "请输入文件的确切文件名"
        },
        {
            "id": "inline",
            "type": "switch",
            "title": "内联显示",
            "layout": "half"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">下载文件（<code>typeform_files</code>）</div>

下载 Typeform 回复中上传的文件

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `formId` | string | 是 | Typeform 表单 ID |
| `responseId` | string | 是 | 包含文件的回复 ID |
| `fieldId` | string | 是 | 文件上传字段的唯一 ID |
| `filename` | string | 是 | 已上传文件的文件名 |
| `inline` | boolean | 否 | 是否以内联 Content-Disposition 方式请求文件 |
| `apiKey` | string | 是 | Typeform 个人访问令牌 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `fileUrl` | string | 已上传文件的直接下载 URL |
| `contentType` | string | 已上传文件的 MIME 类型 |

</div>

---

### 表单洞察

<ShowcaseCard>
  <BlockConfigPreview
    name="Typeform"
    type="typeform"
    color=""
    iconSvg={``}
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "获取响应",
                    "id": "typeform_responses"
                },
                {
                    "label": "下载文件",
                    "id": "typeform_files"
                },
                {
                    "label": "表单洞察",
                    "id": "typeform_insights"
                }
            ],
            "defaultValue": "typeform_insights"
        },
        {
            "id": "formId",
            "type": "short-input",
            "title": "表单 ID",
            "layout": "full",
            "placeholder": "请输入您的 Typeform 表单 ID",
            "required": true
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "个人访问令牌",
            "layout": "full",
            "placeholder": "请输入您的 Typeform 个人访问令牌",
            "password": true,
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">表单洞察 (<code>typeform_insights</code>)</div>

检索 Typeform 表单的洞察数据与分析报告

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 描述 |
| --------- | ---- | -------- | ----------- |
| `formId` | string | 是 | Typeform 表单 ID |
| `apiKey` | string | 是 | Typeform 个人访问令牌 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 描述 |
| --------- | ---- | ----------- |
| `dropoffs` | number | 在此字段流失的用户数量 |
| `id` | string | 唯一字段 ID |
| `label` | string | 字段标签 |
| `ref` | string | 字段引用名称 |
| `title` | string | 字段标题／问题 |
| `type` | string | 字段类型 \(例如：short_text、multiple_choice\) |

</div>

---

## 使用说明

将 Typeform 集成至工作流中。支持检索响应数据、下载文件及获取表单洞察，需提供 API 密钥。

## 注意事项

- 分类：`tools`
- 类型：`typeform`
