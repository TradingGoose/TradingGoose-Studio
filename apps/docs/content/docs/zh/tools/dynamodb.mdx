---
title: Amazon DynamoDB
description: 连接 Amazon DynamoDB
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="dynamodb"
  color="#527FFF"
/>


## 配置

### 获取项目

<ShowcaseCard>
  <BlockConfigPreview
    name="Amazon DynamoDB"
    type="dynamodb"
    color="#527FFF"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Item",
                    "id": "get"
                },
                {
                    "label": "Put Item",
                    "id": "put"
                },
                {
                    "label": "Query",
                    "id": "query"
                },
                {
                    "label": "Scan",
                    "id": "scan"
                },
                {
                    "label": "Update Item",
                    "id": "update"
                },
                {
                    "label": "Delete Item",
                    "id": "delete"
                }
            ],
            "defaultValue": "get"
        },
        {
            "id": "region",
            "type": "short-input",
            "title": "AWS Region",
            "placeholder": "us-east-1",
            "required": true
        },
        {
            "id": "accessKeyId",
            "type": "short-input",
            "title": "AWS 访问权限键 ID",
            "placeholder": "AKIA...",
            "password": true,
            "required": true
        },
        {
            "id": "secretAccessKey",
            "type": "short-input",
            "title": "AWS Secret 访问权限键",
            "placeholder": "你的 secret 访问权限 key",
            "password": true,
            "required": true
        },
        {
            "id": "tableName",
            "type": "short-input",
            "title": "表名称",
            "placeholder": "my-table",
            "required": true
        },
        {
            "id": "key",
            "type": "code",
            "title": "键 (JSON)",
            "placeholder": "{\\n  \"pk\": \"用户#123\"\\n}",
            "language": "json",
            "required": true
        },
        {
            "id": "consistentRead",
            "type": "dropdown",
            "title": "Consistent 读取",
            "options": [
                {
                    "label": "Eventually Consistent",
                    "id": "false"
                },
                {
                    "label": "Strongly Consistent",
                    "id": "true"
                }
            ]
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取项目 (<code>dynamodb_get</code>)</div>

获取一个项目从一个 DynamoDB table 按 primary key

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `region` | 字符串 | 是 | AWS region \(例如, us-east-1\) |
| `accessKeyId` | 字符串 | 是 | AWS 访问权限 key ID |
| `secretAccessKey` | 字符串 | 是 | AWS secret 访问权限 key |
| `tableName` | 字符串 | 是 | DynamoDB table 名称 |
| `key` | 对象 | 是 | Primary key 的该项目到 retrieve |
| `consistentRead` | 布尔值 | 否 | 使用 strongly consistent read |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作 status message |

</div>



---

### Put 项目

<ShowcaseCard>
  <BlockConfigPreview
    name="Amazon DynamoDB"
    type="dynamodb"
    color="#527FFF"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Item",
                    "id": "get"
                },
                {
                    "label": "Put Item",
                    "id": "put"
                },
                {
                    "label": "Query",
                    "id": "query"
                },
                {
                    "label": "Scan",
                    "id": "scan"
                },
                {
                    "label": "Update Item",
                    "id": "update"
                },
                {
                    "label": "Delete Item",
                    "id": "delete"
                }
            ],
            "defaultValue": "put"
        },
        {
            "id": "region",
            "type": "short-input",
            "title": "AWS Region",
            "placeholder": "us-east-1",
            "required": true
        },
        {
            "id": "accessKeyId",
            "type": "short-input",
            "title": "AWS 访问权限键 ID",
            "placeholder": "AKIA...",
            "password": true,
            "required": true
        },
        {
            "id": "secretAccessKey",
            "type": "short-input",
            "title": "AWS Secret 访问权限键",
            "placeholder": "你的 secret 访问权限 key",
            "password": true,
            "required": true
        },
        {
            "id": "tableName",
            "type": "short-input",
            "title": "表名称",
            "placeholder": "my-table",
            "required": true
        },
        {
            "id": "item",
            "type": "code",
            "title": "项目 (JSON)",
            "placeholder": "{\\n  \"pk\": \"用户#123\",\\n  \"name\": \"John Doe\",\\n  \"email\": \"john@example.com\"\\n}",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Put 项目 (<code>dynamodb_put</code>)</div>

Put 一个项目到一个 DynamoDB table

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `region` | 字符串 | 是 | AWS region \(例如, us-east-1\) |
| `accessKeyId` | 字符串 | 是 | AWS 访问权限 key ID |
| `secretAccessKey` | 字符串 | 是 | AWS secret 访问权限 key |
| `tableName` | 字符串 | 是 | DynamoDB table 名称 |
| `item` | 对象 | 是 | 项目到 put 到该 table |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作 status message |

</div>



---

### 查询

<ShowcaseCard>
  <BlockConfigPreview
    name="Amazon DynamoDB"
    type="dynamodb"
    color="#527FFF"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Item",
                    "id": "get"
                },
                {
                    "label": "Put Item",
                    "id": "put"
                },
                {
                    "label": "Query",
                    "id": "query"
                },
                {
                    "label": "Scan",
                    "id": "scan"
                },
                {
                    "label": "Update Item",
                    "id": "update"
                },
                {
                    "label": "Delete Item",
                    "id": "delete"
                }
            ],
            "defaultValue": "query"
        },
        {
            "id": "region",
            "type": "short-input",
            "title": "AWS Region",
            "placeholder": "us-east-1",
            "required": true
        },
        {
            "id": "accessKeyId",
            "type": "short-input",
            "title": "AWS 访问权限键 ID",
            "placeholder": "AKIA...",
            "password": true,
            "required": true
        },
        {
            "id": "secretAccessKey",
            "type": "short-input",
            "title": "AWS Secret 访问权限键",
            "placeholder": "你的 secret 访问权限 key",
            "password": true,
            "required": true
        },
        {
            "id": "tableName",
            "type": "short-input",
            "title": "表名称",
            "placeholder": "my-table",
            "required": true
        },
        {
            "id": "keyConditionExpression",
            "type": "short-input",
            "title": "键 Condition Expression",
            "placeholder": "pk = :pk",
            "required": true
        },
        {
            "id": "filterExpression",
            "type": "short-input",
            "title": "筛选条件 Expression",
            "placeholder": "attribute_exists(电子邮件)"
        },
        {
            "id": "expressionAttributeNames",
            "type": "code",
            "title": "Expression Attribute Names (JSON)",
            "placeholder": "{\\n  \"#name\": \"名称\"\\n}",
            "language": "json"
        },
        {
            "id": "expressionAttributeValues",
            "type": "code",
            "title": "Expression Attribute 值 (JSON)",
            "placeholder": "{\\n  \":pk\": \"用户#123\",\\n  \":名称\": \"Jane\"\\n}",
            "language": "json"
        },
        {
            "id": "indexName",
            "type": "short-input",
            "title": "索引名称",
            "placeholder": "GSI1"
        },
        {
            "id": "limit",
            "type": "short-input",
            "title": "限制",
            "placeholder": "100"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">查询 (<code>dynamodb_query</code>)</div>

查询项目从一个 DynamoDB table 使用 key conditions

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `region` | 字符串 | 是 | AWS region \(例如, us-east-1\) |
| `accessKeyId` | 字符串 | 是 | AWS 访问权限 key ID |
| `secretAccessKey` | 字符串 | 是 | AWS secret 访问权限 key |
| `tableName` | 字符串 | 是 | DynamoDB table 名称 |
| `keyConditionExpression` | 字符串 | 是 | 键 condition expression \(例如, "pk = :pk"\) |
| `filterExpression` | 字符串 | 否 | 筛选条件 expression 用于结果 |
| `expressionAttributeNames` | 对象 | 否 | Attribute 名称 mappings 用于 reserved words |
| `expressionAttributeValues` | 对象 | 否 | Expression attribute values |
| `indexName` | 字符串 | 否 | Secondary index 名称 到 query |
| `limit` | 数字 | 否 | 最大数字的项目到 return |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作 status message |
| `items` | 数组 | 数组的项目 returned |

</div>



---

### 扫描

<ShowcaseCard>
  <BlockConfigPreview
    name="Amazon DynamoDB"
    type="dynamodb"
    color="#527FFF"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Item",
                    "id": "get"
                },
                {
                    "label": "Put Item",
                    "id": "put"
                },
                {
                    "label": "Query",
                    "id": "query"
                },
                {
                    "label": "Scan",
                    "id": "scan"
                },
                {
                    "label": "Update Item",
                    "id": "update"
                },
                {
                    "label": "Delete Item",
                    "id": "delete"
                }
            ],
            "defaultValue": "scan"
        },
        {
            "id": "region",
            "type": "short-input",
            "title": "AWS Region",
            "placeholder": "us-east-1",
            "required": true
        },
        {
            "id": "accessKeyId",
            "type": "short-input",
            "title": "AWS 访问权限键 ID",
            "placeholder": "AKIA...",
            "password": true,
            "required": true
        },
        {
            "id": "secretAccessKey",
            "type": "short-input",
            "title": "AWS Secret 访问权限键",
            "placeholder": "你的 secret 访问权限 key",
            "password": true,
            "required": true
        },
        {
            "id": "tableName",
            "type": "short-input",
            "title": "表名称",
            "placeholder": "my-table",
            "required": true
        },
        {
            "id": "filterExpression",
            "type": "short-input",
            "title": "筛选条件 Expression",
            "placeholder": "attribute_exists(电子邮件)"
        },
        {
            "id": "projectionExpression",
            "type": "short-input",
            "title": "Projection Expression",
            "placeholder": "pk, #名称, 电子邮件"
        },
        {
            "id": "expressionAttributeNames",
            "type": "code",
            "title": "Expression Attribute Names (JSON)",
            "placeholder": "{\\n  \"#name\": \"名称\"\\n}",
            "language": "json"
        },
        {
            "id": "expressionAttributeValues",
            "type": "code",
            "title": "Expression Attribute 值 (JSON)",
            "placeholder": "{\\n  \":status\": \"active\"\\n}",
            "language": "json"
        },
        {
            "id": "limit",
            "type": "short-input",
            "title": "限制",
            "placeholder": "100"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">扫描 (<code>dynamodb_scan</code>)</div>

扫描全部项目在一个 DynamoDB table

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `region` | 字符串 | 是 | AWS region \(例如, us-east-1\) |
| `accessKeyId` | 字符串 | 是 | AWS 访问权限 key ID |
| `secretAccessKey` | 字符串 | 是 | AWS secret 访问权限 key |
| `tableName` | 字符串 | 是 | DynamoDB table 名称 |
| `filterExpression` | 字符串 | 否 | 筛选条件 expression 用于结果 |
| `projectionExpression` | 字符串 | 否 | Attributes 到 retrieve |
| `expressionAttributeNames` | 对象 | 否 | Attribute 名称 mappings 用于 reserved words |
| `expressionAttributeValues` | 对象 | 否 | Expression attribute values |
| `limit` | 数字 | 否 | 最大数字的项目到 return |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作 status message |
| `items` | 数组 | 数组的项目 returned |

</div>



---

### 更新项目

<ShowcaseCard>
  <BlockConfigPreview
    name="Amazon DynamoDB"
    type="dynamodb"
    color="#527FFF"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Item",
                    "id": "get"
                },
                {
                    "label": "Put Item",
                    "id": "put"
                },
                {
                    "label": "Query",
                    "id": "query"
                },
                {
                    "label": "Scan",
                    "id": "scan"
                },
                {
                    "label": "Update Item",
                    "id": "update"
                },
                {
                    "label": "Delete Item",
                    "id": "delete"
                }
            ],
            "defaultValue": "update"
        },
        {
            "id": "region",
            "type": "short-input",
            "title": "AWS Region",
            "placeholder": "us-east-1",
            "required": true
        },
        {
            "id": "accessKeyId",
            "type": "short-input",
            "title": "AWS 访问权限键 ID",
            "placeholder": "AKIA...",
            "password": true,
            "required": true
        },
        {
            "id": "secretAccessKey",
            "type": "short-input",
            "title": "AWS Secret 访问权限键",
            "placeholder": "你的 secret 访问权限 key",
            "password": true,
            "required": true
        },
        {
            "id": "tableName",
            "type": "short-input",
            "title": "表名称",
            "placeholder": "my-table",
            "required": true
        },
        {
            "id": "key",
            "type": "code",
            "title": "键 (JSON)",
            "placeholder": "{\\n  \"pk\": \"用户#123\"\\n}",
            "language": "json",
            "required": true
        },
        {
            "id": "updateExpression",
            "type": "short-input",
            "title": "更新 Expression",
            "placeholder": "SET #名称 = :名称",
            "required": true
        },
        {
            "id": "expressionAttributeNames",
            "type": "code",
            "title": "Expression Attribute Names (JSON)",
            "placeholder": "{\\n  \"#name\": \"名称\"\\n}",
            "language": "json"
        },
        {
            "id": "expressionAttributeValues",
            "type": "code",
            "title": "Expression Attribute 值 (JSON)",
            "placeholder": "{\\n  \":名称\": \"Jane Doe\"\\n}",
            "language": "json"
        },
        {
            "id": "conditionExpression",
            "type": "short-input",
            "title": "Condition Expression",
            "placeholder": "attribute_exists(pk)"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">更新项目 (<code>dynamodb_update</code>)</div>

更新一个项目在一个 DynamoDB table

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `region` | 字符串 | 是 | AWS region \(例如, us-east-1\) |
| `accessKeyId` | 字符串 | 是 | AWS 访问权限 key ID |
| `secretAccessKey` | 字符串 | 是 | AWS secret 访问权限 key |
| `tableName` | 字符串 | 是 | DynamoDB table 名称 |
| `key` | 对象 | 是 | Primary key 的该项目到 update |
| `updateExpression` | 字符串 | 是 | 更新 expression \(例如, "SET #名称 = :名称"\) |
| `expressionAttributeNames` | 对象 | 否 | Attribute 名称 mappings 用于 reserved words |
| `expressionAttributeValues` | 对象 | 否 | Expression attribute values |
| `conditionExpression` | 字符串 | 否 | 更新成功必须满足的条件 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `message` | 字符串 | 操作 status message |

</div>



---

### 删除项目

<ShowcaseCard>
  <BlockConfigPreview
    name="Amazon DynamoDB"
    type="dynamodb"
    color="#527FFF"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Item",
                    "id": "get"
                },
                {
                    "label": "Put Item",
                    "id": "put"
                },
                {
                    "label": "Query",
                    "id": "query"
                },
                {
                    "label": "Scan",
                    "id": "scan"
                },
                {
                    "label": "Update Item",
                    "id": "update"
                },
                {
                    "label": "Delete Item",
                    "id": "delete"
                }
            ],
            "defaultValue": "delete"
        },
        {
            "id": "region",
            "type": "short-input",
            "title": "AWS Region",
            "placeholder": "us-east-1",
            "required": true
        },
        {
            "id": "accessKeyId",
            "type": "short-input",
            "title": "AWS 访问权限键 ID",
            "placeholder": "AKIA...",
            "password": true,
            "required": true
        },
        {
            "id": "secretAccessKey",
            "type": "short-input",
            "title": "AWS Secret 访问权限键",
            "placeholder": "你的 secret 访问权限 key",
            "password": true,
            "required": true
        },
        {
            "id": "tableName",
            "type": "short-input",
            "title": "表名称",
            "placeholder": "my-table",
            "required": true
        },
        {
            "id": "key",
            "type": "code",
            "title": "键 (JSON)",
            "placeholder": "{\\n  \"pk\": \"用户#123\"\\n}",
            "language": "json",
            "required": true
        },
        {
            "id": "conditionExpression",
            "type": "short-input",
            "title": "Condition Expression",
            "placeholder": "attribute_exists(pk)"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">删除项目 (<code>dynamodb_delete</code>)</div>

删除一个项目从一个 DynamoDB table

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `region` | 字符串 | 是 | AWS region \(例如, us-east-1\) |
| `accessKeyId` | 字符串 | 是 | AWS 访问权限 key ID |
| `secretAccessKey` | 字符串 | 是 | AWS secret 访问权限 key |
| `tableName` | 字符串 | 是 | DynamoDB table 名称 |
| `key` | 对象 | 是 | Primary key 的该项目到 delete |
| `conditionExpression` | 字符串 | 否 | 删除成功必须满足的条件 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

请参阅此操作的块输出。

</div>



---


## 用量说明

集成 Amazon DynamoDB 到工作流. Supports 获取, Put, 查询, 扫描, 更新, 和删除操作在 DynamoDB tables.



## 备注

- Category: `tools`
- Type: `dynamodb`
