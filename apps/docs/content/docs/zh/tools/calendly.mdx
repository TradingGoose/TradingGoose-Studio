---
title: Calendly
description: 管理 Calendly 日程和事件
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"
import { Card, Cards } from 'fumadocs-ui/components/card'

<BlockInfoCard
  type="calendly"
  color=""
/>

<Cards>
  <Card title="Use as a Trigger" href="/triggers/calendly">
    See 触发器 events, setup, 和 event payloads 用于 Calendly.
  </Card>
</Cards>


## 配置

### 获取当前用户

<ShowcaseCard>
  <BlockConfigPreview
    name="Calendly"
    type="calendly"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Current User",
                    "id": "calendly_get_current_user"
                },
                {
                    "label": "List Event Types",
                    "id": "calendly_list_event_types"
                },
                {
                    "label": "Get Event Type",
                    "id": "calendly_get_event_type"
                },
                {
                    "label": "List Scheduled Events",
                    "id": "calendly_list_scheduled_events"
                },
                {
                    "label": "Get Scheduled Event",
                    "id": "calendly_get_scheduled_event"
                },
                {
                    "label": "List Event Invitees",
                    "id": "calendly_list_event_invitees"
                },
                {
                    "label": "Cancel Event",
                    "id": "calendly_cancel_event"
                }
            ],
            "defaultValue": "calendly_get_current_user"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "个人访问令牌",
            "placeholder": "输入你的 Calendly 个人访问权限令牌",
            "password": true,
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取当前用户 (<code>calendly_get_current_user</code>)</div>

获取信息 关于 该 currently authenticated Calendly 用户

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Calendly 个人访问令牌 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `uri` | 字符串 | Canonical reference 到该 用户 |
| `name` | 字符串 | 用户完整 名称 |
| `slug` | 字符串 | 唯一标识符用于该 用户 在 URLs |
| `email` | 字符串 | 用户 电子邮件 address |
| `scheduling_url` | 字符串 | URL 到该 用户 |
| `timezone` | 字符串 | 用户时区 |
| `avatar_url` | 字符串 | URL 到 用户 avatar 图像 |
| `created_at` | 字符串 | ISO 时间戳何时 用户 was created |
| `updated_at` | 字符串 | ISO 时间戳何时 用户 was 最后一个 updated |

</div>



---

### 列出事件类型

<ShowcaseCard>
  <BlockConfigPreview
    name="Calendly"
    type="calendly"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Current User",
                    "id": "calendly_get_current_user"
                },
                {
                    "label": "List Event Types",
                    "id": "calendly_list_event_types"
                },
                {
                    "label": "Get Event Type",
                    "id": "calendly_get_event_type"
                },
                {
                    "label": "List Scheduled Events",
                    "id": "calendly_list_scheduled_events"
                },
                {
                    "label": "Get Scheduled Event",
                    "id": "calendly_get_scheduled_event"
                },
                {
                    "label": "List Event Invitees",
                    "id": "calendly_list_event_invitees"
                },
                {
                    "label": "Cancel Event",
                    "id": "calendly_cancel_event"
                }
            ],
            "defaultValue": "calendly_list_event_types"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "个人访问令牌",
            "placeholder": "输入你的 Calendly 个人访问权限令牌",
            "password": true,
            "required": true
        },
        {
            "id": "user",
            "type": "short-input",
            "title": "用户 URI",
            "placeholder": "按条件筛选 用户 URI"
        },
        {
            "id": "organization",
            "type": "short-input",
            "title": "组织 URI",
            "placeholder": "按条件筛选组织 URI (可选)"
        },
        {
            "id": "active",
            "type": "switch",
            "title": "活跃仅",
            "description": "何时已启用, 显示仅活跃 event 类型. 何时已禁用, 显示全部 event 类型."
        },
        {
            "id": "count",
            "type": "short-input",
            "title": "结果每页面",
            "placeholder": "数字的结果 (默认: 20, max: 100)",
            "max": 100
        },
        {
            "id": "pageToken",
            "type": "short-input",
            "title": "页面令牌",
            "placeholder": "令牌用于分页"
        },
        {
            "id": "sort",
            "type": "short-input",
            "title": "排序顺序",
            "placeholder": "例如, \"名称:asc\", \"start_time:desc\""
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">列出事件类型 (<code>calendly_list_event_types</code>)</div>

检索一个列表的全部 event 类型用于一个 用户 或组织

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Calendly 个人访问令牌 |
| `user` | 字符串 | 否 | 返回仅 event 类型该 belong 到此 用户 \(URI 格式\) |
| `organization` | 字符串 | 否 | 返回仅 event 类型该 belong 到此组织 \(URI 格式\) |
| `count` | 数字 | 否 | 数字的结果每页面 \(默认: 20, max: 100\) |
| `pageToken` | 字符串 | 否 | 页面令牌用于分页 |
| `sort` | 字符串 | 否 | 排序顺序用于结果 \(例如, "名称:asc", "名称:desc"\) |
| `active` | 布尔值 | 否 | 何时是, show 仅活跃 event 类型. 何时否或 unchecked, show 全部 event 类型 \(both 活跃和 inactive\). |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `uri` | 字符串 | Canonical reference 到该 event type |
| `name` | 字符串 | 事件 type 名称 |
| `active` | 布尔值 | 是否该 event type is 活跃 |
| `booking_method` | 字符串 | Booking 方法 \(例如,  |
| `color` | 字符串 | Hex 颜色 code |
| `created_at` | 字符串 | ISO 时间戳的 creation |
| `description_html` | 字符串 | HTML formatted 说明 |
| `description_plain` | 字符串 | 纯文本说明 |
| `duration` | 数字 | 时长在 minutes |
| `scheduling_url` | 字符串 | URL 到 scheduling 页面 |
| `slug` | 字符串 | 唯一标识符用于 URLs |
| `type` | 字符串 | 事件 type classification |

</div>



---

### 获取事件类型

<ShowcaseCard>
  <BlockConfigPreview
    name="Calendly"
    type="calendly"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Current User",
                    "id": "calendly_get_current_user"
                },
                {
                    "label": "List Event Types",
                    "id": "calendly_list_event_types"
                },
                {
                    "label": "Get Event Type",
                    "id": "calendly_get_event_type"
                },
                {
                    "label": "List Scheduled Events",
                    "id": "calendly_list_scheduled_events"
                },
                {
                    "label": "Get Scheduled Event",
                    "id": "calendly_get_scheduled_event"
                },
                {
                    "label": "List Event Invitees",
                    "id": "calendly_list_event_invitees"
                },
                {
                    "label": "Cancel Event",
                    "id": "calendly_cancel_event"
                }
            ],
            "defaultValue": "calendly_get_event_type"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "个人访问令牌",
            "placeholder": "输入你的 Calendly 个人访问权限令牌",
            "password": true,
            "required": true
        },
        {
            "id": "eventTypeUuid",
            "type": "short-input",
            "title": "事件类型 UUID",
            "placeholder": "输入 event type UUID 或 URI",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取事件类型 (<code>calendly_get_event_type</code>)</div>

获取 detailed 信息 关于 一个指定 event type

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Calendly 个人访问令牌 |
| `eventTypeUuid` | 字符串 | 是 | 事件 type UUID \(可以 be 完整 URI 或 just 该 UUID\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `uri` | 字符串 | 事件类型的规范引用 |
| `name` | 字符串 | 问题文本 |
| `active` | 布尔值 | 事件类型是否活跃 |
| `booking_method` | 字符串 | 预订方式 |
| `color` | 字符串 | 十六进制颜色代码 |
| `created_at` | 字符串 | 创建时间的 ISO 时间戳 |
| `type` | 字符串 | 问题类型 \(text、single_select、multi_select 等\) |
| `position` | 数字 | 问题顺序 |
| `enabled` | 布尔值 | 问题是否已启用 |
| `required` | 布尔值 | 是否问题为必填项 |
| `items` | 字符串 | 输出项目 |

</div>



---

### 列出已安排事件

<ShowcaseCard>
  <BlockConfigPreview
    name="Calendly"
    type="calendly"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Current User",
                    "id": "calendly_get_current_user"
                },
                {
                    "label": "List Event Types",
                    "id": "calendly_list_event_types"
                },
                {
                    "label": "Get Event Type",
                    "id": "calendly_get_event_type"
                },
                {
                    "label": "List Scheduled Events",
                    "id": "calendly_list_scheduled_events"
                },
                {
                    "label": "Get Scheduled Event",
                    "id": "calendly_get_scheduled_event"
                },
                {
                    "label": "List Event Invitees",
                    "id": "calendly_list_event_invitees"
                },
                {
                    "label": "Cancel Event",
                    "id": "calendly_cancel_event"
                }
            ],
            "defaultValue": "calendly_list_scheduled_events"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "个人访问令牌",
            "placeholder": "输入你的 Calendly 个人访问权限令牌",
            "password": true,
            "required": true
        },
        {
            "id": "user",
            "type": "short-input",
            "title": "用户 URI",
            "placeholder": "按条件筛选 用户 URI"
        },
        {
            "id": "organization",
            "type": "short-input",
            "title": "组织 URI",
            "placeholder": "按条件筛选组织 URI (可选)"
        },
        {
            "id": "invitee_email",
            "type": "short-input",
            "title": "Invitee 电子邮件",
            "placeholder": "按条件筛选 invitee 电子邮件"
        },
        {
            "id": "min_start_time",
            "type": "datetime-input",
            "title": "Min 开始时间",
            "placeholder": "例如, 2024-01-01 00:00:00"
        },
        {
            "id": "max_start_time",
            "type": "datetime-input",
            "title": "Max 开始时间",
            "placeholder": "2024-12-31 23:59:59"
        },
        {
            "id": "status",
            "type": "dropdown",
            "title": "状态",
            "options": [
                {
                    "label": "Active",
                    "id": "active"
                },
                {
                    "label": "Canceled",
                    "id": "canceled"
                }
            ]
        },
        {
            "id": "count",
            "type": "short-input",
            "title": "结果每页面",
            "placeholder": "数字的结果 (默认: 20, max: 100)",
            "max": 100
        },
        {
            "id": "pageToken",
            "type": "short-input",
            "title": "页面令牌",
            "placeholder": "令牌用于分页"
        },
        {
            "id": "sort",
            "type": "short-input",
            "title": "排序顺序",
            "placeholder": "例如, \"名称:asc\", \"start_time:desc\""
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">列出已安排事件 (<code>calendly_list_scheduled_events</code>)</div>

检索一个列表的已安排 events 用于一个 用户 或组织

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Calendly 个人访问令牌 |
| `user` | 字符串 | 否 | 返回 events 该 belong 到此 用户 \(URI 格式\). Either "用户" 或 "组织" 必须 be 提供的. |
| `organization` | 字符串 | 否 | 返回 events 该 belong 到此组织 \(URI 格式\). Either "用户" 或 "组织" 必须 be 提供的. |
| `invitee_email` | 字符串 | 否 | 返回 events 位置 invitee has 此 电子邮件 |
| `count` | 数字 | 否 | 数字的结果每页面 \(默认: 20, max: 100\) |
| `max_start_time` | 字符串 | 否 | 返回 events 使用开始时间之前此 time \(ISO 8601 格式\) |
| `min_start_time` | 字符串 | 否 | 返回 events 使用开始时间之后此 time \(ISO 8601 格式\) |
| `pageToken` | 字符串 | 否 | 页面令牌用于分页 |
| `sort` | 字符串 | 否 | 排序顺序用于结果 \(例如, "start_time:asc", "start_time:desc"\) |
| `status` | 字符串 | 否 | 按条件筛选 status \("活跃" 或 "canceled"\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `uri` | 字符串 | Canonical reference 到该 event |
| `name` | 字符串 | 事件 名称 |
| `status` | 字符串 | 事件 status \(活跃或 canceled\) |
| `start_time` | 字符串 | ISO 时间戳的 event start |
| `end_time` | 字符串 | ISO 时间戳的 event end |
| `event_type` | 字符串 | URI 的该 event type |
| `type` | 字符串 | 位置 type \(例如,  |
| `location` | 字符串 | 位置说明 |

</div>



---

### 获取已安排事件

<ShowcaseCard>
  <BlockConfigPreview
    name="Calendly"
    type="calendly"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Current User",
                    "id": "calendly_get_current_user"
                },
                {
                    "label": "List Event Types",
                    "id": "calendly_list_event_types"
                },
                {
                    "label": "Get Event Type",
                    "id": "calendly_get_event_type"
                },
                {
                    "label": "List Scheduled Events",
                    "id": "calendly_list_scheduled_events"
                },
                {
                    "label": "Get Scheduled Event",
                    "id": "calendly_get_scheduled_event"
                },
                {
                    "label": "List Event Invitees",
                    "id": "calendly_list_event_invitees"
                },
                {
                    "label": "Cancel Event",
                    "id": "calendly_cancel_event"
                }
            ],
            "defaultValue": "calendly_get_scheduled_event"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "个人访问令牌",
            "placeholder": "输入你的 Calendly 个人访问权限令牌",
            "password": true,
            "required": true
        },
        {
            "id": "eventUuid",
            "type": "short-input",
            "title": "事件 UUID",
            "placeholder": "输入已安排 event UUID 或 URI",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取已安排事件 (<code>calendly_get_scheduled_event</code>)</div>

获取 detailed 信息 关于 一个指定已安排 event

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Calendly 个人访问令牌 |
| `eventUuid` | 字符串 | 是 | 已安排 event UUID \(可以 be 完整 URI 或 just 该 UUID\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `uri` | 字符串 | Canonical reference 到该 event |
| `name` | 字符串 | 事件 名称 |
| `status` | 字符串 | 事件 status \(活跃或 canceled\) |
| `start_time` | 字符串 | ISO 时间戳的 event start |
| `end_time` | 字符串 | ISO 时间戳的 event end |
| `event_type` | 字符串 | URI 的该 event type |
| `type` | 字符串 | 位置 type |
| `location` | 字符串 | 位置说明 |

</div>



---

### 列出事件 Invitees

<ShowcaseCard>
  <BlockConfigPreview
    name="Calendly"
    type="calendly"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Current User",
                    "id": "calendly_get_current_user"
                },
                {
                    "label": "List Event Types",
                    "id": "calendly_list_event_types"
                },
                {
                    "label": "Get Event Type",
                    "id": "calendly_get_event_type"
                },
                {
                    "label": "List Scheduled Events",
                    "id": "calendly_list_scheduled_events"
                },
                {
                    "label": "Get Scheduled Event",
                    "id": "calendly_get_scheduled_event"
                },
                {
                    "label": "List Event Invitees",
                    "id": "calendly_list_event_invitees"
                },
                {
                    "label": "Cancel Event",
                    "id": "calendly_cancel_event"
                }
            ],
            "defaultValue": "calendly_list_event_invitees"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "个人访问令牌",
            "placeholder": "输入你的 Calendly 个人访问权限令牌",
            "password": true,
            "required": true
        },
        {
            "id": "status",
            "type": "dropdown",
            "title": "状态",
            "options": [
                {
                    "label": "Active",
                    "id": "active"
                },
                {
                    "label": "Canceled",
                    "id": "canceled"
                }
            ]
        },
        {
            "id": "eventUuid",
            "type": "short-input",
            "title": "事件 UUID",
            "placeholder": "输入已安排 event UUID 或 URI",
            "required": true
        },
        {
            "id": "email",
            "type": "short-input",
            "title": "电子邮件",
            "placeholder": "按条件筛选 invitee 电子邮件"
        },
        {
            "id": "count",
            "type": "short-input",
            "title": "结果每页面",
            "placeholder": "数字的结果 (默认: 20, max: 100)",
            "max": 100
        },
        {
            "id": "pageToken",
            "type": "short-input",
            "title": "页面令牌",
            "placeholder": "令牌用于分页"
        },
        {
            "id": "sort",
            "type": "short-input",
            "title": "排序顺序",
            "placeholder": "例如, \"名称:asc\", \"start_time:desc\""
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">列出事件 Invitees (<code>calendly_list_event_invitees</code>)</div>

检索一个列表的 invitees 用于一个已安排 event

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Calendly 个人访问令牌 |
| `eventUuid` | 字符串 | 是 | 已安排 event UUID \(可以 be 完整 URI 或 just 该 UUID\) |
| `count` | 数字 | 否 | 数字的结果每页面 \(默认: 20, max: 100\) |
| `email` | 字符串 | 否 | 筛选条件 invitees 按 电子邮件 address |
| `pageToken` | 字符串 | 否 | 页面令牌用于分页 |
| `sort` | 字符串 | 否 | 排序顺序用于结果 \(例如, "created_at:asc", "created_at:desc"\) |
| `status` | 字符串 | 否 | 按条件筛选 status \("活跃" 或 "canceled"\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `uri` | 字符串 | Canonical reference 到该 invitee |
| `email` | 字符串 | Invitee 电子邮件 address |
| `name` | 字符串 | Invitee 完整 名称 |
| `first_name` | 字符串 | Invitee 第一个 名称 |
| `last_name` | 字符串 | Invitee 最后一个 名称 |
| `status` | 字符串 | Invitee status \(活跃或 canceled\) |
| `question` | 字符串 | 问题 text |
| `answer` | 字符串 | Invitee 答案 |

</div>



---

### 取消事件

<ShowcaseCard>
  <BlockConfigPreview
    name="Calendly"
    type="calendly"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "options": [
                {
                    "label": "Get Current User",
                    "id": "calendly_get_current_user"
                },
                {
                    "label": "List Event Types",
                    "id": "calendly_list_event_types"
                },
                {
                    "label": "Get Event Type",
                    "id": "calendly_get_event_type"
                },
                {
                    "label": "List Scheduled Events",
                    "id": "calendly_list_scheduled_events"
                },
                {
                    "label": "Get Scheduled Event",
                    "id": "calendly_get_scheduled_event"
                },
                {
                    "label": "List Event Invitees",
                    "id": "calendly_list_event_invitees"
                },
                {
                    "label": "Cancel Event",
                    "id": "calendly_cancel_event"
                }
            ],
            "defaultValue": "calendly_cancel_event"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "个人访问令牌",
            "placeholder": "输入你的 Calendly 个人访问权限令牌",
            "password": true,
            "required": true
        },
        {
            "id": "eventUuid",
            "type": "short-input",
            "title": "事件 UUID",
            "placeholder": "输入已安排 event UUID 或 URI",
            "required": true
        },
        {
            "id": "reason",
            "type": "long-input",
            "title": "Cancellation 原因",
            "placeholder": "原因用于 cancellation (可选)"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">取消事件 (<code>calendly_cancel_event</code>)</div>

取消一个已安排 event

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `apiKey` | 字符串 | 是 | Calendly 个人访问令牌 |
| `eventUuid` | 字符串 | 是 | 已安排 event UUID 到 cancel \(可以 be 完整 URI 或 just 该 UUID\) |
| `reason` | 字符串 | 否 | 原因用于 cancellation \(将 be sent 到 invitees\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `canceler_type` | 字符串 | 类型的 canceler \(主机或 invitee\) |
| `canceled_by` | 字符串 | 名称的人员 who canceled |
| `reason` | 字符串 | Cancellation 原因 |

</div>



---


## 用量说明

集成 Calendly 到你的 workflow. 管理 event 类型, 已安排 events, invitees, 和 Webhook. 可以 also 触发器工作流根据 Calendly Webhook events (invitee 已安排, invitee canceled, routing 表单 submitted). Requires 个人访问令牌.



## 备注

- Category: `tools`
- Type: `calendly`
