---
title: Firecrawl
description: 抓取或搜索网页
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="firecrawl"
  color=""
/>


## 配置

### 抓取

<ShowcaseCard>
  <BlockConfigPreview
    name="Firecrawl"
    type="firecrawl"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "Scrape",
                    "id": "scrape"
                },
                {
                    "label": "Search",
                    "id": "search"
                },
                {
                    "label": "Crawl",
                    "id": "crawl"
                }
            ],
            "defaultValue": "scrape"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "layout": "full",
            "placeholder": "输入你的 Firecrawl API 密钥",
            "password": true,
            "required": true
        },
        {
            "id": "url",
            "type": "short-input",
            "title": "网站 URL",
            "layout": "full",
            "placeholder": "输入该网站 URL",
            "required": true
        },
        {
            "id": "onlyMainContent",
            "type": "switch",
            "title": "仅 Main 内容",
            "layout": "half"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">抓取 (<code>firecrawl_scrape</code>)</div>

提取 structured content 从 web pages 使用 comprehensive metadata support. Converts content 到 markdown 或 HTML while capturing SEO metadata, 打开 Graph 标签, 和页面信息.

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `url` | 字符串 | 是 | 该 URL 到抓取 content 从 |
| `scrapeOptions` | json | 否 | 选项用于 content scraping |
| `apiKey` | 字符串 | 是 | Firecrawl API 密钥 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `markdown` | 字符串 | 页面 content 在 markdown 格式 |
| `html` | 字符串 | 原始 HTML 内容的该页面 |

</div>



---

### 搜索

<ShowcaseCard>
  <BlockConfigPreview
    name="Firecrawl"
    type="firecrawl"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "Scrape",
                    "id": "scrape"
                },
                {
                    "label": "Search",
                    "id": "search"
                },
                {
                    "label": "Crawl",
                    "id": "crawl"
                }
            ],
            "defaultValue": "search"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "layout": "full",
            "placeholder": "输入你的 Firecrawl API 密钥",
            "password": true,
            "required": true
        },
        {
            "id": "query",
            "type": "short-input",
            "title": "搜索查询",
            "layout": "full",
            "placeholder": "输入该 search query",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">搜索 (<code>firecrawl_search</code>)</div>

搜索用于信息在该 web 使用 Firecrawl

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `query` | 字符串 | 是 | 该 search query 到使用 |
| `apiKey` | 字符串 | 是 | Firecrawl API 密钥 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `title` | 字符串 | title output |
| `description` | 字符串 | 说明 output |
| `url` | 字符串 | url output |
| `markdown` | 字符串 | markdown output |
| `html` | 字符串 | html output |
| `rawHtml` | 字符串 | rawHtml output |
| `links` | 数组 | 链接 output |
| `screenshot` | 字符串 | screenshot output |

</div>



---

### 抓取

<ShowcaseCard>
  <BlockConfigPreview
    name="Firecrawl"
    type="firecrawl"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "options": [
                {
                    "label": "Scrape",
                    "id": "scrape"
                },
                {
                    "label": "Search",
                    "id": "search"
                },
                {
                    "label": "Crawl",
                    "id": "crawl"
                }
            ],
            "defaultValue": "crawl"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API 密钥",
            "layout": "full",
            "placeholder": "输入你的 Firecrawl API 密钥",
            "password": true,
            "required": true
        },
        {
            "id": "url",
            "type": "short-input",
            "title": "网站 URL",
            "layout": "full",
            "placeholder": "输入该网站 URL",
            "required": true
        },
        {
            "id": "limit",
            "type": "short-input",
            "title": "页面限制",
            "layout": "half",
            "placeholder": "100"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">抓取 (<code>firecrawl_crawl</code>)</div>

抓取 entire websites 和提取 structured content 从全部 accessible pages

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `url` | 字符串 | 是 | 该网站 URL 到抓取 |
| `limit` | 数字 | 否 | 最大数字的 pages 到抓取 \(默认: 100\) |
| `onlyMainContent` | 布尔值 | 否 | 提取仅 main content 从 pages |
| `apiKey` | 字符串 | 是 | Firecrawl API 密钥 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `markdown` | 字符串 | 页面 content 在 markdown 格式 |
| `html` | 字符串 | 页面 HTML 内容 |
| `title` | 字符串 | 页面 title |
| `description` | 字符串 | 页面说明 |
| `language` | 字符串 | 页面语言 |
| `sourceURL` | 字符串 | 来源 URL 的该页面 |

</div>



---


## 用量说明

集成 Firecrawl 到该 workflow. 可以 search, 抓取, 或抓取 websites.



## 备注

- Category: `tools`
- Type: `firecrawl`
