---
title: Microsoft Teams
description: 读取、写入和创建消息
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"
import { Card, Cards } from 'fumadocs-ui/components/card'

<BlockInfoCard
  type="microsoft_teams"
  color="#5059C9"
/>

<Cards>
  <Card title="用作触发器" href="/triggers/microsoft-teams">
    查看 Microsoft Teams 的触发事件、设置和事件载荷。
  </Card>
</Cards>

## 配置

### 读取聊天消息

<ShowcaseCard>
  <BlockConfigPreview
    name="Microsoft Teams"
    type="microsoft_teams"
    color="#5059C9"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "defaultValue": "read_chat",
            "options": [
                {
                    "id": "read_chat",
                    "label": "读取聊天消息"
                },
                {
                    "id": "write_chat",
                    "label": "写入聊天消息"
                },
                {
                    "id": "read_channel",
                    "label": "读取频道消息"
                },
                {
                    "id": "write_channel",
                    "label": "写入频道消息"
                }
            ]
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Microsoft 账户",
            "layout": "full",
            "placeholder": "选择 Microsoft 账户",
            "provider": "microsoft-teams",
            "required": true
        },
        {
            "id": "chatId",
            "type": "file-selector",
            "title": "选择聊天",
            "layout": "full",
            "placeholder": "选择一个聊天",
            "provider": "microsoft-teams"
        },
        {
            "id": "manualChatId",
            "type": "short-input",
            "title": "聊天 ID",
            "layout": "full",
            "placeholder": "输入聊天 ID"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">读取聊天消息 (<code>microsoft_teams_read_chat</code>)</div>

从 Microsoft Teams 聊天中读取内容

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `chatId` | string | 是 | 要读取的聊天 ID |
| `includeAttachments` | boolean | 否 | 下载消息附件（托管内容）并纳入存储 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `success` | boolean | Teams 聊天读取操作是否成功 |
| `messageCount` | number | 从聊天检索到的消息数量 |
| `chatId` | string | 读取消息所在聊天的 ID |
| `messages` | array | 聊天消息对象数组 |
| `attachmentCount` | number | 找到的附件总数 |
| `attachmentTypes` | array | 找到的附件类型 |
| `content` | string | 聊天消息的格式化内容 |
| `attachments` | file[] | 为方便使用而上传的附件（已展平） |

</div>

---

### 写入聊天消息

<ShowcaseCard>
  <BlockConfigPreview
    name="Microsoft Teams"
    type="microsoft_teams"
    color="#5059C9"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "defaultValue": "write_chat",
            "options": [
                {
                    "id": "read_chat",
                    "label": "读取聊天消息"
                },
                {
                    "id": "write_chat",
                    "label": "写入聊天消息"
                },
                {
                    "id": "read_channel",
                    "label": "读取频道消息"
                },
                {
                    "id": "write_channel",
                    "label": "写入频道消息"
                }
            ]
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Microsoft 账户",
            "layout": "full",
            "placeholder": "选择 Microsoft 账户",
            "provider": "microsoft-teams",
            "required": true
        },
        {
            "id": "chatId",
            "type": "file-selector",
            "title": "选择聊天",
            "layout": "full",
            "placeholder": "选择聊天",
            "provider": "microsoft-teams"
        },
        {
            "id": "manualChatId",
            "type": "short-input",
            "title": "聊天 ID",
            "layout": "full",
            "placeholder": "输入聊天 ID"
        },
        {
            "id": "content",
            "type": "long-input",
            "title": "消息",
            "layout": "full",
            "placeholder": "输入消息内容",
            "required": true
        },
        {
            "id": "attachmentFiles",
            "type": "file-upload",
            "title": "附件",
            "layout": "full",
            "placeholder": "上传要附加的文件",
            "required": false
        },
        {
            "id": "files",
            "type": "short-input",
            "title": "文件附件",
            "layout": "full",
            "placeholder": "引用先前块中的文件",
            "required": false
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">写入聊天消息（<code>microsoft_teams_write_chat</code>）</div>

在 Microsoft Teams 聊天中写入或更新内容

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `chatId` | string | 是 | 要写入的聊天 ID |
| `content` | string | 是 | 要写入消息的内容 |
| `files` | file[] | 否 | 要附加到消息的文件 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `success` | boolean | Teams 聊天消息发送成功状态 |
| `messageId` | string | 已发送消息的唯一标识符 |
| `chatId` | string | 消息发送所在聊天的 ID |
| `createdTime` | string | 消息创建时间戳 |
| `url` | string | 消息的 Web URL |
| `updatedContent` | boolean | 内容是否已成功更新 |

</div>

---

### 读取频道消息

<ShowcaseCard>
  <BlockConfigPreview
    name="Microsoft Teams"
    type="microsoft_teams"
    color="#5059C9"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "defaultValue": "read_channel",
            "options": [
                {
                    "id": "read_chat",
                    "label": "读取聊天消息"
                },
                {
                    "id": "write_chat",
                    "label": "写入聊天消息"
                },
                {
                    "id": "read_channel",
                    "label": "读取频道消息"
                },
                {
                    "id": "write_channel",
                    "label": "写入频道消息"
                }
            ]
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Microsoft 帐户",
            "layout": "full",
            "placeholder": "选择 Microsoft 帐户",
            "provider": "microsoft-teams",
            "required": true
        },
        {
            "id": "teamId",
            "type": "file-selector",
            "title": "选择团队",
            "layout": "full",
            "placeholder": "选择团队",
            "provider": "microsoft-teams"
        },
        {
            "id": "manualTeamId",
            "type": "short-input",
            "title": "团队 ID",
            "layout": "full",
            "placeholder": "输入团队 ID"
        },
        {
            "id": "channelId",
            "type": "file-selector",
            "title": "选择频道",
            "layout": "full",
            "placeholder": "选择频道",
            "provider": "microsoft-teams"
        },
        {
            "id": "manualChannelId",
            "type": "short-input",
            "title": "频道 ID",
            "layout": "full",
            "placeholder": "输入频道 ID"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">读取频道消息（<code>microsoft_teams_read_channel</code>）</div>

读取 Microsoft Teams 频道中的内容

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `teamId` | string | 是 | 要读取的团队 ID |
| `channelId` | string | 是 | 要读取的频道 ID |
| `includeAttachments` | boolean | 否 | 下载消息附件（托管内容）并纳入存储 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `success` | boolean | Teams 频道读取操作是否成功 |
| `messageCount` | number | 从频道获取的消息数量 |
| `teamId` | string | 已读取的团队 ID |
| `channelId` | string | 已读取的频道 ID |
| `messages` | array | 频道消息对象数组 |
| `attachmentCount` | number | 找到的附件总数 |
| `attachmentTypes` | array | 找到的附件类型 |
| `content` | string | 频道消息的格式化内容 |
| `attachments` | file[] | 为便于使用而上传的附件（扁平化） |

</div>

---

### 发送频道消息

<ShowcaseCard>
  <BlockConfigPreview
    name="Microsoft Teams"
    type="microsoft_teams"
    color="#5059C9"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "layout": "full",
            "defaultValue": "write_channel",
            "options": [
                {
                    "id": "read_chat",
                    "label": "读取聊天消息"
                },
                {
                    "id": "write_chat",
                    "label": "发送聊天消息"
                },
                {
                    "id": "read_channel",
                    "label": "读取频道消息"
                },
                {
                    "id": "write_channel",
                    "label": "发送频道消息"
                }
            ]
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "微软账户",
            "layout": "full",
            "placeholder": "选择微软账户",
            "provider": "microsoft-teams",
            "required": true
        },
        {
            "id": "teamId",
            "type": "file-selector",
            "title": "选择团队",
            "layout": "full",
            "placeholder": "选择团队",
            "provider": "microsoft-teams"
        },
        {
            "id": "manualTeamId",
            "type": "short-input",
            "title": "团队 ID",
            "layout": "full",
            "placeholder": "输入团队 ID"
        },
        {
            "id": "channelId",
            "type": "file-selector",
            "title": "选择频道",
            "layout": "full",
            "placeholder": "选择频道",
            "provider": "microsoft-teams"
        },
        {
            "id": "manualChannelId",
            "type": "short-input",
            "title": "频道 ID",
            "layout": "full",
            "placeholder": "输入频道 ID"
        },
        {
            "id": "content",
            "type": "long-input",
            "title": "消息",
            "layout": "full",
            "placeholder": "输入消息内容",
            "required": true
        },
        {
            "id": "attachmentFiles",
            "type": "file-upload",
            "title": "附件",
            "layout": "full",
            "placeholder": "上传要附加的文件",
            "required": false
        },
        {
            "id": "files",
            "type": "short-input",
            "title": "文件附件",
            "layout": "full",
            "placeholder": "引用上一模块中的文件",
            "required": false
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">写入频道消息（<code>microsoft_teams_write_channel</code>）</div>

向 Microsoft Teams 频道写入或发送消息

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `teamId` | string | 是 | 要写入的团队 ID |
| `channelId` | string | 是 | 要写入的频道 ID |
| `content` | string | 是 | 要写入频道的内容 |
| `files` | file[] | 否 | 要附加到消息的文件 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `success` | boolean | Teams 频道消息发送成功状态 |
| `messageId` | string | 已发送消息的唯一标识符 |
| `teamId` | string | 消息发送所在团队的 ID |
| `channelId` | string | 消息发送所在频道的 ID |
| `createdTime` | string | 消息创建时间戳 |
| `url` | string | 消息的网页 URL |
| `updatedContent` | boolean | 内容是否已成功更新 |

</div>

---

## 使用说明

将 Microsoft Teams 集成到工作流中。可读写聊天消息，也可读写频道消息。可用于触发模式，在聊天或频道收到消息时触发工作流。

## 备注

- 类别：`tools`
- 类型：`microsoft_teams`
