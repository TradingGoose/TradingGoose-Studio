---
title: ServiceNow
description: 创建、读取、更新和删除 ServiceNow 记录
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"

<BlockInfoCard
  type="servicenow"
  color="#62D84E"
/>

## 配置

### 创建记录

<ShowcaseCard>
  <BlockConfigPreview
    name="ServiceNow"
    type="servicenow"
    color="#62D84E"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "defaultValue": "servicenow_create_record",
            "options": [
                {
                    "id": "servicenow_create_record",
                    "label": "创建记录"
                },
                {
                    "id": "servicenow_read_record",
                    "label": "读取记录"
                },
                {
                    "id": "servicenow_update_record",
                    "label": "更新记录"
                },
                {
                    "id": "servicenow_delete_record",
                    "label": "删除记录"
                }
            ]
        },
        {
            "id": "instanceUrl",
            "type": "short-input",
            "title": "实例 URL",
            "placeholder": "https://instance.service-now.com",
            "description": "你的 ServiceNow 实例 URL（例如 https://yourcompany.service-now.com）",
            "required": true
        },
        {
            "id": "username",
            "type": "short-input",
            "title": "用户名",
            "placeholder": "输入你的 ServiceNow 用户名",
            "description": "具有 Web 服务访问权限的 ServiceNow 用户",
            "required": true
        },
        {
            "id": "password",
            "type": "short-input",
            "title": "密码",
            "placeholder": "输入你的 ServiceNow 密码",
            "description": "ServiceNow 用户的密码",
            "password": true,
            "required": true
        },
        {
            "id": "tableName",
            "type": "short-input",
            "title": "表名称",
            "placeholder": "incident, task, sys_user, etc.",
            "description": "ServiceNow 表名称",
            "required": true
        },
        {
            "id": "fields",
            "type": "code",
            "title": "字段（JSON）",
            "placeholder": "{\n  \"short_description\": \"问题描述\",\n  \"priority\": \"1\"\n}",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">创建记录 (<code>servicenow_create_record</code>)</div>

在 ServiceNow 表中创建新记录

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `instanceUrl` | string | 是 | ServiceNow 实例 URL \(例如 https://instance.service-now.com\) |
| `username` | string | 是 | ServiceNow 用户名 |
| `password` | string | 是 | ServiceNow 密码 |
| `tableName` | string | 是 | 表名称 \(例如 incident、task、sys_user\) |
| `fields` | json | 是 | 要在记录上设置的字段 \(JSON 对象\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `record` | json | 已创建的 ServiceNow 记录，包含 sys_id 及其他字段 |
| `metadata` | json | 操作元数据 |

</div>

---

### 读取记录

<ShowcaseCard>
  <BlockConfigPreview
    name="ServiceNow"
    type="servicenow"
    color="#62D84E"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "defaultValue": "servicenow_read_record",
            "options": [
                {
                    "id": "servicenow_create_record",
                    "label": "创建记录"
                },
                {
                    "id": "servicenow_read_record",
                    "label": "读取记录"
                },
                {
                    "id": "servicenow_update_record",
                    "label": "更新记录"
                },
                {
                    "id": "servicenow_delete_record",
                    "label": "删除记录"
                }
            ]
        },
        {
            "id": "instanceUrl",
            "type": "short-input",
            "title": "实例 URL",
            "placeholder": "https://instance.service-now.com",
            "description": "你的 ServiceNow 实例 URL（例如 https://yourcompany.service-now.com）",
            "required": true
        },
        {
            "id": "username",
            "type": "short-input",
            "title": "用户名",
            "placeholder": "输入你的 ServiceNow 用户名",
            "description": "具有 Web 服务访问权限的 ServiceNow 用户",
            "required": true
        },
        {
            "id": "password",
            "type": "short-input",
            "title": "密码",
            "placeholder": "输入你的 ServiceNow 密码",
            "description": "ServiceNow 用户的密码",
            "password": true,
            "required": true
        },
        {
            "id": "tableName",
            "type": "short-input",
            "title": "表名",
            "placeholder": "incident, task, sys_user, etc.",
            "description": "ServiceNow 表名",
            "required": true
        },
        {
            "id": "sysId",
            "type": "short-input",
            "title": "记录 sys_id",
            "placeholder": "特定记录的 sys_id（可选）"
        },
        {
            "id": "number",
            "type": "short-input",
            "title": "记录编号",
            "placeholder": "例如 INC0010001（可选）"
        },
        {
            "id": "query",
            "type": "short-input",
            "title": "查询字符串",
            "placeholder": "active=true^priority=1",
            "description": "ServiceNow 编码查询字符串"
        },
        {
            "id": "limit",
            "type": "short-input",
            "title": "限制",
            "placeholder": "10"
        },
        {
            "id": "fields",
            "type": "short-input",
            "title": "返回字段",
            "placeholder": "number,short_description,priority",
            "description": "逗号分隔的字段列表"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">读取记录（<code>servicenow_read_record</code>）</div>

从 ServiceNow 表中读取记录

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `instanceUrl` | string | 是 | ServiceNow 实例 URL（例如：https://instance.service-now.com） |
| `username` | string | 是 | ServiceNow 用户名 |
| `password` | string | 是 | ServiceNow 密码 |
| `tableName` | string | 是 | 表名称 |
| `sysId` | string | 否 | 特定记录的 sys_id |
| `number` | string | 否 | 记录编号（例如：INC0010001） |
| `query` | string | 否 | 编码查询字符串（例如："active=true^priority=1"） |
| `limit` | number | 否 | 返回记录的最大数量 |
| `fields` | string | 否 | 要返回的字段列表，以逗号分隔 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `records` | array | ServiceNow 记录数组 |
| `metadata` | json | 操作元数据 |

</div>

---

### 更新记录

<ShowcaseCard>
  <BlockConfigPreview
    name="ServiceNow"
    type="servicenow"
    color="#62D84E"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "defaultValue": "servicenow_update_record",
            "options": [
                {
                    "id": "servicenow_create_record",
                    "label": "创建记录"
                },
                {
                    "id": "servicenow_read_record",
                    "label": "读取记录"
                },
                {
                    "id": "servicenow_update_record",
                    "label": "更新记录"
                },
                {
                    "id": "servicenow_delete_record",
                    "label": "删除记录"
                }
            ]
        },
        {
            "id": "instanceUrl",
            "type": "short-input",
            "title": "实例 URL",
            "placeholder": "https://instance.service-now.com",
            "description": "你的 ServiceNow 实例 URL（例如：https://yourcompany.service-now.com）",
            "required": true
        },
        {
            "id": "username",
            "type": "short-input",
            "title": "用户名",
            "placeholder": "输入你的 ServiceNow 用户名",
            "description": "具有 Web 服务访问权限的 ServiceNow 用户",
            "required": true
        },
        {
            "id": "password",
            "type": "short-input",
            "title": "密码",
            "placeholder": "输入你的 ServiceNow 密码",
            "description": "该 ServiceNow 用户的密码",
            "password": true,
            "required": true
        },
        {
            "id": "tableName",
            "type": "short-input",
            "title": "表名称",
            "placeholder": "incident, task, sys_user 等",
            "description": "ServiceNow 表名称",
            "required": true
        },
        {
            "id": "sysId",
            "type": "short-input",
            "title": "记录 sys_id",
            "placeholder": "要更新的记录 sys_id",
            "required": true
        },
        {
            "id": "fields",
            "type": "code",
            "title": "要更新的字段（JSON）",
            "placeholder": "{\n  \"state\": \"2\",\n  \"assigned_to\": \"user.sys_id\"\n}",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">更新记录（<code>servicenow_update_record</code>）</div>

更新 ServiceNow 表中的现有记录

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `instanceUrl` | string | 是 | ServiceNow 实例 URL（例如：https://instance.service-now.com） |
| `username` | string | 是 | ServiceNow 用户名 |
| `password` | string | 是 | ServiceNow 密码 |
| `tableName` | string | 是 | 表名称 |
| `sysId` | string | 是 | 要更新的记录 sys_id |
| `fields` | json | 是 | 要更新的字段（JSON 对象） |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `record` | json | 已更新的 ServiceNow 记录 |
| `metadata` | json | 操作元数据 |

</div>

---

### 删除记录

<ShowcaseCard>
  <BlockConfigPreview
    name="ServiceNow"
    type="servicenow"
    color="#62D84E"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "defaultValue": "servicenow_delete_record",
            "options": [
                {
                    "id": "servicenow_create_record",
                    "label": "创建记录"
                },
                {
                    "id": "servicenow_read_record",
                    "label": "读取记录"
                },
                {
                    "id": "servicenow_update_record",
                    "label": "更新记录"
                },
                {
                    "id": "servicenow_delete_record",
                    "label": "删除记录"
                }
            ]
        },
        {
            "id": "instanceUrl",
            "type": "short-input",
            "title": "实例 URL",
            "placeholder": "https://instance.service-now.com",
            "description": "你的 ServiceNow 实例 URL（例如：https://yourcompany.service-now.com）",
            "required": true
        },
        {
            "id": "username",
            "type": "short-input",
            "title": "用户名",
            "placeholder": "输入 ServiceNow 用户名",
            "description": "具有 Web 服务访问权限的 ServiceNow 用户",
            "required": true
        },
        {
            "id": "password",
            "type": "short-input",
            "title": "密码",
            "placeholder": "输入 ServiceNow 密码",
            "description": "ServiceNow 用户的密码",
            "password": true,
            "required": true
        },
        {
            "id": "tableName",
            "type": "short-input",
            "title": "表名称",
            "placeholder": "incident, task, sys_user, etc.",
            "description": "ServiceNow 表名称",
            "required": true
        },
        {
            "id": "sysId",
            "type": "short-input",
            "title": "记录 sys_id",
            "placeholder": "要删除的记录 sys_id",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">删除记录（<code>servicenow_delete_record</code>）</div>

从 ServiceNow 表中删除一条记录

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `instanceUrl` | string | 是 | ServiceNow 实例 URL\（例如：https://instance.service-now.com\） |
| `username` | string | 是 | ServiceNow 用户名 |
| `password` | string | 是 | ServiceNow 密码 |
| `tableName` | string | 是 | 表名 |
| `sysId` | string | 是 | 要删除的记录 sys_id |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `success` | boolean | 删除是否成功 |
| `metadata` | json | 操作元数据 |

</div>

---

## 使用说明

将 ServiceNow 集成到你的工作流中。可在任意 ServiceNow 表中创建、读取、更新和删除记录，包括事件、任务、变更请求、用户等。

## 备注

- 类别：`tools`
- 类型：`servicenow`
