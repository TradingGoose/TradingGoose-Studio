---
title: Trello
description: 管理 Trello 看板和卡片
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"

<BlockInfoCard
  type="trello"
  color="#0052CC"
/>

## 配置

### 获取列表

<ShowcaseCard>
  <BlockConfigPreview
    name="Trello"
    type="trello"
    color="#0052CC"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "defaultValue": "trello_list_lists",
            "options": [
                {
                    "id": "trello_list_lists",
                    "label": "获取列表"
                },
                {
                    "id": "trello_list_cards",
                    "label": "列出卡片"
                },
                {
                    "id": "trello_create_card",
                    "label": "创建卡片"
                },
                {
                    "id": "trello_update_card",
                    "label": "更新卡片"
                },
                {
                    "id": "trello_get_actions",
                    "label": "获取操作"
                },
                {
                    "id": "trello_add_comment",
                    "label": "添加评论"
                }
            ]
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Trello 账号",
            "placeholder": "选择 Trello 账号",
            "provider": "trello",
            "required": true
        },
        {
            "id": "boardId",
            "type": "short-input",
            "title": "看板",
            "placeholder": "输入看板 ID",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取列表 (<code>trello_list_lists</code>)</div>

列出 Trello 看板上的所有列表

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `boardId` | string | 是 | 要列出列表的看板 ID |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `lists` | array | 列表对象数组，包含 id、name、closed、pos 和 idBoard |
| `count` | number | 返回的列表数量 |

</div>

---

### 列出卡片

<ShowcaseCard>
  <BlockConfigPreview
    name="Trello"
    type="trello"
    color="#0052CC"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "defaultValue": "trello_list_cards",
            "options": [
                {
                    "id": "trello_list_lists",
                    "label": "获取列表"
                },
                {
                    "id": "trello_list_cards",
                    "label": "列出卡片"
                },
                {
                    "id": "trello_create_card",
                    "label": "创建卡片"
                },
                {
                    "id": "trello_update_card",
                    "label": "更新卡片"
                },
                {
                    "id": "trello_get_actions",
                    "label": "获取操作"
                },
                {
                    "id": "trello_add_comment",
                    "label": "添加评论"
                }
            ]
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Trello 账号",
            "placeholder": "选择 Trello 账号",
            "provider": "trello",
            "required": true
        },
        {
            "id": "boardId",
            "type": "short-input",
            "title": "看板",
            "placeholder": "输入看板 ID 或搜索看板",
            "required": true
        },
        {
            "id": "listId",
            "type": "short-input",
            "title": "列表（可选）",
            "placeholder": "输入列表 ID 以按列表筛选卡片"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">列出卡片（<code>trello_list_cards</code>）</div>

列出 Trello 看板上的所有卡片

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `boardId` | string | 是 | 要从中列出卡片的看板 ID |
| `listId` | string | 否 | 可选：按列表 ID 筛选卡片 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `cards` | array | 卡片对象数组，包含 id、name、desc、url、看板/列表 ID、标签和截止日期 |
| `count` | number | 返回的卡片数量 |

</div>

---

### 创建卡片

<ShowcaseCard>
  <BlockConfigPreview
    name="Trello"
    type="trello"
    color="#0052CC"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "defaultValue": "trello_create_card",
            "options": [
                {
                    "id": "trello_list_lists",
                    "label": "获取列表"
                },
                {
                    "id": "trello_list_cards",
                    "label": "列出卡片"
                },
                {
                    "id": "trello_create_card",
                    "label": "创建卡片"
                },
                {
                    "id": "trello_update_card",
                    "label": "更新卡片"
                },
                {
                    "id": "trello_get_actions",
                    "label": "获取动态"
                },
                {
                    "id": "trello_add_comment",
                    "label": "添加评论"
                }
            ]
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Trello 账号",
            "placeholder": "选择 Trello 账号",
            "provider": "trello",
            "required": true
        },
        {
            "id": "boardId",
            "type": "short-input",
            "title": "看板",
            "placeholder": "输入看板 ID 或搜索看板",
            "required": true
        },
        {
            "id": "listId",
            "type": "short-input",
            "title": "列表",
            "placeholder": "输入列表 ID 或搜索列表",
            "required": true
        },
        {
            "id": "name",
            "type": "short-input",
            "title": "卡片名称",
            "placeholder": "输入卡片名称/标题",
            "required": true
        },
        {
            "id": "desc",
            "type": "long-input",
            "title": "描述",
            "placeholder": "输入卡片描述（可选）"
        },
        {
            "id": "pos",
            "type": "dropdown",
            "title": "位置",
            "options": [
                {
                    "id": "top",
                    "label": "顶部"
                },
                {
                    "id": "bottom",
                    "label": "底部"
                }
            ]
        },
        {
            "id": "due",
            "type": "datetime-input",
            "title": "截止日期",
            "placeholder": "YYYY-MM-DD 或 YYYY-MM-DDTHH:mm:ssZ"
        },
        {
            "id": "labels",
            "type": "short-input",
            "title": "标签",
            "placeholder": "逗号分隔的标签 ID（可选）"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">创建卡片 (<code>trello_create_card</code>)</div>

在 Trello 看板上创建新卡片

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `boardId` | string | 是 | 要创建卡片的看板 ID |
| `listId` | string | 是 | 要创建卡片的列表 ID |
| `name` | string | 是 | 卡片名称/标题 |
| `desc` | string | 否 | 卡片描述 |
| `pos` | string | 否 | 卡片位置 \(top、bottom 或正浮点数\) |
| `due` | string | 否 | 截止日期 \(ISO 8601 格式\) |
| `labels` | string | 否 | 以逗号分隔的标签 ID 列表 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `card` | object | 已创建的卡片对象，包含 id、name、desc、url 及其他属性 |

</div>

---

### 更新卡片

<ShowcaseCard>
  <BlockConfigPreview
    name="Trello"
    type="trello"
    color="#0052CC"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "defaultValue": "trello_update_card",
            "options": [
                {
                    "id": "trello_list_lists",
                    "label": "获取列表"
                },
                {
                    "id": "trello_list_cards",
                    "label": "列出卡片"
                },
                {
                    "id": "trello_create_card",
                    "label": "创建卡片"
                },
                {
                    "id": "trello_update_card",
                    "label": "更新卡片"
                },
                {
                    "id": "trello_get_actions",
                    "label": "获取操作"
                },
                {
                    "id": "trello_add_comment",
                    "label": "添加评论"
                }
            ]
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Trello 账号",
            "placeholder": "选择 Trello 账号",
            "provider": "trello",
            "required": true
        },
        {
            "id": "cardId",
            "type": "short-input",
            "title": "卡片",
            "placeholder": "输入卡片 ID 或搜索卡片",
            "required": true
        },
        {
            "id": "name",
            "type": "short-input",
            "title": "新卡片名称",
            "placeholder": "输入新卡片名称（留空则保持当前名称）"
        },
        {
            "id": "desc",
            "type": "long-input",
            "title": "新描述",
            "placeholder": "输入新描述（留空则保持当前描述）"
        },
        {
            "id": "closed",
            "type": "switch",
            "title": "归档卡片"
        },
        {
            "id": "dueComplete",
            "type": "switch",
            "title": "将截止日期标为完成"
        },
        {
            "id": "idList",
            "type": "short-input",
            "title": "移动到列表",
            "placeholder": "输入要移入的列表 ID"
        },
        {
            "id": "due",
            "type": "datetime-input",
            "title": "截止日期",
            "placeholder": "YYYY-MM-DD or YYYY-MM-DDTHH:mm:ssZ"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">更新卡片 (<code>trello_update_card</code>)</div>

更新 Trello 上的现有卡片

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `cardId` | string | 是 | 要更新的卡片 ID |
| `name` | string | 否 | 卡片的新名称/标题 |
| `desc` | string | 否 | 卡片的新描述 |
| `closed` | boolean | 否 | 归档/关闭卡片 \(true\) 或重新打开 \(false\) |
| `idList` | string | 否 | 将卡片移至其他列表 |
| `due` | string | 否 | 截止日期 \(ISO 8601 格式\) |
| `dueComplete` | boolean | 否 | 将截止日期标记为已完成 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `card` | object | 更新后的卡片对象，包含 id、name、desc、url 及其他属性 |

</div>

---

### 获取操作

<ShowcaseCard>
  <BlockConfigPreview
    name="Trello"
    type="trello"
    color="#0052CC"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "defaultValue": "trello_get_actions",
            "options": [
                {
                    "id": "trello_list_lists",
                    "label": "获取列表"
                },
                {
                    "id": "trello_list_cards",
                    "label": "列出卡片"
                },
                {
                    "id": "trello_create_card",
                    "label": "创建卡片"
                },
                {
                    "id": "trello_update_card",
                    "label": "更新卡片"
                },
                {
                    "id": "trello_get_actions",
                    "label": "获取操作"
                },
                {
                    "id": "trello_add_comment",
                    "label": "添加评论"
                }
            ]
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Trello 账号",
            "placeholder": "选择 Trello 账号",
            "provider": "trello",
            "required": true
        },
        {
            "id": "boardId",
            "type": "short-input",
            "title": "看板 ID",
            "placeholder": "输入看板 ID 以获取看板操作"
        },
        {
            "id": "cardId",
            "type": "short-input",
            "title": "卡片 ID",
            "placeholder": "输入卡片 ID 以获取卡片操作"
        },
        {
            "id": "filter",
            "type": "short-input",
            "title": "操作筛选",
            "placeholder": "例如：commentCard,updateCard"
        },
        {
            "id": "limit",
            "type": "short-input",
            "title": "限制",
            "placeholder": "50"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">获取操作 (<code>trello_get_actions</code>)</div>

从看板或卡片获取动态/操作

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `boardId` | string | 否 | 要从中获取操作的看板 ID（必须提供 boardId 或 cardId 之一） |
| `cardId` | string | 否 | 要从中获取操作的卡片 ID（必须提供 boardId 或 cardId 之一） |
| `filter` | string | 否 | 按类型筛选操作（例如 "commentCard,updateCard,createCard" 或 "all"） |
| `limit` | number | 否 | 返回的操作数量上限（默认：50，最大：1000） |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `actions` | array | 包含 type、date、member 和 data 的操作对象数组 |
| `count` | number | 返回的操作数量 |

</div>

---

### 添加评论

<ShowcaseCard>
  <BlockConfigPreview
    name="Trello"
    type="trello"
    color="#0052CC"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "操作",
            "defaultValue": "trello_add_comment",
            "options": [
                {
                    "id": "trello_list_lists",
                    "label": "获取列表"
                },
                {
                    "id": "trello_list_cards",
                    "label": "列出卡片"
                },
                {
                    "id": "trello_create_card",
                    "label": "创建卡片"
                },
                {
                    "id": "trello_update_card",
                    "label": "更新卡片"
                },
                {
                    "id": "trello_get_actions",
                    "label": "获取操作"
                },
                {
                    "id": "trello_add_comment",
                    "label": "添加评论"
                }
            ]
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Trello 账号",
            "placeholder": "选择 Trello 账号",
            "provider": "trello",
            "required": true
        },
        {
            "id": "cardId",
            "type": "short-input",
            "title": "卡片",
            "placeholder": "输入卡片 ID 或搜索卡片",
            "required": true
        },
        {
            "id": "text",
            "type": "long-input",
            "title": "评论",
            "placeholder": "输入评论",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">添加评论 (<code>trello_add_comment</code>)</div>

向 Trello 卡片添加评论

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输入</div>

| 参数 | 类型 | 必填 | 说明 |
| --------- | ---- | -------- | ----------- |
| `cardId` | string | 是 | 要添加评论的卡片 ID |
| `text` | string | 是 | 评论内容 |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">输出</div>

| 参数 | 类型 | 说明 |
| --------- | ---- | ----------- |
| `comment` | object | 已创建的评论对象，包含 id、text、date 以及创建成员 |

</div>

---

## 使用说明

与 Trello 集成以管理看板和卡片。列出看板、列出卡片、创建卡片、更新卡片、获取动态并添加评论。

## 备注

- 分类：`tools`
- 类型：`trello`
