---
title: API 参考
description: TradingGoose 中 PineTS 函数的完整参考
---

import { Callout } from 'fumadocs-ui/components/callout'

本页按命名空间整理了 TradingGoose 支持的全部 PineTS 函数。内容基于 PineTS 运行时类型定义。

## input（13 个函数）

| 函数 | 返回值 | 说明 |
|----------|---------|-------------|
| `input.int(defval, title, min?, max?, step?)` | `number` | 整数输入 |
| `input.float(defval, title, min?, max?, step?)` | `number` | 浮点数输入 |
| `input.bool(defval, title)` | `boolean` | 布尔值输入 |
| `input.string(defval, title)` | `string` | 字符串输入 |
| `input.color(defval, title)` | `string` | 颜色选择器 |
| `input.source(defval, title)` | `series` | 数据源选择器 |
| `input.timeframe(defval, title)` | `string` | 时间周期选择器 |
| `input.price(defval, title)` | `number` | 价格输入 |
| `input.time(defval, title)` | `number` | 时间戳输入 |
| `input.session(defval, title)` | `string` | 交易时段输入 |
| `input.symbol(defval, title)` | `string` | 交易品种输入 |
| `input.text_area(defval, title)` | `string` | 多行文本 |
| `input.enum(defval, title, options)` | `T` | 下拉选择器 |

## ta（57 个函数）

### 移动平均线

| 函数 | 说明 |
|----------|-------------|
| `ta.alma(source, length, offset?, sigma?)` | Arnaud Legoux 移动平均线 |
| `ta.ema(source, length)` | 指数移动平均线 |
| `ta.hma(source, length)` | Hull 移动平均线 |
| `ta.linreg(source, length, offset?)` | 线性回归 |
| `ta.rma(source, length)` | 平滑移动平均线 |
| `ta.sma(source, length)` | 简单移动平均线 |
| `ta.swma(source)` | 对称加权移动平均线 |
| `ta.vwap` / `ta.vwap(source)` | 成交量加权平均价 |
| `ta.vwma(source, length)` | 成交量加权移动平均线 |
| `ta.wma(source, length)` | 加权移动平均线 |

### 震荡指标

| 函数 | 说明 |
|----------|-------------|
| `ta.cci(source, length)` | 商品通道指数 |
| `ta.change(source, length?)` | 价格变化 |
| `ta.cmo(source, length)` | 钱德动量摆动指标 |
| `ta.cog(source, length)` | 重心指标 |
| `ta.macd(source, fast, slow, signal)` | MACD → `[macd, signal, hist]` |
| `ta.mfi(series, length)` | 资金流量指标 |
| `ta.mom(source, length)` | 动量 |
| `ta.roc(source, length)` | 变动率 |
| `ta.rsi(source, length)` | 相对强弱指数 |
| `ta.stoch(source, high, low, length)` | 随机指标 |
| `ta.tsi(source, short, long)` | 真实强弱指数 |
| `ta.wpr(length)` | Williams %R |

### 波动率

| 函数 | 说明 |
|----------|-------------|
| `ta.atr(length)` | 平均真实波幅 |
| `ta.bb(source, length, mult)` | 布林带 → `[mid, upper, lower]` |
| `ta.bbw(source, length, mult)` | 布林带宽度 |
| `ta.dev(source, length)` | 平均绝对偏差 |
| `ta.kc(source, length, mult)` | Keltner 通道 → `[mid, upper, lower]` |
| `ta.kcw(source, length, mult)` | Keltner 通道宽度 |
| `ta.range(source, length)` | 区间 |
| `ta.stdev(source, length)` | 标准差 |
| `ta.tr` / `ta.tr(handleNa?)` | 真实波幅 |
| `ta.variance(source, length)` | 方差 |

### 成交量（变量）

| 变量 | 说明 |
|----------|-------------|
| `ta.accdist` | 累积/派发线 |
| `ta.iii` | 日内强度指数 |
| `ta.nvi` | 负成交量指数 |
| `ta.obv` | 能量潮 |
| `ta.pvi` | 正成交量指数 |
| `ta.pvt` | 价量趋势 |
| `ta.wad` | Williams 累积/派发指标 |
| `ta.wvad` | Williams 可变累积/派发指标 |

### 趋势

| 函数 | 说明 |
|----------|-------------|
| `ta.cross(a, b)` | 任意方向交叉 |
| `ta.crossover(a, b)` | 向上交叉 |
| `ta.crossunder(a, b)` | 向下交叉 |
| `ta.dmi(diLength, adxSmoothing)` | DMI → `[plus, minus, adx]` |
| `ta.falling(source, length)` | 检测下降 |
| `ta.rising(source, length)` | 检测上升 |
| `ta.sar(start, inc, max)` | 抛物线 SAR |
| `ta.supertrend(factor, atrLength)` | SuperTrend → `[value, direction]` |

### 统计

| 函数 | 说明 |
|----------|-------------|
| `ta.barssince(condition)` | 条件成立后经过的 K 线数 |
| `ta.correlation(a, b, length)` | 相关系数 |
| `ta.cum(source)` | 累计和 |
| `ta.highest(source, length)` | N 根 K 线内的最高值 |
| `ta.highestbars(source, length)` | 距最高值的 K 线数 |
| `ta.lowest(source, length)` | N 根 K 线内的最低值 |
| `ta.lowestbars(source, length)` | 距最低值的 K 线数 |
| `ta.max(a, b)` | 最大值 |
| `ta.median(source, length)` | 中位数 |
| `ta.min(a, b)` | 最小值 |
| `ta.mode(source, length)` | 众数 |
| `ta.percentile_linear_interpolation(source, len, pct)` | 百分位数（线性插值） |
| `ta.percentile_nearest_rank(source, len, pct)` | 百分位数（最邻近秩） |
| `ta.percentrank(source, length)` | 百分位排名 |
| `ta.pivot_point_levels(type, anchor)` | 枢轴点位 |
| `ta.pivothigh(source, left, right)` | 枢轴高点 |
| `ta.pivotlow(source, left, right)` | 枢轴低点 |
| `ta.rci(source, length)` | 秩相关指数 |
| `ta.valuewhen(condition, source, occurrence)` | 条件成立时的值 |

## math（28 个函数 + 4 个常量）

| 函数/常量 | 说明 |
|---------------------|-------------|
| `math.pi`, `math.e`, `math.phi`, `math.rphi` | 数学常量 |
| `math.abs(x)` | 绝对值 |
| `math.ceil(x)` | 向上取整 |
| `math.floor(x)` | 向下取整 |
| `math.round(x, precision?)` | 四舍五入 |
| `math.round_to_mintick(x)` | 按最小变动价位取整 |
| `math.sign(x)` | 符号 |
| `math.max(a, b)` | 最大值 |
| `math.min(a, b)` | 最小值 |
| `math.avg(...)` | 平均值 |
| `math.sum(source, length)` | 求和 |
| `math.pow(base, exp)` | 幂 |
| `math.sqrt(x)` | 平方根 |
| `math.exp(x)` | 指数 |
| `math.log(x)` | 自然对数 |
| `math.log10(x)` | 以 10 为底的对数 |
| `math.sin(x)`, `math.cos(x)`, `math.tan(x)` | 三角函数 |
| `math.asin(x)`, `math.acos(x)`, `math.atan(x)` | 反三角函数 |
| `math.todegrees(x)`, `math.toradians(x)` | 角度转换 |
| `math.random(min?, max?)` | 随机数 |

## plot（10 个函数）

| 函数 | 说明 |
|----------|-------------|
| `plot(series, title?, options?)` | 线形图、面积图或直方图 |
| `plotshape(series, title?, options?)` | 形状标记 |
| `plotchar(series, title?, char?, options?)` | 字符标记 |
| `plotarrow(series, title?, options?)` | 箭头标记 |
| `plotbar(o, h, l, c, title?, options?)` | OHLC 条形图 |
| `plotcandle(o, h, l, c, title?, options?)` | K 线图 |
| `hline(price, title?, options?)` | 水平线 |
| `fill(plot1, plot2, color?)` | 填充图形之间的区域 |
| `bgcolor(color, offset?, options?)` | 背景颜色 |
| `barcolor(color, offset?, options?)` | K 线颜色 |

## array（52 个方法）

| 类别 | 函数 |
|----------|-----------|
| **创建** | `from`, `new_float`, `new_int`, `new_bool`, `new_string`, `copy` |
| **访问** | `get`, `set`, `first`, `last` |
| **修改** | `push`, `pop`, `shift`, `unshift`, `insert`, `remove`, `clear`, `fill`, `reverse` |
| **形状** | `size`, `slice`, `concat` |
| **搜索** | `indexof`, `lastindexof`, `includes`, `binary_search`, `binary_search_leftmost`, `binary_search_rightmost`, `every`, `some` |
| **排序** | `sort`, `sort_indices` |
| **统计** | `sum`, `avg`, `min`, `max`, `median`, `mode`, `stdev`, `variance`, `covariance`, `range`, `percentrank`, `percentile_linear_interpolation`, `percentile_nearest_rank` |
| **转换** | `abs`, `join`, `standardize` |

## map（11 个方法）

`new`, `get`, `put`, `put_all`, `contains`, `remove`, `keys`, `values`, `size`, `clear`, `copy`

## matrix（50 个方法）

| 类别 | 函数 |
|----------|-----------|
| **访问** | `get`, `set`, `row`, `col` |
| **大小** | `rows`, `columns`, `elements_count`, `reshape`, `submatrix` |
| **修改** | `add_row`, `add_col`, `remove_row`, `remove_col`, `swap_rows`, `swap_columns`, `fill`, `reverse`, `concat`, `copy` |
| **算术** | `mult`, `pow`, `diff`, `kron` |
| **线性代数** | `det`, `inv`, `pinv`, `eigenvalues`, `eigenvectors`, `rank`, `trace`, `transpose` |
| **统计** | `sum`, `avg`, `min`, `max`, `median`, `mode`, `stdev`, `variance` |
| **布尔值** | `is_square`, `is_identity`, `is_diagonal`, `is_antidiagonal`, `is_triangular`, `is_symmetric`, `is_antisymmetric`, `is_stochastic`, `is_binary`, `is_zero` |
| **排序** | `sort` |

## str（19 个方法）

`length`, `contains`, `startswith`, `endswith`, `pos`, `match`, `substring`, `split`, `replace`, `replace_all`, `lower`, `upper`, `trim`, `repeat`, `format`, `tonumber`, `tostring`

## log（3 个方法）

`log.info(msg)`, `log.warning(msg)`, `log.error(msg)`

## request（2 个函数）

| 函数 | 说明 |
|----------|-------------|
| `request.security(symbol, timeframe, expression)` | 多时间周期数据 |
| `request.security_lower_tf(expression)` | 较低时间周期数据 |

## trigger（TradingGoose 扩展）

```
trigger(event: string, options: { condition, input, signal, position?, color? })
```

完整说明请参阅[指标触发器](/indicators/triggers)。

<Callout type="info">
  本参考涵盖 TradingGoose 的 PineTS 运行时中可用的函数。PineTS 基于开源的 [PineTS 项目](https://github.com/nicholasgasior/PineTS)，并支持 Pine Script v5/v6 API 的一部分。目前不支持 `strategy.*`、`label.*`、`line.*`、`box.*` 和 `table.*` 等功能。
</Callout>
