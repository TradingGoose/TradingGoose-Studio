---
title: 绘图
description: 使用绘图函数、形状、填充和颜色呈现指标数据
---

import { Callout } from 'fumadocs-ui/components/callout'

## 绘图函数

| 函数 | 说明 |
|----------|-------------|
| `plot(series, title?, options?)` | 绘制线形图、面积图、直方图或其他序列样式 |
| `plotshape(series, title?, options?)` | 绘制形状标记（箭头、圆形、菱形等） |
| `plotchar(series, title?, char?, options?)` | 绘制字符标记 |
| `plotarrow(series, title?, options?)` | 绘制箭头标记 |
| `plotbar(open, high, low, close, title?, options?)` | 绘制 OHLC 条形图 |
| `plotcandle(open, high, low, close, title?, options?)` | 绘制 K 线图 |
| `hline(price, title?, options?)` | 在固定价格水平绘制水平线 |
| `fill(plot1, plot2, color?)` | 填充两个图形之间的区域 |
| `bgcolor(color, offset?, options?)` | 设置 K 线的背景颜色 |
| `barcolor(color, offset?, options?)` | 设置 K 线颜色 |

## 绘图样式

使用 `plot.style_*` 常量控制数据的呈现方式：

| 样式 | 说明 |
|-------|-------------|
| `plot.style_line` | 连续线（默认） |
| `plot.style_linebr` | 在 `na` 值处中断的线 |
| `plot.style_stepline` | 阶梯线 |
| `plot.style_stepline_diamond` | 带菱形标记的阶梯线 |
| `plot.style_steplinebr` | 带中断的阶梯线 |
| `plot.style_area` | 线下方的填充区域 |
| `plot.style_areabr` | 在 `na` 值处中断的区域 |
| `plot.style_histogram` | 垂直直方图柱 |
| `plot.style_columns` | 柱形图 |
| `plot.style_circles` | 圆形标记 |
| `plot.style_cross` | 十字标记 |

### 线条样式（用于 `plot` 和 `hline`）

| 样式 | 说明 |
|-------|-------------|
| `plot.linestyle_solid` | 实线 |
| `plot.linestyle_dashed` | 虚线 |
| `plot.linestyle_dotted` | 点线 |
| `hline.style_solid` | 水平实线 |
| `hline.style_dashed` | 水平虚线 |
| `hline.style_dotted` | 水平点线 |

## 示例

### 基本线形图

```javascript
indicator('EMA Crossover', { overlay: true });

const fast = ta.ema(close, 9);
const slow = ta.ema(close, 21);

plot(fast, 'Fast EMA', { color: '#2962FF' });
plot(slow, 'Slow EMA', { color: '#FF6D00' });
```

### 直方图

```javascript
indicator('MACD Histogram');

const [macdLine, signalLine, hist] = ta.macd(close, 12, 26, 9);

plot(hist, 'Histogram', {
  style: plot.style_histogram,
  color: hist > 0 ? '#26A69A' : '#EF5350'
});
plot(macdLine, 'MACD', { color: '#2962FF' });
plot(signalLine, 'Signal', { color: '#FF6D00' });
```

### 图形间填充

```javascript
indicator('Bollinger Bands', { overlay: true });

const [mid, upper, lower] = ta.bb(close, 20, 2);

const p1 = plot(upper, 'Upper', { color: '#F23645' });
plot(mid, 'Middle', { color: '#787B86' });
const p2 = plot(lower, 'Lower', { color: '#089981' });

fill(p1, p2);
```

### 形状标记

```javascript
// Plot buy/sell arrows at crossover points
if (ta.crossover(fast, slow)) {
  plotshape(close, 'Buy', {
    style: shape.triangleup,
    location: location.belowbar,
    color: '#00FF00',
    size: size.small
  });
}

if (ta.crossunder(fast, slow)) {
  plotshape(close, 'Sell', {
    style: shape.triangledown,
    location: location.abovebar,
    color: '#FF0000',
    size: size.small
  });
}
```

### 形状常量

| 常量 | 说明 |
|----------|-------------|
| `shape.triangleup` | 向上三角形 |
| `shape.triangledown` | 向下三角形 |
| `shape.circle` | 圆形 |
| `shape.cross` | 十字 |
| `shape.xcross` | X 形十字 |
| `shape.diamond` | 菱形 |
| `shape.square` | 正方形 |
| `shape.arrowup` | 向上箭头 |
| `shape.arrowdown` | 向下箭头 |
| `shape.flag` | 旗帜 |
| `shape.labelup` | 向上标签 |
| `shape.labeldown` | 向下标签 |

### 位置常量

| 常量 | 说明 |
|----------|-------------|
| `location.abovebar` | K 线上方 |
| `location.belowbar` | K 线下方 |
| `location.top` | 窗格顶部 |
| `location.bottom` | 窗格底部 |
| `location.absolute` | 精确值处 |

### 大小常量

| 常量 | 说明 |
|----------|-------------|
| `size.tiny` | 极小 |
| `size.small` | 小 |
| `size.normal` | 正常 |
| `size.large` | 大 |
| `size.huge` | 极大 |
| `size.auto` | 自动 |

### 背景和 K 线颜色

```javascript
// Highlight bars based on condition
bgcolor(ta.crossover(fast, slow) ? '#00FF0020' : na);
barcolor(close > open ? '#26A69A' : '#EF5350');
```

<Callout type="info">
  颜色可以指定为十六进制字符串（例如 `'#FF0000'`），也可以使用 `color` 命名空间（例如 `color.red`）。追加两位十六进制数字可设置 Alpha 透明度（例如，`'#FF000080'` 表示 50% 不透明度）。
</Callout>
