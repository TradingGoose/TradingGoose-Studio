---
title: 数据结构
description: 指标脚本中的数组、映射、矩阵和字符串操作
---

PineTS 提供四个数据结构命名空间，用于在指标中处理复杂数据。

## Array（52 个方法）

数组是有序的元素集合。您可以创建、修改和查询数组，以实现高级指标逻辑。

### 创建

```javascript
const a = array.from(1, 2, 3, 4, 5);
const b = array.new_float(10, 0.0);    // 10 elements, all 0.0
const c = array.new_int(5, 0);
const d = array.new_bool(3, false);
const e = array.new_string(2, '');
```

### 访问和修改

| 函数 | 说明 |
|----------|-------------|
| `array.get(arr, index)` | 获取指定索引处的元素 |
| `array.set(arr, index, value)` | 设置指定索引处的元素 |
| `array.first(arr)` | 获取第一个元素 |
| `array.last(arr)` | 获取最后一个元素 |
| `array.push(arr, value)` | 在末尾添加元素 |
| `array.pop(arr)` | 删除并返回最后一个元素 |
| `array.unshift(arr, value)` | 在开头添加元素 |
| `array.shift(arr)` | 删除并返回第一个元素 |
| `array.insert(arr, index, value)` | 在指定索引处插入元素 |
| `array.remove(arr, index)` | 删除指定索引处的元素 |
| `array.clear(arr)` | 删除所有元素 |

### 大小和转换

| 函数 | 说明 |
|----------|-------------|
| `array.size(arr)` | 元素数量 |
| `array.slice(arr, start, end)` | 提取子数组 |
| `array.concat(arr1, arr2)` | 连接数组 |
| `array.copy(arr)` | 浅拷贝 |
| `array.fill(arr, value, start?, end?)` | 使用指定值填充 |
| `array.reverse(arr)` | 原地反转 |
| `array.sort(arr, order?)` | 原地排序 |
| `array.sort_indices(arr, order?)` | 返回排序后的索引 |
| `array.join(arr, separator)` | 连接为字符串 |

### 搜索

| 函数 | 说明 |
|----------|-------------|
| `array.indexof(arr, value)` | 值首次出现的索引 |
| `array.lastindexof(arr, value)` | 值最后出现的索引 |
| `array.includes(arr, value)` | 检查是否包含指定值 |
| `array.binary_search(arr, value)` | 二分查找（已排序数组） |
| `array.every(arr, fn)` | 所有元素都符合断言时为真 |
| `array.some(arr, fn)` | 任一元素符合断言时为真 |

### 统计

| 函数 | 说明 |
|----------|-------------|
| `array.sum(arr)` | 求和 |
| `array.avg(arr)` | 平均值 |
| `array.min(arr)` | 最小值 |
| `array.max(arr)` | 最大值 |
| `array.median(arr)` | 中位数 |
| `array.mode(arr)` | 众数 |
| `array.stdev(arr)` | 标准差 |
| `array.variance(arr)` | 方差 |
| `array.covariance(arr1, arr2)` | 协方差 |
| `array.range(arr)` | 极差（最大值 - 最小值） |
| `array.percentrank(arr, value)` | 百分位排名 |

---

## Map（11 个方法）

用于关联数据的键值字典：

```javascript
const m = map.new();
map.put(m, 'ema', ta.ema(close, 20));
map.put(m, 'sma', ta.sma(close, 20));

const emaValue = map.get(m, 'ema');
const hasKey = map.contains(m, 'ema');     // true
const allKeys = map.keys(m);               // ['ema', 'sma']
```

| 函数 | 说明 |
|----------|-------------|
| `map.new()` | 创建空映射 |
| `map.get(m, key)` | 获取值 |
| `map.put(m, key, value)` | 设置值 |
| `map.put_all(m, other)` | 从另一个映射复制全部内容 |
| `map.contains(m, key)` | 检查键是否存在 |
| `map.remove(m, key)` | 删除键 |
| `map.keys(m)` | 所有键 |
| `map.values(m)` | 所有值 |
| `map.size(m)` | 元素数量 |
| `map.clear(m)` | 删除全部内容 |
| `map.copy(m)` | 复制映射 |

---

## Matrix（50 个方法）

用于高级数学运算的二维数据结构：

```javascript
const m = matrix.new(3, 3, 0);  // 3x3 matrix of zeros
matrix.set(m, 0, 0, 1);
matrix.set(m, 1, 1, 1);
matrix.set(m, 2, 2, 1);         // Identity matrix
```

### 主要操作

| 类别 | 函数 |
|----------|-----------|
| **访问** | `get`, `set`, `row`, `col` |
| **大小** | `rows`, `columns`, `elements_count` |
| **修改** | `add_row`, `add_col`, `remove_row`, `remove_col`, `swap_rows`, `swap_columns`, `fill`, `reverse` |
| **转换** | `transpose`, `reshape`, `slice`, `submatrix`, `concat`, `copy` |
| **算术** | `mult`, `pow`, `diff` |
| **线性代数** | `det`, `inv`, `pinv`, `eigenvalues`, `eigenvectors`, `rank`, `trace` |
| **统计** | `sum`, `avg`, `min`, `max`, `median`, `mode`, `stdev`, `variance` |
| **布尔测试** | `is_square`, `is_identity`, `is_diagonal`, `is_symmetric`, `is_triangular`, `is_zero`, `is_binary`, `is_stochastic` |

---

## String（19 个方法）

用于标签、格式设置和文本处理的字符串操作：

| 函数 | 说明 |
|----------|-------------|
| `str.length(s)` | 字符串长度 |
| `str.contains(s, substr)` | 检查是否包含子字符串 |
| `str.startswith(s, prefix)` | 检查前缀 |
| `str.endswith(s, suffix)` | 检查后缀 |
| `str.pos(s, substr)` | 查找子字符串的位置 |
| `str.match(s, pattern)` | 正则表达式匹配 |
| `str.substring(s, start, end)` | 提取子字符串 |
| `str.split(s, delimiter)` | 拆分为数组 |
| `str.replace(s, old, new)` | 替换首次出现的内容 |
| `str.replace_all(s, old, new)` | 替换所有匹配内容 |
| `str.lower(s)` | 转换为小写 |
| `str.upper(s)` | 转换为大写 |
| `str.trim(s)` | 删除空白字符 |
| `str.repeat(s, count)` | 重复字符串 |
| `str.format(fmt, ...)` | 格式化字符串 |
| `str.tonumber(s)` | 解析为数字 |
| `str.tostring(value)` | 转换为字符串 |

---

## Log（3 个方法）

用于在开发期间调试指标的日志记录：

```javascript
log.info('RSI value: ' + str.tostring(rsi));
log.warning('Volume spike detected');
log.error('Invalid input configuration');
```
