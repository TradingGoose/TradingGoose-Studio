---
title: Chat Trigger
description: Start a workflow from a chat deployment
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"
import { Callout } from 'fumadocs-ui/components/callout'

<BlockInfoCard
  type="chat_trigger"
  color=""
  icon={true}
/>

The Chat trigger creates a conversational interface for your workflow. Deploy your workflow as a chat and users can interact with it through a shareable URL. Each message starts a new workflow execution.

<Callout type="info">
  This is a **deployment-based** trigger. Users interact with your workflow through a hosted chat URL.
</Callout>

## Configuration

<ShowcaseCard>
  <BlockConfigPreview
    name="Chat Trigger"
    type="chat_trigger"
    hideHeader
    subBlocks={[
      { "id": "identifier", "title": "Identifier", "type": "short-input", "required": true, "placeholder": "company-name", "description": "Public path segment used for the published chat URL" },
      { "id": "title", "title": "Chat Title", "type": "short-input", "required": true, "placeholder": "Customer Support Assistant" },
      { "id": "description", "title": "Description", "type": "long-input", "placeholder": "A brief description of what this chat does" },
      { "id": "selectedOutputBlocks", "title": "Chat Output", "type": "checkbox-list", "required": true, "options": [], "description": "Select which workflow outputs are returned to the user" },
      { "id": "authType", "title": "Access Control", "type": "dropdown", "defaultValue": "public", "options": [{ "label": "Public Access", "id": "public" }, { "label": "Password Protected", "id": "password" }, { "label": "Email Access", "id": "email" }, { "label": "SSO Access", "id": "sso" }] },
      { "id": "welcomeMessage", "title": "Welcome Message", "type": "long-input", "placeholder": "Hi! How can I help you today?" },
      { "id": "imageUrl", "title": "Chat Logo", "type": "short-input", "placeholder": "URL to logo image" }
    ]}
  />
</ShowcaseCard>

Select at least one **Chat Output** before publishing. This required setting determines which block outputs the chat returns; publication fails if it is empty. Run the workflow in the editor to verify the selected outputs. For Password access, configure a password; Email and SSO access require an allowed-email list.

### Output Schema

| Field | Type | Description |
|-------|------|-------------|
| `input` | string | Latest user message |
| `conversationId` | string | Conversation thread ID |
| `files` | files | Optional uploaded files |

### File Properties

| Property | Type | Description |
|----------|------|-------------|
| `<chat.files[0].url>` | string | Signed download URL |
| `<chat.files[0].name>` | string | Original filename |
| `<chat.files[0].size>` | number | File size in bytes |
| `<chat.files[0].type>` | string | MIME type |

<Callout>
Each conversation maintains its own thread. Users can have multiple concurrent conversations.
</Callout>
