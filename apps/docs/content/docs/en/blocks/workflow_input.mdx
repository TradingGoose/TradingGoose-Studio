---
title: Workflow
description: Execute another workflow and map variables to its Input Form Trigger schema.
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="workflow_input"
  color="#6366F1"
/>


## Configuration

<ShowcaseCard>
  <BlockConfigPreview
    name="Workflow"
    type="workflow_input"
    color="#6366F1"
    hideHeader
    subBlocks={[
        {
            "id": "workflowId",
            "type": "dropdown",
            "title": "Select Workflow",
            "required": true
        },
        {
            "id": "inputMapping",
            "type": "input-mapping",
            "title": "Input Mapping",
            "layout": "full",
            "description": "Map fields defined in the child workflow's Input Trigger to variables/values in this workflow."
        }
    ]}
  />
</ShowcaseCard>

## Input

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `workflowId` | string | Yes | ID of the child workflow |
| `inputMapping` | json | No | Mapping of input fields to values |

## Output

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `success` | boolean | Execution success status |
| `childWorkflowName` | string | Child workflow name |
| `result` | json | Workflow execution result |
| `childTraceSpans` | array | Trace spans from the child execution. |


## Usage Instructions

Select a child workflow and map values to its Input Form Trigger fields. The child runs with the mapped input and its own workflow variables. Deployed parent executions select the deployed child version; live parent executions use the live child. The parent waits for the child, then exposes its final output under result. Child failures fail this block rather than returning success: false; connect an error path if recovery is required. Recursive workflow nesting is limited to 10 levels.



## Notes

- Category: `blocks`
- Type: `workflow_input`
