---
title: Memory
description: Add memory store
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="memory"
  color="#F64F9E"
/>


## Configuration

### Add Memory

<ShowcaseCard>
  <BlockConfigPreview
    name="Memory"
    type="memory"
    color="#F64F9E"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "full",
            "placeholder": "Select operation",
            "defaultValue": "add",
            "options": [
                {
                    "id": "add",
                    "label": "Add Memory"
                },
                {
                    "id": "getAll",
                    "label": "Get All Memories"
                },
                {
                    "id": "get",
                    "label": "Get Memory"
                },
                {
                    "id": "delete",
                    "label": "Delete Memory"
                }
            ]
        },
        {
            "id": "id",
            "type": "short-input",
            "title": "ID",
            "layout": "full",
            "placeholder": "Enter memory identifier",
            "required": true
        },
        {
            "id": "role",
            "type": "dropdown",
            "title": "Role",
            "layout": "full",
            "placeholder": "Select agent role",
            "required": true,
            "options": [
                {
                    "id": "user",
                    "label": "User"
                },
                {
                    "id": "assistant",
                    "label": "Assistant"
                },
                {
                    "id": "system",
                    "label": "System"
                }
            ]
        },
        {
            "id": "content",
            "type": "short-input",
            "title": "Content",
            "layout": "full",
            "placeholder": "Enter message content",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Add Memory (<code>memory_add</code>)</div>

Add a new memory to the database or append to existing memory with the same ID.

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `id` | string | Yes | Conversation identifier within the current workflow. Reusing the ID appends the message to that conversation. |
| `role` | string | Yes | Role for agent memory \(user, assistant, or system\) |
| `content` | string | Yes | Content for agent memory |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `memories` | array | Array of memory objects including the new or updated memory |

</div>



---

### Get All Memories

<ShowcaseCard>
  <BlockConfigPreview
    name="Memory"
    type="memory"
    color="#F64F9E"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "full",
            "placeholder": "Select operation",
            "defaultValue": "getAll",
            "options": [
                {
                    "id": "add",
                    "label": "Add Memory"
                },
                {
                    "id": "getAll",
                    "label": "Get All Memories"
                },
                {
                    "id": "get",
                    "label": "Get Memory"
                },
                {
                    "id": "delete",
                    "label": "Delete Memory"
                }
            ]
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Get All Memories (<code>memory_get_all</code>)</div>

Retrieve up to 50 conversations stored in the current workflow.

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

This operation has no direct parameters.

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `memories` | array | Up to 50 conversation objects with keys, types, and message data |
| `message` | string | Success or error message |

</div>



---

### Get Memory

<ShowcaseCard>
  <BlockConfigPreview
    name="Memory"
    type="memory"
    color="#F64F9E"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "full",
            "placeholder": "Select operation",
            "defaultValue": "get",
            "options": [
                {
                    "id": "add",
                    "label": "Add Memory"
                },
                {
                    "id": "getAll",
                    "label": "Get All Memories"
                },
                {
                    "id": "get",
                    "label": "Get Memory"
                },
                {
                    "id": "delete",
                    "label": "Delete Memory"
                }
            ]
        },
        {
            "id": "id",
            "type": "short-input",
            "title": "ID",
            "layout": "full",
            "placeholder": "Enter memory identifier to retrieve",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Get Memory (<code>memory_get</code>)</div>

Retrieve a specific memory by its ID

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `id` | string | Yes | Identifier for the memory to retrieve |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `memories` | array | Array of memory data for the requested ID |
| `message` | string | Success or error message |

</div>



---

### Delete Memory

<ShowcaseCard>
  <BlockConfigPreview
    name="Memory"
    type="memory"
    color="#F64F9E"
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "full",
            "placeholder": "Select operation",
            "defaultValue": "delete",
            "options": [
                {
                    "id": "add",
                    "label": "Add Memory"
                },
                {
                    "id": "getAll",
                    "label": "Get All Memories"
                },
                {
                    "id": "get",
                    "label": "Get Memory"
                },
                {
                    "id": "delete",
                    "label": "Delete Memory"
                }
            ]
        },
        {
            "id": "id",
            "type": "short-input",
            "title": "ID",
            "layout": "full",
            "placeholder": "Enter memory identifier to delete",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Delete Memory (<code>memory_delete</code>)</div>

Delete a specific memory by its ID

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `id` | string | Yes | Identifier for the memory to delete |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `message` | string | Success or error message |

</div>



---

## Input

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `operation` | string | No | Operation to perform |
| `id` | string | No | Memory identifier |
| `role` | string | No | Agent role. Shown when operation is 'add'. |
| `content` | string | No | Memory content. Shown when operation is 'add'. |

## Output

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `memories` | array | Conversation messages for Add/Get, or key/type/data records for Get All \(up to 50\) |
| `message` | string | Operation confirmation for Get, Get All, or Delete |


## Usage Instructions

Persist conversation messages across runs of the same workflow. Add Memory appends a user, assistant, or system message under an ID; Get Memory returns that conversation. Get All Memories returns up to 50 records, each with key, type, and data. All operations require access to the workflow workspace; writes require write permission.



## Notes

- Category: `blocks`
- Type: `memory`
