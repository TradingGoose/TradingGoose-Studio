---
title: Wait
description: Pause workflow execution for a specified time delay
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="wait"
  color="#F59E0B"
/>


## Configuration

<ShowcaseCard>
  <BlockConfigPreview
    name="Wait"
    type="wait"
    color="#F59E0B"
    hideHeader
    subBlocks={[
        {
            "id": "timeValue",
            "type": "short-input",
            "title": "Wait Amount",
            "layout": "half",
            "placeholder": "10",
            "description": "Max: 600 seconds or 10 minutes",
            "required": true,
            "defaultValue": "10"
        },
        {
            "id": "timeUnit",
            "type": "dropdown",
            "title": "Unit",
            "layout": "half",
            "required": true,
            "defaultValue": "seconds",
            "options": [
                {
                    "id": "seconds",
                    "label": "Seconds"
                },
                {
                    "id": "minutes",
                    "label": "Minutes"
                }
            ]
        }
    ]}
  />
</ShowcaseCard>

## Input

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `timeValue` | string | Yes | Wait duration value |
| `timeUnit` | string | Yes | Wait duration unit \(seconds or minutes\) |

## Output

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `waitDuration` | number | Wait duration in milliseconds |
| `status` | string | completed after the delay, or cancelled when a browser-side wait is interrupted. |


## Usage Instructions

Delay the current execution path by a positive number of seconds or minutes, up to 600 seconds or 10 minutes. The default is 10 seconds. The runtime parses the amount as an integer, so use whole numbers. This is an in-process delay, not a persisted human-approval pause. Browser execution checks for cancellation during the wait; server execution uses a single sleep and does not interrupt it mid-flight.



## Notes

- Category: `blocks`
- Type: `wait`
