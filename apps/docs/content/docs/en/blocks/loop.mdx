---
title: Loop
description: Repeat the blocks inside a workflow container sequentially.
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="loop"
  color="#00ccff"
/>


## Configuration

<ShowcaseCard>
  <BlockConfigPreview
    name="Loop"
    type="loop"
    color="#00ccff"
    hideHeader
    subBlocks={[
        {
            "id": "loopType",
            "type": "dropdown",
            "title": "Loop Type",
            "options": [
                {
                    "id": "for",
                    "label": "For Loop"
                },
                {
                    "id": "forEach",
                    "label": "For Each"
                },
                {
                    "id": "while",
                    "label": "While Loop"
                },
                {
                    "id": "doWhile",
                    "label": "Do While Loop"
                }
            ]
        },
        {
            "id": "iterations",
            "type": "short-input",
            "title": "Loop Iterations",
            "description": "Shown when loopType is 'for'."
        },
        {
            "id": "collection",
            "type": "long-input",
            "title": "Collection Items",
            "description": "Shown when loopType is 'forEach'."
        },
        {
            "id": "whileCondition",
            "type": "long-input",
            "title": "While Condition",
            "description": "Shown when loopType is one of 'while', 'doWhile'."
        }
    ]}
  />
</ShowcaseCard>

## Input

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `loopType` | string | No | for, forEach, while, or doWhile. |
| `iterations` | number | No | Iteration count for For Loop; the editor allows 1–100. Shown when loopType is 'for'. |
| `forEachItems` | json | No | Non-empty array or object for For Each; supports upstream references. |
| `whileCondition` | string | No | Boolean expression for While or Do While. Shown when loopType is one of 'while', 'doWhile'. |

## Output

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `loopId` | string | Loop container ID. |
| `currentIteration` | number | One-based during loop execution; completed For and For Each outputs use the final zero-based index. |
| `maxIterations` | number | Count or collection length; condition-driven loops use the runtime sentinel Number.MAX_SAFE_INTEGER. |
| `loopType` | string | Configured loop mode. |
| `completed` | boolean | Whether the loop has finished. |
| `results` | array | Child outputs grouped by iteration after For or For Each completes; absent from While and Do While completion outputs. |
| `message` | string | Loop progress message. |


## Usage Instructions

For Loop repeats a configured number of times. For Each iterates over a non-empty array or object; object items are key/value pairs. While tests its condition before each iteration, and Do While always executes its first iteration. Conditions use the same expression evaluator as Condition blocks. While and Do While have no fixed iteration cap: provide a terminating condition and account for workflow timeout and execution limits. Connect the container start to its first child and the container end to the blocks that should run afterward. Completed For and For Each loops aggregate child outputs under results in iteration order; a single iteration can contain multiple child outputs. While and Do While completion outputs do not include that results array.



## Notes

- Category: `blocks`
- Type: `loop`
