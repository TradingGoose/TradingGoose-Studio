---
title: Parallel
description: Execute copies of a workflow container concurrently.
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="parallel"
  color="#ffdd00"
/>


## Configuration

<ShowcaseCard>
  <BlockConfigPreview
    name="Parallel"
    type="parallel"
    color="#ffdd00"
    hideHeader
    subBlocks={[
        {
            "id": "parallelType",
            "type": "dropdown",
            "title": "Parallel Type",
            "options": [
                {
                    "id": "count",
                    "label": "Parallel Count"
                },
                {
                    "id": "collection",
                    "label": "Parallel Each"
                }
            ]
        },
        {
            "id": "count",
            "type": "short-input",
            "title": "Parallel Executions",
            "description": "Shown when parallelType is 'count'."
        },
        {
            "id": "distribution",
            "type": "long-input",
            "title": "Collection Items",
            "description": "Shown when parallelType is 'collection'."
        }
    ]}
  />
</ShowcaseCard>

## Input

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `parallelType` | string | No | count or collection. |
| `count` | number | No | Number of executions in count mode; clamped to 1–20. Shown when parallelType is 'count'. |
| `distribution` | json | No | Array or object for collection mode; supports upstream references. Shown when parallelType is 'collection'. |

## Output

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `parallelId` | string | Parallel container ID. |
| `parallelCount` | number | Number of parallel executions. |
| `completed` | boolean | True once all executions have completed. |
| `results` | array | Completed execution results in iteration-index order. |
| `message` | string | Parallel progress message. |


## Usage Instructions

Parallel Count runs between 1 and 20 copies of its child blocks. Parallel Each creates one execution per array item or object entry; the count-mode limit of 20 does not cap collection mode. Child executions share the workflow variable namespace, so concurrent writes are not isolated. Connect the container start to its first child and the container end to the downstream path. After all executions finish, results are aggregated in iteration-index order, not completion order. For object collections, each item is a key/value pair.



## Notes

- Category: `blocks`
- Type: `parallel`
