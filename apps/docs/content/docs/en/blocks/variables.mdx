---
title: Variables
description: Set workflow-scoped variables
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="variables"
  color="#8B5CF6"
/>


## Configuration

<ShowcaseCard>
  <BlockConfigPreview
    name="Variables"
    type="variables"
    color="#8B5CF6"
    hideHeader
    subBlocks={[
        {
            "id": "variables",
            "type": "variables-input",
            "title": "Variable Assignments",
            "layout": "full",
            "description": "Select workflow variables and update their values during execution. Access them anywhere using exact tags like <variable.riskLimit>.",
            "required": false
        }
    ]}
  />
</ShowcaseCard>

## Input

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `variables` | json | No | Assignment entries with variableId, variableName, type, and value. |

## Output

This block does not declare a structured output schema.


## Usage Instructions

Create variables in the Workflow Variables widget, then select them here to update their values during execution. Supported types are plain, number, boolean, object, array, and listingIdentity. Reference a value with an exact tag such as &lt;variable.riskLimit&gt;. Updates apply to this execution, not future runs. All Variables blocks and parallel branches share the workflow variable namespace. Each assignment is also returned as a dynamically named top-level block output. An assignment to an unknown variable does not create a workflow variable.



## Notes

- Category: `blocks`
- Type: `variables`
