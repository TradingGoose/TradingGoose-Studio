---
title: Condition
description: Add a condition
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="condition"
  color="#FF752F"
/>


## Configuration

<ShowcaseCard>
  <BlockConfigPreview
    name="Condition"
    type="condition"
    color="#FF752F"
    hideHeader
    subBlocks={[
        {
            "id": "conditions",
            "type": "condition-input",
            "title": "Conditions",
            "layout": "full",
            "description": "Ordered condition expressions and the Else fallback; connect each desired branch to its downstream block."
        }
    ]}
  />
</ShowcaseCard>

## Input

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `conditions` | json | No | Ordered condition entries with IDs, titles, and expressions. |

## Output

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `conditionResult` | boolean | True when a connected condition or Else path was selected. |
| `selectedPath` | json | Selected downstream block metadata, or null when no path is selected. |
| `selectedOption` | string | Selected condition option ID |


## Usage Instructions

Evaluate JavaScript expressions in order and follow the first matching connected condition. Else is the fallback when no expression matches. Reference upstream outputs and workflow variables with exact TradingGoose tags. conditionResult reports whether a connected path was selected, including Else; it is not the value of the first expression. Without a matching connected path, conditionResult is false and selectedPath is null. Invalid expressions fail the block. Condition blocks do not support error-path recovery; validate uncertain data in an upstream Function block when recovery is required.



## Notes

- Category: `blocks`
- Type: `condition`
