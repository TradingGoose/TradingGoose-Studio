---
title: Python SDK
description: Python client library for TradingGoose (coming soon)
---

import { Callout } from 'fumadocs-ui/components/callout'

<Callout type="warn">
  The Python SDK is under active development. This page will be updated when the SDK is released.
</Callout>

## Status

The Python SDK is being built to provide a native Python interface for executing TradingGoose workflows, managing workspace resources, and handling streaming responses.

## In the Meantime

Use the [Execution API](/execution/api) directly with any HTTP client:

```python
import requests

response = requests.post(
    "https://tradinggoose.ai/api/workflows/WORKFLOW_ID/execute",
    headers={
        "Content-Type": "application/json",
        "X-API-Key": "YOUR_KEY",
    },
    json={"input": "your data"},
)

result = response.json()
```

For streaming responses, see the [Streaming section](/triggers/api#streaming-responses) of the API Trigger docs.
