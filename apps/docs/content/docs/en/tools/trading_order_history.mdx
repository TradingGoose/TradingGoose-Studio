---
title: Trading Order History
description: Retrieve recorded trading order submissions for a date range.
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="trading_order_history"
  color="#0f766e"
/>


## Configuration

<ShowcaseCard>
  <BlockConfigPreview
    name="Trading Order History"
    type="trading_order_history"
    color="#0f766e"
    hideHeader
    subBlocks={[
        {
            "id": "startDate",
            "type": "datetime-input",
            "title": "Start Date",
            "layout": "full",
            "placeholder": "YYYY-MM-DDTHH:mm:ssZ",
            "required": true
        },
        {
            "id": "endDate",
            "type": "datetime-input",
            "title": "End Date",
            "layout": "full",
            "placeholder": "YYYY-MM-DDTHH:mm:ssZ",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

## Tools

### `trading_order_history`

Retrieve workspace order submissions recorded within a datetime range.

#### Input

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `startDate` | string | Yes | Start datetime \(ISO 8601\). |
| `endDate` | string | Yes | End datetime \(ISO 8601\). |

#### Output

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `id` | string | Order history record ID |
| `workspaceId` | string | Owning workspace ID |
| `provider` | string | Trading provider |
| `recordedAt` | string | Recorded timestamp |
| `submissionSource` | string | Order submission source |
| `logId` | string | Linked log ID |
| `providerOrderId` | string | Provider order ID |


## Usage Instructions

Fetches order submission history recorded by the Trading Action tool for the selected date range.



## Notes

- Category: `tools`
- Type: `trading_order_history`
