---
title: Airtable
description: Read, create, and update Airtable
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"
import { Card, Cards } from 'fumadocs-ui/components/card'

<BlockInfoCard
  type="airtable"
  color=""
/>

<Cards>
  <Card title="Use as a Trigger" href="/triggers/airtable">
    See trigger events, setup, and event payloads for Airtable.
  </Card>
</Cards>


## Configuration

### List Records

<ShowcaseCard>
  <BlockConfigPreview
    name="Airtable"
    type="airtable"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "full",
            "options": [
                {
                    "label": "List Records",
                    "id": "list"
                },
                {
                    "label": "Get Record",
                    "id": "get"
                },
                {
                    "label": "Create Records",
                    "id": "create"
                },
                {
                    "label": "Update Record",
                    "id": "update"
                }
            ],
            "defaultValue": "list"
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Airtable Account",
            "layout": "full",
            "placeholder": "Select Airtable account",
            "provider": "airtable",
            "required": true
        },
        {
            "id": "baseId",
            "type": "short-input",
            "title": "Base ID",
            "layout": "full",
            "placeholder": "Enter your base ID (e.g., appXXXXXXXXXXXXXX)",
            "required": true
        },
        {
            "id": "tableId",
            "type": "short-input",
            "title": "Table ID",
            "layout": "full",
            "placeholder": "Enter table ID (e.g., tblXXXXXXXXXXXXXX)",
            "required": true
        },
        {
            "id": "maxRecords",
            "type": "short-input",
            "title": "Max Records",
            "layout": "half",
            "placeholder": "Maximum records to return (optional)"
        },
        {
            "id": "filterFormula",
            "type": "long-input",
            "title": "Filter Formula",
            "layout": "full",
            "placeholder": "Airtable formula to filter records (optional)"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">List Records (<code>airtable_list_records</code>)</div>

Read records from an Airtable table

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `baseId` | string | Yes | ID of the Airtable base |
| `tableId` | string | Yes | ID of the table |
| `maxRecords` | number | No | Maximum number of records to return |
| `filterFormula` | string | No | Formula to filter records \(e.g., "\(\{Field Name\} = \'Value\'\)"\) |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `id` | string | id output |
| `createdTime` | string | createdTime output |

</div>



---

### Get Record

<ShowcaseCard>
  <BlockConfigPreview
    name="Airtable"
    type="airtable"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "full",
            "options": [
                {
                    "label": "List Records",
                    "id": "list"
                },
                {
                    "label": "Get Record",
                    "id": "get"
                },
                {
                    "label": "Create Records",
                    "id": "create"
                },
                {
                    "label": "Update Record",
                    "id": "update"
                }
            ],
            "defaultValue": "get"
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Airtable Account",
            "layout": "full",
            "placeholder": "Select Airtable account",
            "provider": "airtable",
            "required": true
        },
        {
            "id": "baseId",
            "type": "short-input",
            "title": "Base ID",
            "layout": "full",
            "placeholder": "Enter your base ID (e.g., appXXXXXXXXXXXXXX)",
            "required": true
        },
        {
            "id": "tableId",
            "type": "short-input",
            "title": "Table ID",
            "layout": "full",
            "placeholder": "Enter table ID (e.g., tblXXXXXXXXXXXXXX)",
            "required": true
        },
        {
            "id": "recordId",
            "type": "short-input",
            "title": "Record ID",
            "layout": "full",
            "placeholder": "ID of the record (e.g., recXXXXXXXXXXXXXX)",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Get Record (<code>airtable_get_record</code>)</div>

Retrieve a single record from an Airtable table by its ID

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `baseId` | string | Yes | ID of the Airtable base |
| `tableId` | string | Yes | ID or name of the table |
| `recordId` | string | Yes | ID of the record to retrieve |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `record` | json | Retrieved Airtable record with id, createdTime, and fields |

</div>



---

### Create Records

<ShowcaseCard>
  <BlockConfigPreview
    name="Airtable"
    type="airtable"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "full",
            "options": [
                {
                    "label": "List Records",
                    "id": "list"
                },
                {
                    "label": "Get Record",
                    "id": "get"
                },
                {
                    "label": "Create Records",
                    "id": "create"
                },
                {
                    "label": "Update Record",
                    "id": "update"
                }
            ],
            "defaultValue": "create"
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Airtable Account",
            "layout": "full",
            "placeholder": "Select Airtable account",
            "provider": "airtable",
            "required": true
        },
        {
            "id": "baseId",
            "type": "short-input",
            "title": "Base ID",
            "layout": "full",
            "placeholder": "Enter your base ID (e.g., appXXXXXXXXXXXXXX)",
            "required": true
        },
        {
            "id": "tableId",
            "type": "short-input",
            "title": "Table ID",
            "layout": "full",
            "placeholder": "Enter table ID (e.g., tblXXXXXXXXXXXXXX)",
            "required": true
        },
        {
            "id": "records",
            "type": "code",
            "title": "Records (JSON Array)",
            "layout": "full",
            "placeholder": "For Create: `[{ \"fields\": { ... } }]`\\n",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Create Records (<code>airtable_create_records</code>)</div>

Write new records to an Airtable table

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `baseId` | string | Yes | ID of the Airtable base |
| `tableId` | string | Yes | ID or name of the table |
| `records` | json | Yes | Array of records to create, each with a `fields` object |
| `fields` | string | No | No description |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `id` | string | id output |
| `createdTime` | string | createdTime output |

</div>



---

### Update Record

<ShowcaseCard>
  <BlockConfigPreview
    name="Airtable"
    type="airtable"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "full",
            "options": [
                {
                    "label": "List Records",
                    "id": "list"
                },
                {
                    "label": "Get Record",
                    "id": "get"
                },
                {
                    "label": "Create Records",
                    "id": "create"
                },
                {
                    "label": "Update Record",
                    "id": "update"
                }
            ],
            "defaultValue": "update"
        },
        {
            "id": "credential",
            "type": "oauth-input",
            "title": "Airtable Account",
            "layout": "full",
            "placeholder": "Select Airtable account",
            "provider": "airtable",
            "required": true
        },
        {
            "id": "baseId",
            "type": "short-input",
            "title": "Base ID",
            "layout": "full",
            "placeholder": "Enter your base ID (e.g., appXXXXXXXXXXXXXX)",
            "required": true
        },
        {
            "id": "tableId",
            "type": "short-input",
            "title": "Table ID",
            "layout": "full",
            "placeholder": "Enter table ID (e.g., tblXXXXXXXXXXXXXX)",
            "required": true
        },
        {
            "id": "recordId",
            "type": "short-input",
            "title": "Record ID",
            "layout": "full",
            "placeholder": "ID of the record (e.g., recXXXXXXXXXXXXXX)",
            "required": true
        },
        {
            "id": "fields",
            "type": "code",
            "title": "Fields (JSON Object)",
            "layout": "full",
            "placeholder": "Fields to update: `{ \"Field Name\": \"New Value\" }`",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Update Record (<code>airtable_update_record</code>)</div>

Update an existing record in an Airtable table by ID

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `baseId` | string | Yes | ID of the Airtable base |
| `tableId` | string | Yes | ID or name of the table |
| `recordId` | string | Yes | ID of the record to update |
| `fields` | json | Yes | An object containing the field names and their new values |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `record` | json | Updated Airtable record with id, createdTime, and fields |

</div>



---


## Usage Instructions

Integrates Airtable into the workflow. Can create, get, list, or update Airtable records. Can be used in trigger mode to trigger a workflow when an update is made to an Airtable table.



## Notes

- Category: `tools`
- Type: `airtable`
