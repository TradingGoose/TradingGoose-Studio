---
title: Zep
description: Long-term memory for AI agents
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="zep"
  color=""
/>


## Configuration

### Create Thread

<ShowcaseCard>
  <BlockConfigPreview
    name="Zep"
    type="zep"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "half",
            "placeholder": "Select an operation",
            "options": [
                {
                    "label": "Create Thread",
                    "id": "create_thread"
                },
                {
                    "label": "Add Messages",
                    "id": "add_messages"
                },
                {
                    "label": "Get Context",
                    "id": "get_context"
                },
                {
                    "label": "Get Messages",
                    "id": "get_messages"
                },
                {
                    "label": "Get Threads",
                    "id": "get_threads"
                },
                {
                    "label": "Delete Thread",
                    "id": "delete_thread"
                },
                {
                    "label": "Add User",
                    "id": "add_user"
                },
                {
                    "label": "Get User",
                    "id": "get_user"
                },
                {
                    "label": "Get User Threads",
                    "id": "get_user_threads"
                }
            ],
            "defaultValue": "create_thread"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Zep API key",
            "password": true,
            "required": true
        },
        {
            "id": "threadId",
            "type": "short-input",
            "title": "Thread ID",
            "layout": "full",
            "placeholder": "Enter unique thread identifier",
            "required": true
        },
        {
            "id": "userId",
            "type": "short-input",
            "title": "User ID",
            "layout": "full",
            "placeholder": "Enter user identifier",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-sm font-medium text-fd-muted-foreground mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `threadId` | string | Thread identifier |
| `userId` | string | User identifier |
| `uuid` | string | Internal UUID |
| `createdAt` | string | Creation timestamp |
| `updatedAt` | string | Update timestamp |
| `threads` | json | Array of threads |
| `deleted` | boolean | Deletion status |
| `messages` | json | Message data |
| `messageIds` | json | Message identifiers |
| `context` | string | User context string |
| `facts` | json | Extracted facts |
| `entities` | json | Extracted entities |
| `summary` | string | Conversation summary |
| `batchId` | string | Batch operation ID |
| `email` | string | User email |
| `firstName` | string | User first name |
| `lastName` | string | User last name |
| `metadata` | json | User metadata |
| `responseCount` | number | Number of items in response |
| `totalCount` | number | Total number of items available |
| `rowCount` | number | Number of rows in response |

</div>



---

### Add Messages

<ShowcaseCard>
  <BlockConfigPreview
    name="Zep"
    type="zep"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "half",
            "placeholder": "Select an operation",
            "options": [
                {
                    "label": "Create Thread",
                    "id": "create_thread"
                },
                {
                    "label": "Add Messages",
                    "id": "add_messages"
                },
                {
                    "label": "Get Context",
                    "id": "get_context"
                },
                {
                    "label": "Get Messages",
                    "id": "get_messages"
                },
                {
                    "label": "Get Threads",
                    "id": "get_threads"
                },
                {
                    "label": "Delete Thread",
                    "id": "delete_thread"
                },
                {
                    "label": "Add User",
                    "id": "add_user"
                },
                {
                    "label": "Get User",
                    "id": "get_user"
                },
                {
                    "label": "Get User Threads",
                    "id": "get_user_threads"
                }
            ],
            "defaultValue": "add_messages"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Zep API key",
            "password": true,
            "required": true
        },
        {
            "id": "threadId",
            "type": "short-input",
            "title": "Thread ID",
            "layout": "full",
            "placeholder": "Enter unique thread identifier",
            "required": true
        },
        {
            "id": "messages",
            "type": "code",
            "title": "Messages",
            "layout": "full",
            "placeholder": "[{\"role\": \"user\", \"content\": \"Hello!\"}]",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-sm font-medium text-fd-muted-foreground mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `threadId` | string | Thread identifier |
| `userId` | string | User identifier |
| `uuid` | string | Internal UUID |
| `createdAt` | string | Creation timestamp |
| `updatedAt` | string | Update timestamp |
| `threads` | json | Array of threads |
| `deleted` | boolean | Deletion status |
| `messages` | json | Message data |
| `messageIds` | json | Message identifiers |
| `context` | string | User context string |
| `facts` | json | Extracted facts |
| `entities` | json | Extracted entities |
| `summary` | string | Conversation summary |
| `batchId` | string | Batch operation ID |
| `email` | string | User email |
| `firstName` | string | User first name |
| `lastName` | string | User last name |
| `metadata` | json | User metadata |
| `responseCount` | number | Number of items in response |
| `totalCount` | number | Total number of items available |
| `rowCount` | number | Number of rows in response |

</div>



---

### Get Context

<ShowcaseCard>
  <BlockConfigPreview
    name="Zep"
    type="zep"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "half",
            "placeholder": "Select an operation",
            "options": [
                {
                    "label": "Create Thread",
                    "id": "create_thread"
                },
                {
                    "label": "Add Messages",
                    "id": "add_messages"
                },
                {
                    "label": "Get Context",
                    "id": "get_context"
                },
                {
                    "label": "Get Messages",
                    "id": "get_messages"
                },
                {
                    "label": "Get Threads",
                    "id": "get_threads"
                },
                {
                    "label": "Delete Thread",
                    "id": "delete_thread"
                },
                {
                    "label": "Add User",
                    "id": "add_user"
                },
                {
                    "label": "Get User",
                    "id": "get_user"
                },
                {
                    "label": "Get User Threads",
                    "id": "get_user_threads"
                }
            ],
            "defaultValue": "get_context"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Zep API key",
            "password": true,
            "required": true
        },
        {
            "id": "threadId",
            "type": "short-input",
            "title": "Thread ID",
            "layout": "full",
            "placeholder": "Enter unique thread identifier",
            "required": true
        },
        {
            "id": "mode",
            "type": "dropdown",
            "title": "Context Mode",
            "layout": "half",
            "placeholder": "Select context mode",
            "options": [
                {
                    "label": "Summary (Natural Language)",
                    "id": "summary"
                },
                {
                    "label": "Basic (Raw Facts)",
                    "id": "basic"
                }
            ]
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-sm font-medium text-fd-muted-foreground mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `threadId` | string | Thread identifier |
| `userId` | string | User identifier |
| `uuid` | string | Internal UUID |
| `createdAt` | string | Creation timestamp |
| `updatedAt` | string | Update timestamp |
| `threads` | json | Array of threads |
| `deleted` | boolean | Deletion status |
| `messages` | json | Message data |
| `messageIds` | json | Message identifiers |
| `context` | string | User context string |
| `facts` | json | Extracted facts |
| `entities` | json | Extracted entities |
| `summary` | string | Conversation summary |
| `batchId` | string | Batch operation ID |
| `email` | string | User email |
| `firstName` | string | User first name |
| `lastName` | string | User last name |
| `metadata` | json | User metadata |
| `responseCount` | number | Number of items in response |
| `totalCount` | number | Total number of items available |
| `rowCount` | number | Number of rows in response |

</div>



---

### Get Messages

<ShowcaseCard>
  <BlockConfigPreview
    name="Zep"
    type="zep"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "half",
            "placeholder": "Select an operation",
            "options": [
                {
                    "label": "Create Thread",
                    "id": "create_thread"
                },
                {
                    "label": "Add Messages",
                    "id": "add_messages"
                },
                {
                    "label": "Get Context",
                    "id": "get_context"
                },
                {
                    "label": "Get Messages",
                    "id": "get_messages"
                },
                {
                    "label": "Get Threads",
                    "id": "get_threads"
                },
                {
                    "label": "Delete Thread",
                    "id": "delete_thread"
                },
                {
                    "label": "Add User",
                    "id": "add_user"
                },
                {
                    "label": "Get User",
                    "id": "get_user"
                },
                {
                    "label": "Get User Threads",
                    "id": "get_user_threads"
                }
            ],
            "defaultValue": "get_messages"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Zep API key",
            "password": true,
            "required": true
        },
        {
            "id": "threadId",
            "type": "short-input",
            "title": "Thread ID",
            "layout": "full",
            "placeholder": "Enter unique thread identifier",
            "required": true
        },
        {
            "id": "limit",
            "type": "slider",
            "title": "Result Limit",
            "layout": "full",
            "min": 1,
            "max": 100,
            "step": 1
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-sm font-medium text-fd-muted-foreground mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `threadId` | string | Thread identifier |
| `userId` | string | User identifier |
| `uuid` | string | Internal UUID |
| `createdAt` | string | Creation timestamp |
| `updatedAt` | string | Update timestamp |
| `threads` | json | Array of threads |
| `deleted` | boolean | Deletion status |
| `messages` | json | Message data |
| `messageIds` | json | Message identifiers |
| `context` | string | User context string |
| `facts` | json | Extracted facts |
| `entities` | json | Extracted entities |
| `summary` | string | Conversation summary |
| `batchId` | string | Batch operation ID |
| `email` | string | User email |
| `firstName` | string | User first name |
| `lastName` | string | User last name |
| `metadata` | json | User metadata |
| `responseCount` | number | Number of items in response |
| `totalCount` | number | Total number of items available |
| `rowCount` | number | Number of rows in response |

</div>



---

### Get Threads

<ShowcaseCard>
  <BlockConfigPreview
    name="Zep"
    type="zep"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "half",
            "placeholder": "Select an operation",
            "options": [
                {
                    "label": "Create Thread",
                    "id": "create_thread"
                },
                {
                    "label": "Add Messages",
                    "id": "add_messages"
                },
                {
                    "label": "Get Context",
                    "id": "get_context"
                },
                {
                    "label": "Get Messages",
                    "id": "get_messages"
                },
                {
                    "label": "Get Threads",
                    "id": "get_threads"
                },
                {
                    "label": "Delete Thread",
                    "id": "delete_thread"
                },
                {
                    "label": "Add User",
                    "id": "add_user"
                },
                {
                    "label": "Get User",
                    "id": "get_user"
                },
                {
                    "label": "Get User Threads",
                    "id": "get_user_threads"
                }
            ],
            "defaultValue": "get_threads"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Zep API key",
            "password": true,
            "required": true
        },
        {
            "id": "limit",
            "type": "slider",
            "title": "Result Limit",
            "layout": "full",
            "min": 1,
            "max": 100,
            "step": 1
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-sm font-medium text-fd-muted-foreground mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `threadId` | string | Thread identifier |
| `userId` | string | User identifier |
| `uuid` | string | Internal UUID |
| `createdAt` | string | Creation timestamp |
| `updatedAt` | string | Update timestamp |
| `threads` | json | Array of threads |
| `deleted` | boolean | Deletion status |
| `messages` | json | Message data |
| `messageIds` | json | Message identifiers |
| `context` | string | User context string |
| `facts` | json | Extracted facts |
| `entities` | json | Extracted entities |
| `summary` | string | Conversation summary |
| `batchId` | string | Batch operation ID |
| `email` | string | User email |
| `firstName` | string | User first name |
| `lastName` | string | User last name |
| `metadata` | json | User metadata |
| `responseCount` | number | Number of items in response |
| `totalCount` | number | Total number of items available |
| `rowCount` | number | Number of rows in response |

</div>



---

### Delete Thread

<ShowcaseCard>
  <BlockConfigPreview
    name="Zep"
    type="zep"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "half",
            "placeholder": "Select an operation",
            "options": [
                {
                    "label": "Create Thread",
                    "id": "create_thread"
                },
                {
                    "label": "Add Messages",
                    "id": "add_messages"
                },
                {
                    "label": "Get Context",
                    "id": "get_context"
                },
                {
                    "label": "Get Messages",
                    "id": "get_messages"
                },
                {
                    "label": "Get Threads",
                    "id": "get_threads"
                },
                {
                    "label": "Delete Thread",
                    "id": "delete_thread"
                },
                {
                    "label": "Add User",
                    "id": "add_user"
                },
                {
                    "label": "Get User",
                    "id": "get_user"
                },
                {
                    "label": "Get User Threads",
                    "id": "get_user_threads"
                }
            ],
            "defaultValue": "delete_thread"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Zep API key",
            "password": true,
            "required": true
        },
        {
            "id": "threadId",
            "type": "short-input",
            "title": "Thread ID",
            "layout": "full",
            "placeholder": "Enter unique thread identifier",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-sm font-medium text-fd-muted-foreground mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `threadId` | string | Thread identifier |
| `userId` | string | User identifier |
| `uuid` | string | Internal UUID |
| `createdAt` | string | Creation timestamp |
| `updatedAt` | string | Update timestamp |
| `threads` | json | Array of threads |
| `deleted` | boolean | Deletion status |
| `messages` | json | Message data |
| `messageIds` | json | Message identifiers |
| `context` | string | User context string |
| `facts` | json | Extracted facts |
| `entities` | json | Extracted entities |
| `summary` | string | Conversation summary |
| `batchId` | string | Batch operation ID |
| `email` | string | User email |
| `firstName` | string | User first name |
| `lastName` | string | User last name |
| `metadata` | json | User metadata |
| `responseCount` | number | Number of items in response |
| `totalCount` | number | Total number of items available |
| `rowCount` | number | Number of rows in response |

</div>



---

### Add User

<ShowcaseCard>
  <BlockConfigPreview
    name="Zep"
    type="zep"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "half",
            "placeholder": "Select an operation",
            "options": [
                {
                    "label": "Create Thread",
                    "id": "create_thread"
                },
                {
                    "label": "Add Messages",
                    "id": "add_messages"
                },
                {
                    "label": "Get Context",
                    "id": "get_context"
                },
                {
                    "label": "Get Messages",
                    "id": "get_messages"
                },
                {
                    "label": "Get Threads",
                    "id": "get_threads"
                },
                {
                    "label": "Delete Thread",
                    "id": "delete_thread"
                },
                {
                    "label": "Add User",
                    "id": "add_user"
                },
                {
                    "label": "Get User",
                    "id": "get_user"
                },
                {
                    "label": "Get User Threads",
                    "id": "get_user_threads"
                }
            ],
            "defaultValue": "add_user"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Zep API key",
            "password": true,
            "required": true
        },
        {
            "id": "userId",
            "type": "short-input",
            "title": "User ID",
            "layout": "full",
            "placeholder": "Enter user identifier",
            "required": true
        },
        {
            "id": "email",
            "type": "short-input",
            "title": "Email",
            "layout": "half",
            "placeholder": "user@example.com"
        },
        {
            "id": "firstName",
            "type": "short-input",
            "title": "First Name",
            "layout": "half",
            "placeholder": "John"
        },
        {
            "id": "lastName",
            "type": "short-input",
            "title": "Last Name",
            "layout": "half",
            "placeholder": "Doe"
        },
        {
            "id": "metadata",
            "type": "code",
            "title": "Metadata",
            "layout": "full",
            "placeholder": "{\"key\": \"value\"}",
            "language": "json"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-sm font-medium text-fd-muted-foreground mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `threadId` | string | Thread identifier |
| `userId` | string | User identifier |
| `uuid` | string | Internal UUID |
| `createdAt` | string | Creation timestamp |
| `updatedAt` | string | Update timestamp |
| `threads` | json | Array of threads |
| `deleted` | boolean | Deletion status |
| `messages` | json | Message data |
| `messageIds` | json | Message identifiers |
| `context` | string | User context string |
| `facts` | json | Extracted facts |
| `entities` | json | Extracted entities |
| `summary` | string | Conversation summary |
| `batchId` | string | Batch operation ID |
| `email` | string | User email |
| `firstName` | string | User first name |
| `lastName` | string | User last name |
| `metadata` | json | User metadata |
| `responseCount` | number | Number of items in response |
| `totalCount` | number | Total number of items available |
| `rowCount` | number | Number of rows in response |

</div>



---

### Get User

<ShowcaseCard>
  <BlockConfigPreview
    name="Zep"
    type="zep"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "half",
            "placeholder": "Select an operation",
            "options": [
                {
                    "label": "Create Thread",
                    "id": "create_thread"
                },
                {
                    "label": "Add Messages",
                    "id": "add_messages"
                },
                {
                    "label": "Get Context",
                    "id": "get_context"
                },
                {
                    "label": "Get Messages",
                    "id": "get_messages"
                },
                {
                    "label": "Get Threads",
                    "id": "get_threads"
                },
                {
                    "label": "Delete Thread",
                    "id": "delete_thread"
                },
                {
                    "label": "Add User",
                    "id": "add_user"
                },
                {
                    "label": "Get User",
                    "id": "get_user"
                },
                {
                    "label": "Get User Threads",
                    "id": "get_user_threads"
                }
            ],
            "defaultValue": "get_user"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Zep API key",
            "password": true,
            "required": true
        },
        {
            "id": "userId",
            "type": "short-input",
            "title": "User ID",
            "layout": "full",
            "placeholder": "Enter user identifier",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-sm font-medium text-fd-muted-foreground mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `threadId` | string | Thread identifier |
| `userId` | string | User identifier |
| `uuid` | string | Internal UUID |
| `createdAt` | string | Creation timestamp |
| `updatedAt` | string | Update timestamp |
| `threads` | json | Array of threads |
| `deleted` | boolean | Deletion status |
| `messages` | json | Message data |
| `messageIds` | json | Message identifiers |
| `context` | string | User context string |
| `facts` | json | Extracted facts |
| `entities` | json | Extracted entities |
| `summary` | string | Conversation summary |
| `batchId` | string | Batch operation ID |
| `email` | string | User email |
| `firstName` | string | User first name |
| `lastName` | string | User last name |
| `metadata` | json | User metadata |
| `responseCount` | number | Number of items in response |
| `totalCount` | number | Total number of items available |
| `rowCount` | number | Number of rows in response |

</div>



---

### Get User Threads

<ShowcaseCard>
  <BlockConfigPreview
    name="Zep"
    type="zep"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "half",
            "placeholder": "Select an operation",
            "options": [
                {
                    "label": "Create Thread",
                    "id": "create_thread"
                },
                {
                    "label": "Add Messages",
                    "id": "add_messages"
                },
                {
                    "label": "Get Context",
                    "id": "get_context"
                },
                {
                    "label": "Get Messages",
                    "id": "get_messages"
                },
                {
                    "label": "Get Threads",
                    "id": "get_threads"
                },
                {
                    "label": "Delete Thread",
                    "id": "delete_thread"
                },
                {
                    "label": "Add User",
                    "id": "add_user"
                },
                {
                    "label": "Get User",
                    "id": "get_user"
                },
                {
                    "label": "Get User Threads",
                    "id": "get_user_threads"
                }
            ],
            "defaultValue": "get_user_threads"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Zep API key",
            "password": true,
            "required": true
        },
        {
            "id": "userId",
            "type": "short-input",
            "title": "User ID",
            "layout": "full",
            "placeholder": "Enter user identifier",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-sm font-medium text-fd-muted-foreground mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `threadId` | string | Thread identifier |
| `userId` | string | User identifier |
| `uuid` | string | Internal UUID |
| `createdAt` | string | Creation timestamp |
| `updatedAt` | string | Update timestamp |
| `threads` | json | Array of threads |
| `deleted` | boolean | Deletion status |
| `messages` | json | Message data |
| `messageIds` | json | Message identifiers |
| `context` | string | User context string |
| `facts` | json | Extracted facts |
| `entities` | json | Extracted entities |
| `summary` | string | Conversation summary |
| `batchId` | string | Batch operation ID |
| `email` | string | User email |
| `firstName` | string | User first name |
| `lastName` | string | User last name |
| `metadata` | json | User metadata |
| `responseCount` | number | Number of items in response |
| `totalCount` | number | Total number of items available |
| `rowCount` | number | Number of rows in response |

</div>



---


## Usage Instructions

Integrate Zep for long-term memory management. Create threads, add messages, retrieve context with AI-powered summaries and facts extraction.



## Notes

- Category: `tools`
- Type: `zep`
