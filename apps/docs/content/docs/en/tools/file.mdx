---
title: File
description: Read and parse multiple files
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="file"
  color="#40916C"
/>


## Configuration

### File URL

<ShowcaseCard>
  <BlockConfigPreview
    name="File"
    type="file"
    color="#40916C"
    hideHeader
    subBlocks={[
        {
            "id": "inputMethod",
            "type": "dropdown",
            "title": "Select Input Method",
            "layout": "full",
            "options": [
                {
                    "id": "url",
                    "label": "File URL"
                },
                {
                    "id": "upload",
                    "label": "Uploaded Files"
                }
            ],
            "defaultValue": "url"
        },
        {
            "id": "filePath",
            "type": "short-input",
            "title": "File URL",
            "layout": "full",
            "placeholder": "Enter URL to a file (https://example.com/document.pdf)"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">File URL (<code>file_parser</code>)</div>

Parse one or more uploaded files or files from URLs (text, PDF, CSV, images, etc.)

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `filePath` | string | Yes | Path to the file\(s\). Can be a single path, URL, or an array of paths. |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `files` | array | Array of parsed files with content and metadata |
| `combinedContent` | string | Combined content of all parsed files |
| `processedFiles` | file[] | Array of UserFile objects for downstream use |

</div>



---

### Uploaded Files

<ShowcaseCard>
  <BlockConfigPreview
    name="File"
    type="file"
    color="#40916C"
    hideHeader
    subBlocks={[
        {
            "id": "inputMethod",
            "type": "dropdown",
            "title": "Select Input Method",
            "layout": "full",
            "options": [
                {
                    "id": "url",
                    "label": "File URL"
                },
                {
                    "id": "upload",
                    "label": "Uploaded Files"
                }
            ],
            "defaultValue": "upload"
        },
        {
            "id": "file",
            "type": "file-upload",
            "title": "Process Files",
            "layout": "full"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Uploaded Files (<code>file_parser</code>)</div>

Parse one or more uploaded files or files from URLs (text, PDF, CSV, images, etc.)

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `filePath` | string | Yes | Path to the file\(s\). Can be a single path, URL, or an array of paths. |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `files` | array | Array of parsed files with content and metadata |
| `combinedContent` | string | Combined content of all parsed files |
| `processedFiles` | file[] | Array of UserFile objects for downstream use |

</div>



---


## Usage Instructions

Integrate File into the workflow. Can upload a file manually or insert a file url.



## Notes

- Category: `tools`
- Type: `file`
