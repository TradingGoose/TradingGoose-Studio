---
title: Mistral Parser
description: Extract text from PDF documents
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="mistral_parse"
  color=""
/>


## Configuration

### PDF Document URL

<ShowcaseCard>
  <BlockConfigPreview
    name="Mistral Parser"
    type="mistral_parse"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "inputMethod",
            "type": "dropdown",
            "title": "Select Input Method",
            "layout": "full",
            "options": [
                {
                    "id": "url",
                    "label": "PDF Document URL"
                },
                {
                    "id": "upload",
                    "label": "Upload PDF Document"
                }
            ],
            "defaultValue": "url"
        },
        {
            "id": "resultType",
            "type": "dropdown",
            "title": "Output Format",
            "layout": "full",
            "options": [
                {
                    "id": "markdown",
                    "label": "Markdown (Formatted)"
                },
                {
                    "id": "text",
                    "label": "Plain Text"
                },
                {
                    "id": "json",
                    "label": "JSON (Raw)"
                }
            ]
        },
        {
            "id": "pages",
            "type": "short-input",
            "title": "Specific Pages",
            "layout": "full",
            "placeholder": "e.g. 0,1,2 (leave empty for all pages)"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Mistral API key",
            "password": true,
            "required": true
        },
        {
            "id": "filePath",
            "type": "short-input",
            "title": "PDF Document URL",
            "layout": "full",
            "placeholder": "Enter full URL to a PDF document (https://example.com/document.pdf)"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">PDF Document URL (<code>mistral_parser</code>)</div>

Parse PDF documents using Mistral OCR API

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `filePath` | string | Yes | URL to a PDF document to be processed |
| `resultType` | string | No | Type of parsed result \(markdown, text, or json\). Defaults to markdown. |
| `pages` | array | No | Specific pages to process \(array of page numbers, starting from 0\) |
| `apiKey` | string | Yes | Mistral API key for this request. |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `success` | boolean | Whether the PDF was parsed successfully |
| `content` | string | Extracted content in the requested format \(markdown, text, or JSON\) |
| `metadata` | object | Processing metadata including jobId, fileType, pageCount, and usage info |

</div>



---

### Upload PDF Document

<ShowcaseCard>
  <BlockConfigPreview
    name="Mistral Parser"
    type="mistral_parse"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "inputMethod",
            "type": "dropdown",
            "title": "Select Input Method",
            "layout": "full",
            "options": [
                {
                    "id": "url",
                    "label": "PDF Document URL"
                },
                {
                    "id": "upload",
                    "label": "Upload PDF Document"
                }
            ],
            "defaultValue": "upload"
        },
        {
            "id": "resultType",
            "type": "dropdown",
            "title": "Output Format",
            "layout": "full",
            "options": [
                {
                    "id": "markdown",
                    "label": "Markdown (Formatted)"
                },
                {
                    "id": "text",
                    "label": "Plain Text"
                },
                {
                    "id": "json",
                    "label": "JSON (Raw)"
                }
            ]
        },
        {
            "id": "pages",
            "type": "short-input",
            "title": "Specific Pages",
            "layout": "full",
            "placeholder": "e.g. 0,1,2 (leave empty for all pages)"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Mistral API key",
            "password": true,
            "required": true
        },
        {
            "id": "fileUpload",
            "type": "file-upload",
            "title": "Upload PDF",
            "layout": "full"
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Upload PDF Document (<code>mistral_parser</code>)</div>

Parse PDF documents using Mistral OCR API

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `filePath` | string | Yes | URL to a PDF document to be processed |
| `resultType` | string | No | Type of parsed result \(markdown, text, or json\). Defaults to markdown. |
| `pages` | array | No | Specific pages to process \(array of page numbers, starting from 0\) |
| `apiKey` | string | Yes | Mistral API key for this request. |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `success` | boolean | Whether the PDF was parsed successfully |
| `content` | string | Extracted content in the requested format \(markdown, text, or JSON\) |
| `metadata` | object | Processing metadata including jobId, fileType, pageCount, and usage info |

</div>



---


## Usage Instructions

Integrate Mistral Parse into the workflow. Can extract text from uploaded PDF documents, or from a URL.



## Notes

- Category: `tools`
- Type: `mistral_parse`
