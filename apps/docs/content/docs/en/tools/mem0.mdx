---
title: Mem0
description: Agent memory management
---

import { BlockInfoCard } from "@/components/ui/block-info-card"
import { BlockConfigPreview } from "@/components/ui/block-config-preview"
import { ShowcaseCard } from "@/components/ui/showcase-card"


<BlockInfoCard
  type="mem0"
  color=""
/>


## Configuration

### Add Memories

<ShowcaseCard>
  <BlockConfigPreview
    name="Mem0"
    type="mem0"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "half",
            "placeholder": "Select an operation",
            "defaultValue": "add",
            "options": [
                {
                    "id": "add",
                    "label": "Add Memories"
                },
                {
                    "id": "search",
                    "label": "Search Memories"
                },
                {
                    "id": "get",
                    "label": "Get Memories"
                }
            ]
        },
        {
            "id": "userId",
            "type": "short-input",
            "title": "User ID",
            "layout": "full",
            "placeholder": "Enter user identifier",
            "required": true,
            "defaultValue": "userid"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Mem0 API key",
            "password": true,
            "required": true
        },
        {
            "id": "messages",
            "type": "code",
            "title": "Messages",
            "layout": "full",
            "placeholder": "JSON array, e.g. [{\"role\": \"user\", \"content\": \"I love TradingGoose!\"}]",
            "language": "json",
            "required": true
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Add Memories (<code>mem0_add_memories</code>)</div>

Add memories to Mem0 for persistent storage and retrieval

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `userId` | string | Yes | User ID associated with the memory |
| `messages` | json | Yes | Array of message objects with role and content |
| `apiKey` | string | Yes | Your Mem0 API key |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `ids` | array | Array of memory IDs that were created |
| `memories` | array | Array of memory objects that were created |

</div>



---

### Search Memories

<ShowcaseCard>
  <BlockConfigPreview
    name="Mem0"
    type="mem0"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "half",
            "placeholder": "Select an operation",
            "defaultValue": "search",
            "options": [
                {
                    "id": "add",
                    "label": "Add Memories"
                },
                {
                    "id": "search",
                    "label": "Search Memories"
                },
                {
                    "id": "get",
                    "label": "Get Memories"
                }
            ]
        },
        {
            "id": "userId",
            "type": "short-input",
            "title": "User ID",
            "layout": "full",
            "placeholder": "Enter user identifier",
            "required": true,
            "defaultValue": "userid"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Mem0 API key",
            "password": true,
            "required": true
        },
        {
            "id": "query",
            "type": "long-input",
            "title": "Search Query",
            "layout": "full",
            "placeholder": "Enter search query to find relevant memories",
            "required": true
        },
        {
            "id": "limit",
            "type": "slider",
            "title": "Result Limit",
            "layout": "full",
            "min": 1,
            "max": 50,
            "step": 1
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Search Memories (<code>mem0_search_memories</code>)</div>

Search for memories in Mem0 using semantic search

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `userId` | string | Yes | User ID to search memories for |
| `query` | string | Yes | Search query to find relevant memories |
| `limit` | number | No | Maximum number of results to return |
| `apiKey` | string | Yes | Your Mem0 API key |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `searchResults` | array | Array of search results with memory data, each containing id, data, and score |
| `ids` | array | Array of memory IDs found in the search results |

</div>



---

### Get Memories

<ShowcaseCard>
  <BlockConfigPreview
    name="Mem0"
    type="mem0"
    color=""
    hideHeader
    subBlocks={[
        {
            "id": "operation",
            "type": "dropdown",
            "title": "Operation",
            "layout": "half",
            "placeholder": "Select an operation",
            "defaultValue": "get",
            "options": [
                {
                    "id": "add",
                    "label": "Add Memories"
                },
                {
                    "id": "search",
                    "label": "Search Memories"
                },
                {
                    "id": "get",
                    "label": "Get Memories"
                }
            ]
        },
        {
            "id": "userId",
            "type": "short-input",
            "title": "User ID",
            "layout": "full",
            "placeholder": "Enter user identifier",
            "required": true,
            "defaultValue": "userid"
        },
        {
            "id": "apiKey",
            "type": "short-input",
            "title": "API Key",
            "layout": "full",
            "placeholder": "Enter your Mem0 API key",
            "password": true,
            "required": true
        },
        {
            "id": "memoryId",
            "type": "short-input",
            "title": "Memory ID",
            "layout": "full",
            "placeholder": "Specific memory ID to retrieve"
        },
        {
            "id": "startDate",
            "type": "datetime-input",
            "title": "Start Date",
            "layout": "half",
            "placeholder": "YYYY-MM-DD"
        },
        {
            "id": "endDate",
            "type": "datetime-input",
            "title": "End Date",
            "layout": "half",
            "placeholder": "YYYY-MM-DD"
        },
        {
            "id": "limit",
            "type": "slider",
            "title": "Result Limit",
            "layout": "full",
            "min": 1,
            "max": 50,
            "step": 1
        }
    ]}
  />
</ShowcaseCard>

<div className="mt-6 border-t border-fd-border pt-4">
<div className="text-base font-semibold mb-2">Get Memories (<code>mem0_get_memories</code>)</div>

Retrieve memories from Mem0 by ID or filter criteria

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Input</div>

| Parameter | Type | Required | Description |
| --------- | ---- | -------- | ----------- |
| `userId` | string | Yes | User ID to retrieve memories for |
| `memoryId` | string | No | Specific memory ID to retrieve |
| `startDate` | string | No | Start date for filtering by created_at \(format: YYYY-MM-DD\) |
| `endDate` | string | No | End date for filtering by created_at \(format: YYYY-MM-DD\) |
| `apiKey` | string | Yes | Your Mem0 API key |

<div className="text-sm font-medium text-fd-muted-foreground mt-4 mb-2">Output</div>

| Parameter | Type | Description |
| --------- | ---- | ----------- |
| `memories` | array | Array of retrieved memory objects |
| `ids` | array | Array of memory IDs that were retrieved |

</div>



---


## Usage Instructions

Integrate Mem0 into the workflow. Can add, search, and retrieve memories.



## Notes

- Category: `tools`
- Type: `mem0`
